# Post-Pivot Review Audit

Date: 2026-05-03
Generated by: `scripts/audit-post-pivot-iterations.ts`
Trigger: Phase 34e corrupted-review trust rebuild (workplan acceptance criterion).

## Method

Walks `.deskwork/entries/` (sidecars) and `.deskwork/review-journal/pipeline/` (legacy workflow records). For each entry whose `Σ iterationByStage` exceeds the matching workflow record's `currentVersion`, surfaces the delta. The premise: pre-Phase-34a, the studio dashboard linked to the legacy review surface, which read frozen workflow records — so any approve/reject during that window could have been against stale content if the entry had been iterated post-pivot via the entry-centric path.

Audit run output:

```
Audit: 4 sidecars × 7 workflow records
Project: /Users/orion/work/deskwork-work/deskwork-plugin

Found 1 entries where sidecar iteration count exceeds the legacy workflow currentVersion:

  - deskwork-plugin/prd (9845c268-670f-4793-b986-0433e9ef4fb9)
      title: Prd
      currentStage: Final
      sidecar Σ iterations: 4
      workflow currentVersion: 1 (state=applied)
      delta: +3 iteration(s) recorded post-pivot
```

## Disposition per entry

### `deskwork-plugin/prd` (9845c268-670f-4793-b986-0433e9ef4fb9)

**Status: NOT a corrupted review.**

Reasoning: per the session journal entry 2026-05-03 ("v0.13.0 ships; F1's `prompt()` IOU surfaces; 'no just for now' rule lands; Phase 34 reframed"), the PRD was iterated to v4 during Phase 34 planning and "approved via direct file-diff bypass" because the studio review surface was itself the gate that 34a would fix. The operator reviewed v4 directly against the file diff, NOT against the stale v1 workflow record.

The chicken-and-egg was named explicitly at the time and the bypass was logged in [#170](https://github.com/audiocontrol-org/deskwork/issues/170). No approval click was made against stale content during the window — the bypass was the substitution.

Going forward (post-34a): future iterations of this PRD will go through the now-functional entry-keyed review surface, which sources from the sidecar + history journal directly. The legacy workflow record stays frozen at v1 (it's a historical artifact); the entry-keyed surface is the canonical review path.

No re-review needed. No content diff to document — every iteration is currently visible at `/dev/editorial-review/entry/9845c268-670f-4793-b986-0433e9ef4fb9` as part of the version-strip's history.

## Summary

| Entry | Status | Disposition |
|---|---|---|
| `deskwork-plugin/prd` | Iterated post-pivot, not corrupted | Operator reviewed v4 via file-diff bypass per #170; no stale-content approval occurred |

**Trust rebuild outcome: zero entries require re-review.** The audit's purpose was to surface entries where a pre-34a operator approval might have been against frozen workflow content. The single entry surfaced here was reviewed via the documented bypass path during the same window 34a was scoped, so the bypass IS the trust signal.

The audit script (`scripts/audit-post-pivot-iterations.ts`) is committed alongside this doc and re-runnable. If future iterations create new mismatches that AREN'T covered by the bypass-during-34a-scoping context, they should be re-reviewed via the entry-keyed surface and added to this document.
