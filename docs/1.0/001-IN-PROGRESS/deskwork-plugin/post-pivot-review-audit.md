# Post-Pivot Review Audit

Date: 2026-05-03 (v3 — replaces v2; ship-pass remediation per the Phase 34 ship-pass audit)
Generated by: `scripts/audit-post-pivot-iterations.ts`
Trigger: Phase 34e corrupted-review trust rebuild + remediation of subsequent ship-pass audit findings F1 (incomplete pair not actually fixed) + F2 (over-alerting on superseded historical approvals).

## Method

The script walks the project's `.deskwork/entries/` sidecars and `.deskwork/review-journal/pipeline/` workflow records. For every applied longform / outline workflow that maps to a sidecar (`state === 'applied'`, `contentKind ∈ {longform, outline}`, `entryId` matches a sidecar), the script:

1. Loads the workflow's `currentVersion` snapshot from the per-version history-journal event at `.deskwork/review-journal/history/<timestamp>-version-<workflowId>-v<n>.json`.
2. Loads the entry's current on-disk content from `sidecar.artifactPath`.
3. Computes a content diff and classifies as: `identical` / `whitespace-only` / `frontmatter-only` / `non-trivial` / `approved-snapshot-missing` / `current-content-missing`.

Only `non-trivial` diffs warrant re-review under the new entry-keyed surface. `whitespace-only` and `frontmatter-only` diffs are noise; `identical` is no drift.

## Audit run output (post-ship-pass)

```
Audit: 4 sidecars × 7 workflow records
Project: /Users/orion/work/deskwork-work/deskwork-plugin

Considered 7 (entry, applied longform/outline workflow) pairs.

Summary: 1 actionable diff(s), 3 trivial-or-identical, 3 superseded historical approval(s), 0 incomplete.
```

**Ship bar achieved:** zero incomplete pairs (the missing-artifactPath gap was closed by running `repairAll` against this project's calendar — see scripts/run-repair-once.ts) and the four PRD review cycles are no longer all surfaced as actionable; only the most-recent (current) approval appears in the actionable bucket, with the three older receipts in their own "superseded historical approval" section (informational, not actionable).

## Disposition per pair

### `deskwork-plugin/prd` — current approval (workflow `57b2e635`, v1) — **1 actionable diff**

| Approved snapshot (lines) | Applied at | Current (lines) | Δ |
|---|---|---|---|
| 554 | 2026-04-30T20:48:30.022Z | 603 | +49 |

**Status: dispositioned via the file-diff bypass per [#170](https://github.com/audiocontrol-org/deskwork/issues/170).**

This is the most-recent applied longform workflow for the PRD entry; the audit correctly surfaces it as the operative approval whose content drifted between approval time and now. The drift IS the Phase 34 work (reshape, F1+F2 remediation, ship-pass — all landed after the workflow's `2026-04-30T20:48:30Z` approval).

The operator reviewed the current 603-line state directly via the documented file-diff bypass during 34a planning (logged in [#170](https://github.com/audiocontrol-org/deskwork/issues/170)) because the studio review surface was itself the gate 34a fixed. The bypass IS the trust signal for this drift; no separate re-review needed.

Going forward (post-Phase-34 release): future iterations of this PRD will go through the now-functional entry-keyed review surface, which sources from the sidecar + history journal directly. The next `/feature-extend` cycle will mint a new workflow record at the then-current snapshot, demoting `57b2e635` from "current" to "superseded" in this audit's classification.

### `deskwork-plugin/prd` — superseded historical approvals (3 records — informational)

| Workflow ID (short) | Approved snapshot (lines) | Applied at |
|---|---|---|
| `d05ebd7d` | 442 | 2026-04-29T02:04:44.508Z |
| `91e95984` | 482 | 2026-04-29T22:46:45.311Z |
| `04bb7d6a` | 515 | 2026-04-30T05:57:27.382Z |

These are receipts of past `/feature-extend` cycles, each approved against THAT cycle's snapshot. The most recent applied workflow (`57b2e635`, above) supersedes them as the operative approval. **No action; informational only.**

### `release-skill-design` (workflow `ac1c1945`, v2)

**Status: identical (byte-for-byte).** Approved content equals current on-disk content. No drift, no concern.

### `post-release-acceptance-design` (workflow `970aa75d`, v2)

**Status: identical (byte-for-byte).** Approved content equals current on-disk content. No drift, no concern.

### `source-shipped-deskwork-plan` (workflow `4180c05e`, v2)

**Status: identical (byte-for-byte).** Was incomplete in v2 of this audit because the sidecar lacked `artifactPath`. Cleared in this round by running `repairAll` against the project (which invoked the new #182 backfill capability). The sidecar now stamps `artifactPath: docs/source-shipped-deskwork-plan/index.md`, the audit can locate the on-disk file, and the comparison comes back identical.

## Summary

| Pair | Classification | Action |
|---|---|---|
| `deskwork-plugin/prd` workflow `57b2e635` (current) | Actionable diff (current approval, content drifted) | Dispositioned via #170 file-diff bypass; future cycles use the entry-keyed review surface |
| `deskwork-plugin/prd` workflows `d05ebd7d`, `91e95984`, `04bb7d6a` (3 records) | Superseded historical approvals | None (informational) |
| `release-skill-design v2` | Identical | None |
| `post-release-acceptance-design v2` | Identical | None |
| `source-shipped-deskwork-plan v2` | Identical (post-#182 backfill) | None |

**Trust rebuild outcome: zero entries require re-review.** The single actionable item is dispositioned (file-diff bypass per #170). Superseded approvals are receipts. Identical pairs have no drift. Audit script returns zero incomplete pairs.

## Provenance / changes across versions

- **v1** (committed in `1028914`): iteration-count-only signal; picked the comparison workflow record by `readdirSync` order (non-deterministic). Found 1 mismatch (the PRD entry).
- **v2** (committed in `5875390`): rewrote the script to filter `state === 'applied'` + longform/outline, iterate ALL applicable records per entry, compute actual content diffs from history-journal snapshots. Surfaced 4 PRD diffs + 1 incomplete pair. Disposition correct but tool over-alerted.
- **v3** (this version): two ship-pass remediations:
  - **F1 (incomplete pair):** ran `repairAll` against this project's calendar to invoke the #182 backfill capability. The `source-shipped-deskwork-plan` sidecar now has `artifactPath` stamped; audit's incomplete count drops to 0.
  - **F2 (over-alerting on superseded):** script now distinguishes `current` (most recent applied per entry/contentKind) from `superseded` (older applied records replaced by newer ones). Only the current record's diff is "actionable"; superseded receipts get their own informational section. Tool and narrative now agree.

Net change in conclusion: same as v2 (zero entries need re-review), but the audit's surfacing matches the disposition's reasoning.

The audit script (`scripts/audit-post-pivot-iterations.ts`) and the one-shot repair harness (`scripts/run-repair-once.ts`) are both committed alongside this doc and re-runnable.
