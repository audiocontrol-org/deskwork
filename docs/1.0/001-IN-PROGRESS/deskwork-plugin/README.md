## Feature: deskwork-plugin

Extract the editorial calendar skills from audiocontrol.org into an open-source Claude Code plugin. The plugin (codename "deskwork") manages the editorial lifecycle from idea capture through publication and distribution tracking. Ships as part of a multi-plugin monorepo.

### Status

| Phase | Description | Status |
|---|---|---|
| 1 | Monorepo bootstrap and plugin skeleton | Complete |
| 2 | Adapter layer and config schema | Complete |
| 3 | Core lifecycle skills | Complete |
| 4 | Dogfood in audiocontrol.org | Deferred to Phase 12 |
| 5 | Visibility and distribution skills | Not started |
| 6 | Cut over and cleanup | Not started |
| 7 | Extract @deskwork/core npm package | Complete |
| 8 | Extract @deskwork/cli with single dispatcher | Complete |
| 9 | Build @deskwork/studio (Hono server + scaffolds) | Complete |
| 10a | Re-sync @deskwork/core from upstream editorial-calendar | Complete |
| 10b | Port studio UI assets + page renders | Complete |
| 11 | Add deskwork-studio plugin shell + marketplace entry | Complete |
| 12 | End-to-end dogfood (CLI + studio) against sandbox | Complete |
| 13 | Hierarchical content + scrapbook secret subdir | Complete |
| 14 | Versioning, release process, and build correctness | Complete (v0.1.0, v0.2.0 released) |
| 15 | `deskwork ingest` — backfill existing content into calendar | Complete ([#15](https://github.com/audiocontrol-org/deskwork/issues/15), [PR #17](https://github.com/audiocontrol-org/deskwork/pull/17)) |
| 16 | Hierarchical content gaps + scrapbook-in-review + bird's-eye content view | Complete ([#18](https://github.com/audiocontrol-org/deskwork/issues/18); design mockup at [`mockups/birds-eye-content-view.html`](mockups/birds-eye-content-view.html)) |
| 17 | Cross-page editorial nav + studio index at `/dev/` | Complete (v0.5.0; design mockup at [`mockups/editorial-nav-and-index.html`](mockups/editorial-nav-and-index.html)) |
| 18 | Deferral catalog — finish the deferred work | Group A code items shipped in v0.6.0 ([#16](https://github.com/audiocontrol-org/deskwork/issues/16), [#24](https://github.com/audiocontrol-org/deskwork/issues/24), [#27](https://github.com/audiocontrol-org/deskwork/issues/27), [#28](https://github.com/audiocontrol-org/deskwork/issues/28), [#29](https://github.com/audiocontrol-org/deskwork/issues/29), [#31](https://github.com/audiocontrol-org/deskwork/issues/31) — all 10 cross-surface design audit unifications). [#30](https://github.com/audiocontrol-org/deskwork/issues/30) conditional. Group B (Phase 4/5/6) and Group C (MCP/npm/shortform-cross-link) remain operator decisions. |
| 19 | Separate identity (UUID) and path-encoding (frontmatter id) from slug; `deskwork doctor` validate/repair | Shipped v0.7.0 ([PR #35](https://github.com/audiocontrol-org/deskwork/pull/35)) + v0.7.1 review-handler/dashboard fixes ([PR #36](https://github.com/audiocontrol-org/deskwork/pull/36)) + v0.7.2 frontmatter round-trip + `deskwork:` namespace ([PR #39](https://github.com/audiocontrol-org/deskwork/pull/39)); [#33](https://github.com/audiocontrol-org/deskwork/issues/33) 19e operator verification still pending |
| 20 | Move outline content out of user-supplied markdown into the scrapbook (minimize intrusion principle) | Not started ([#40](https://github.com/audiocontrol-org/deskwork/issues/40)) |
| 21 | End-to-end shortform composition (LinkedIn) through the unified review surface | Shipped v0.8.0 ([PR #48](https://github.com/audiocontrol-org/deskwork/pull/48); [#47](https://github.com/audiocontrol-org/deskwork/issues/47)). Operator can now author cross-platform shortform posts through the unified review surface; `shortform-start` + `distribute` skills + CLI subcommands. |
| 22 | Install / studio / doctor polish ([#41](https://github.com/audiocontrol-org/deskwork/issues/41)–[#46](https://github.com/audiocontrol-org/deskwork/issues/46)) | Shipped v0.8.0 (bundled with Phase 21 in [PR #48](https://github.com/audiocontrol-org/deskwork/pull/48)). Six fixes: install schema docs, install pre-flight schema probe, install existing-pipeline detection, studio EADDRINUSE auto-increment, doctor exit-code + verbosity + skipReason, scrapbook hierarchical-path docs verified. |
| 22+ | dev-source boot fix | Shipped v0.8.1 ([PR #50](https://github.com/audiocontrol-org/deskwork/pull/50); [#49](https://github.com/audiocontrol-org/deskwork/issues/49)). Cross-package relative `.ts` imports broke `tsx`-from-source boot; `outline-split.ts` promoted to `@deskwork/core/outline-split`, `editorial-skills-catalogue.ts` collocated under `packages/studio/src/lib/`. |
| 22++ | proximate packaging fix | Shipped v0.8.3. `.gitignore` exception for `plugins/deskwork-studio/public/dist/` so client JS bundles ship in the marketplace tarball; release-workflow verification extended to cover `public/dist/`. Unblocks studio interaction layer for adopters until Phase 23 retires the bundle/dist trap entirely. |
| 22+++ | non-website collection support | Shipped v0.8.2 (schema) + v0.8.4 (slash-command rename in studio buttons) + v0.8.5 (re-copy affordance for stuck workflows). `host` field becomes optional; install skill detects non-website collections; studio button-handlers emit `/deskwork:*` instead of legacy `/editorial-*`. Discovered via dogfood install in this monorepo. Phase 24 completes the broader collection-vocabulary reframe. |
| 22++++ | UUID-binding bug cluster (pre-Phase-23 prep) | Shipped v0.8.6. 16 UX issues filed during 2026-04-28 dogfood; 4 bugs shared a root cause (CLI subcommands resolved files by slug-derived path instead of `deskwork.id` UUID frontmatter). Centralized `resolveEntryFilePath` in `@deskwork/core/paths`; refactored `review-start`/`approve`/`iterate`/`publish`; `outline` refuses duplicates when entry already bound; `ingest --apply` writes `deskwork.id` to file frontmatter; content-tree displays real on-disk path. Closed [#63](https://github.com/audiocontrol-org/deskwork/issues/63), [#66](https://github.com/audiocontrol-org/deskwork/issues/66), [#67](https://github.com/audiocontrol-org/deskwork/issues/67), [#70](https://github.com/audiocontrol-org/deskwork/issues/70). 21 new test cases. |
| 22+++++ | Studio skill description fix | Shipped v0.8.7. Skill description claimed "loopback only" but Step 3 of the body documents Tailscale-aware default. Drift fixed; description matches body. Surfaced because the agent reflexively passed `--no-tailscale` based on the wrong description. |
| 23 | Source-shipped re-architecture (drop the bundles) | **Blocker fixes shipped + bin/script dedup landed; v0.9.0 ships via /release skill (Phase 25).** This-session commit `9610ad0` closes #76 (bin wrapper race + portable mkdir lock), #77 (esbuild concurrent-boot race + atomic tmp+rename + self-healing sidecar), #78 (materialize-vendor mode-bit + symlink-traversal guard), #79 (smoke-marketplace SIGINT + port pre-flight). Plus operator-driven dedup: `packages/cli-bin-lib/install-lock.sh` (wrappers shrank 195→22 lines each); smoke now sources `materialize-vendor.sh::materialize_vendor_pairs` (single source of truth for symlink-guard + mode-bit verification); 8 new canonicalize_relative edge-case tests. 683 workspace tests pass. |
| 24 | Content collections (not websites) | Not started. Will follow v0.9.0 (decoupled from Phase 23 — original "coordinated" plan re-scoped because Phase 23 alone is a substantial release). Schema rename `sites` → `collections`. Install skill detects content collections without assuming a website. Studio surfaces drop `host` requirement. Doctor migration rule for existing adopters. Plan: [`docs/source-shipped-deskwork-plan/index.md`](../../../source-shipped-deskwork-plan/index.md). |
| 25 | `/release` skill — enshrine the release procedure | **Implementation complete; first canonical run = ship v0.9.0.** Driven by operator principle: *"What we do 'just for now' overwhelmingly becomes conventional canon. So, if we want a sane release process, we MUST enshrine it in a skill."* Project-level skill at `.claude/skills/release/`. Three TypeScript helpers (`validateVersion`, `checkPreconditions`, `atomicPush`) + CLI dispatcher invoked via tsx. Hard-gated 4-pause flow (precondition+version → post-bump diff → smoke+tag-message → final push). Atomic push: `git push --follow-tags origin HEAD:main HEAD:refs/heads/<branch>`. Re-tag prevention via `git ls-remote --tags`. Brainstormed → spec ([`docs/superpowers/specs/2026-04-29-release-skill-design.md`](../../../superpowers/specs/2026-04-29-release-skill-design.md), workflow `ac1c1945` applied at v2 — bash→TypeScript switch from operator review) → 12-task plan ([`docs/superpowers/plans/2026-04-29-release-skill.md`](../../../superpowers/plans/2026-04-29-release-skill.md)) → subagent-driven implementation. 13 commits (`63efd9b`…`3187227`). 20 skill tests + 683 workspace tests + manual integration smoke against sandbox remote — all green. RELEASING.md rewrites to point at the skill + adds explicit pre-1.0 maturity stance with revisit-at-1.0 trigger. Filed [#84](https://github.com/audiocontrol-org/deskwork/issues/84) (deskwork:iterate UX gap surfaced during dogfood of the spec through the deskwork pipeline). |
| 25+ | v0.9.0/0.9.1/0.9.2 release run + post-release install-blocker | v0.9.0 shipped via the `/release` skill's first canonical run. v0.9.1 + v0.9.2 followed as patch fixes (materialize-vendor `node_modules` exclude; husky pre-push posix-compat). Marketplace-install dogfood surfaced [#88](https://github.com/audiocontrol-org/deskwork/issues/88): every adopter's `vendor/` was empty because Claude Code reads `marketplace.json` from the marketplace repo's default branch (not the tag), and main only ever had the symlinks. Dangling symlinks got stripped on copy, leaving `vendor/cli-bin-lib/install-lock.sh` missing and the bin wrapper dying on line 17. Same family as [#81](https://github.com/audiocontrol-org/deskwork/issues/81). |
| 25++ | Marketplace `source.ref` pin (#88, #81) | Shipped v0.9.3 (commit `f70e1ac` + `8b21d88`). Switched each plugin's `marketplace.json` `source` from a relative path to a `git-subdir` source pinned at the release tag. `scripts/bump-version.ts` now bumps `source.ref` alongside the version bump so the chore-release commit on main carries the new ref atomically. Release workflow gains a `Verify marketplace.json source.ref points at tag` gate that fails before publishing if the chore-release commit didn't bump the ref. End-to-end verified: sparse-clone of v0.9.3 tag at `plugins/deskwork` produces a tree where `vendor/cli-bin-lib/install-lock.sh` is a real file. Closed #88, closed #81. |
| 25+++ | `prepare: husky` defensive form (post-#88 followup) | Shipped v0.9.4 (commits `7f6961f` + `ea0c7b1`). Marketplace install runs `npm install --omit=dev` on the plugin shell; because the plugin sits inside the workspace tree (root `package.json` declares `workspaces`), npm walks up to the workspace root and runs the root's `prepare`. The root prepare invoked husky directly — under `--omit=dev` husky isn't installed, exit 127 (`husky: command not found`) failed the install. Defensive form: `command -v husky >/dev/null 2>&1 && husky \|\| true`. Smoke didn't catch it because `scripts/smoke-marketplace.sh` extracts only `plugins/deskwork + packages/` (no root `package.json`). Smoke alignment with the real install path is a separate followup. |
| 25++++ | Local install-state mismatch on source-shape change | Filed [#89](https://github.com/audiocontrol-org/deskwork/issues/89). After v0.9.3+ marketplace.json source-shape change, Claude Code's `installed_plugins.json` registry retained a stale `installPath` from the relative-path-source layout while the plugin moved to a different on-disk path. `command -v deskwork` returns empty despite reload reporting the plugin loaded. Migration-only friction (fresh adopters never hit it). Workaround documented in MIGRATING.md: clear stale registry entry + reinstall fresh. |
| 26 | npm-publish architecture pivot — v0.9.5 | **Shipped v0.9.5.** Pivot from source-shipped vendor/materialize architecture to npm-published `@deskwork/{core,cli,studio}@0.9.5` packages. Plugin shells first-run-install from npm registry and dispatch via `node_modules/.bin/`. Vendor/materialize/source.ref machinery retired entirely. Closes [#93](https://github.com/audiocontrol-org/deskwork/issues/93). Sub-phases 26a (package.json setup, `da2c921`), 26b (dist build + exports + files, `d8eec0e`/`fa0a229`/`5c1aa09`), 26c (bin shim rewrite, `3ec075d`/`898917d`), 26e (vendor retirement, `45dc30f`/`5704cdd`) all complete. Smoke rewritten for npm-install path (`f3012e1`); release.yml stripped of test/marketplace verification per the project's "no test infrastructure in CI" rule (`f7d447e`). Filed [#97](https://github.com/audiocontrol-org/deskwork/issues/97) (studio runtime deps in devDependencies — workaround: plugin shell declares them). Phase 26 tracking issue: [#94](https://github.com/audiocontrol-org/deskwork/issues/94). 26d (`deskwork-studio` → `dw-studio` rename) and 26-CI (publish via Trusted Publishers) deferred. |
| 26+ | Packaging follow-ups — v0.9.6 | Complete. Four post-Phase-26 packaging defects landed in a single PR: [#97](https://github.com/audiocontrol-org/deskwork/issues/97) (`@deskwork/studio` runtime deps moved from `devDependencies` to `dependencies` — `esbuild`, `@codemirror/*`, `@lezer/highlight`; plugin-shell workaround removed), [#95](https://github.com/audiocontrol-org/deskwork/issues/95) (customize CLI re-anchored on `dist/<category>/<name>.ts` because `src/` is excluded from the `files` whitelist; build pipeline copies `.ts` source files verbatim into `dist/` mirroring the existing `.mjs` precedent in `@deskwork/core`; +2 tarball-shape regression tests, 685 total), [#96](https://github.com/audiocontrol-org/deskwork/issues/96) (per-package READMEs authored for `packages/{core,cli,studio}` so npmjs.com landing pages aren't blank), [#100](https://github.com/audiocontrol-org/deskwork/issues/100) (`deskwork-studio` SKILL.md Step 4 + plugin shell READMEs rewritten to describe the Phase 26 three-tier wrapper resolution; all "Phase 23" / "source-shipped" / "vendor" stale references purged from plugin skill bodies + READMEs). |
| 26++++ | `deskwork repair-install` recovery for #89 — v0.9.8 | New `deskwork repair-install` subcommand prunes stale `*@deskwork` entries from `~/.claude/plugins/installed_plugins.json` (the Claude Code plugin registry that's accumulating ghost installPath entries — see [#89](https://github.com/audiocontrol-org/deskwork/issues/89)). Adopter recovery path documented in `plugins/deskwork/README.md`. Runs portably via the marketplace-clone bin path so adopters with broken PATH can self-heal: `~/.claude/plugins/marketplaces/deskwork/plugins/deskwork/bin/deskwork repair-install`. 9 unit tests, 766 workspace tests pass. Upstream Claude Code issue filed in parallel for the registry-hygiene root cause. |
| 27 | Studio bug tranche — v0.10.0 | **Shipped.** All 7 sub-phases A–G landed; closed [#103](https://github.com/audiocontrol-org/deskwork/issues/103), [#104](https://github.com/audiocontrol-org/deskwork/issues/104), [#105](https://github.com/audiocontrol-org/deskwork/issues/105), [#106](https://github.com/audiocontrol-org/deskwork/issues/106), [#107](https://github.com/audiocontrol-org/deskwork/issues/107), [#108](https://github.com/audiocontrol-org/deskwork/issues/108), [#110](https://github.com/audiocontrol-org/deskwork/issues/110), and [#69](https://github.com/audiocontrol-org/deskwork/issues/69)/[#74](https://github.com/audiocontrol-org/deskwork/issues/74)/[#99](https://github.com/audiocontrol-org/deskwork/issues/99) subsumed. New unified clipboard helper (`plugins/deskwork-studio/public/src/clipboard.ts`) with manual-copy fallback panel; rename form extracted to `rename-form.ts`. Studio tests grew 200 → 211 (+11 regression cases). Filed [#124](https://github.com/audiocontrol-org/deskwork/issues/124) follow-up — rename submit emits `/editorial-rename-slug` (no such command in OSS). |
| 28+ | SessionStart hook hint in `repair-install.sh` — v0.10.2 | **Shipped.** Issue [#132](https://github.com/audiocontrol-org/deskwork/issues/132) — the v0.10.1 README documents the SessionStart auto-repair hook as a manual JSON snippet to paste into `~/.claude/settings.json` (or project-scope), but no deskwork-owned surface exists for an adopter agent to install it on the operator's behalf. The agent had to either point the operator at the README and stop, or reach for the privileged `update-config` harness skill (the workaround that surfaced the gap). Operator decided on the smaller-shape fix instead of a new install surface: hint, not install. `scripts/repair-install.sh` now prints a one-screen TIP at the end of default + `--check` output when neither `~/.claude/settings.json` nor `./.claude/settings.json` contains the hook. Suppressed in `--quiet` mode (preserves SessionStart-hook silence-on-healthy contract). Detection is a substring match on `repair-install.sh` against the JSONC files (leans on the script-path stability rule shipped in v0.10.1 — no JSONC parser needed). Operator (or agent) follows the README's manual snippet to install. Issue left open for operator to close after fresh-session verification (per the "issue closure is the customer's call" rule). |
| 29 | Post-release customer acceptance playbook (`/post-release:walk` + `/post-release:file-issues`) | Not started ([#133](https://github.com/audiocontrol-org/deskwork/issues/133)). Design v2 applied 2026-04-30 via deskwork workflow `970aa75d` (commit `b1f1815`). Pair of skills + structured playbook + findings-doc round-trip through deskwork's review pipeline + `gh issue create` per approved finding. **Stop-gap framing (binding):** the entire feature — `/release` + the planned `/post-release:*` family — is scaffolding inside the deskwork plugin only because dw-lifecycle has not yet shipped customizable lifecycle stages; migrates into dw-lifecycle when that capability lands. Source-of-truth: [`post-release-acceptance-design.md`](../../post-release-acceptance-design.md). Sub-phases A–G enumerated in workplan. **Status note (2026-04-30):** likely deferred behind Phase 30 (deskwork pipeline redesign) since the new entry-centric model becomes the foundation Phase 29 will eventually build on. |
| 34 (all sub-phases shipped + released as v0.14.0) | Retire legacy review surface; complete Phase-30 migration; pay down v0.13.0 IOUs; sub-phase sweep + audit-clean ship-pass | **Shipped to npm + marketplace as v0.14.0** (release commit `903ee13`, tag `v0.14.0`, GitHub release [v0.14.0](https://github.com/audiocontrol-org/deskwork/releases/tag/v0.14.0)). Tag message: *"Phase 34 — retire legacy review surface, complete Phase-30 migration; full sub-phase sweep + audit-clean ship-pass."* `/release` skill drove the 5-pause flow: precondition (24 commits ahead of origin/main, FF-possible) → version (operator chose 0.14.0) → manifest bump (11 files, 18+/18−) committed as `903ee13` → operator ran `make publish` (3× OTP) → smoke (3-plugin clean install + studio boot + routes 200 + per-asset 200) → operator-accepted agent-drafted tag message → atomic-push to main + feature branch + tag. Single empty-scrapbook-note bookkeeping commit (`7dabd06`) cleared the working-tree precondition before bump. **24 substantive commits across all five 34-sub-phases + ship-pass + 3 audit-remediation rounds.** **34a** ([Layer 1 `a7e5804`](https://github.com/audiocontrol-org/deskwork/commit/a7e5804) → [Layer 2 `99c732a`](https://github.com/audiocontrol-org/deskwork/commit/99c732a) → [Layer 3 `bfc9bf7`](https://github.com/audiocontrol-org/deskwork/commit/bfc9bf7) → test cleanup `40086dd` → F1+F2 historical-mode remediation `8dfd80f`): structural cutover. Entry-keyed annotation store + history-journal reader + 4 new entry-keyed API endpoints. Press-check chrome ported into `pages/entry-review/` per-component module tree (10 server modules + 11 client modules). Bare-UUID route restructured: workflow-id (shortform) renders via new slim `pages/shortform-review.ts`; everything else 301-redirects to `/dev/editorial-review/entry/<uuid>`. Slug catch-all retired. `pages/review.ts` deleted (710 lines). Every link emitter switched to entry-keyed. F1 (block live mutations when historical) + F2 (stage-disambiguated version URLs) shipped per the implementation audit. **34b** ([T1 `c93fc65`](https://github.com/audiocontrol-org/deskwork/commit/c93fc65) → [T2-T4 `59aeafe`](https://github.com/audiocontrol-org/deskwork/commit/59aeafe) → composer-audit remediation `37c6ae0`): inline composer restored (#166); editorial-review-client rejection-reason inline composer; full audit zero `window.{prompt,confirm,alert}` in production; new shared `inlineConfirm` helper; image dimensions extended to JPEG/WebP/GIF (#163); `.scrap-name-secret-mark` glyph (#164); edit-toolbar tooltips + `?` shortcuts entry (#175 newly filed + fixed). Two follow-ups filed: #173 (entry-keyed reject) + #174 (entry-keyed save). **34c** ([commit `a906e88`](https://github.com/audiocontrol-org/deskwork/commit/a906e88)): dev-mode now binds Tailscale by default (#165); client init audit (#156) → one dead bootstrap surfaced + filed (#176); dashboard `scrapbook ↗` cross-link per row (#157). **34d** ([commit `f9947e8`](https://github.com/audiocontrol-org/deskwork/commit/f9947e8)): triage + design surfacing — #151 resolved as field-name confusion (sidecar field is `datePublished` not `publishedDate`); #152 verified closed by 34a's chrome port; #158 split into 4 specific child issues (#177-#180) + umbrella closed; #153 design proposal posted with two paths + per-skill categorization. **34e** ([implementation `1028914`](https://github.com/audiocontrol-org/deskwork/commit/1028914) → audit remediation `5875390` → ship-pass remediation `0cdc9dc`): corrupted-review trust rebuild via `scripts/audit-post-pivot-iterations.ts` (v3 — distinguishes current vs. superseded approvals; reports zero incomplete pairs after #182 backfill); repo-wide `for now / TODO / migration window / ...` grep audit; one IOU (`editorial-review-client.ts:1692` outline-approve TODO) replaced with #181 reference. **Ship-pass** ([commit `ee05e2d`](https://github.com/audiocontrol-org/deskwork/commit/ee05e2d)): 6 pre-release fixes — #176 dead initLightbox deleted; #177 shared `--er-container-wide`; #178 dashboard heading "Editorial Studio" → "Press-Check"; #168 scrapbook back-to-review link; #167 enterEditMode handles empty-note no-`.scrap-preview` case; #182 doctor backfill artifactPath capability + 3 regression tests. **Audit-clean ship pass:** 3 implementation-audit rounds (Phase 34b composer audit; Phase 34e iteration-count→content-diff rewrite; ship-pass current-vs-superseded distinction), each with audit doc + remediation commit. **Tests:** Phase 34 cumulative 459 → 477 core (+18); 348 → 365 studio (+17 net; many legacy review.ts tests deleted alongside the 700-line code deletion, replaced by entry-review/* tests). **Issues filed during Phase 34:** #173 (reject), #174 (save), #175 (toolbar tooltips), #176 (dead init), #177-#180 (#158 split), #181 (outline-approve), #182 (doctor backfill). **Issues fix-landed (closure pending operator marketplace-walk verification per `agent-discipline.md`):** #154, #155, #159, #160, #161, #166, #167, #168, #170, #171, #176, #177, #178, #182, #108, #114. **Operator post-release verification:** `/plugin marketplace update deskwork` ran post-tag — 3 plugins bumped to v0.14.0; SessionStart auto-repair hook reported `repaired deskwork@0.14.0 deskwork-studio@0.14.0 dw-lifecycle@0.14.0` cleanly. |
| 34 | Retire legacy review surface; complete Phase-30 migration; pay down v0.13.0 IOUs | **PRD + workplan + issues filed.** Triggered by live audit during Phase 34's own PRD review: the studio's longform editorial review surface is structurally broken — dashboard's per-row link routes to legacy `pages/review.ts` (reads pre-Phase-30 workflow records), `iterateEntry` writes only to entry-centric sidecars + history journal. Result: studio shows frozen pre-2026-05-01 content for any post-pivot iterated entry. Press-check chrome looks right; data is silently stale. **34a is blocking** — until it ships nothing else has a working dogfood path. Sub-phases: **34a** (retire legacy `pages/review.ts` entirely; port press-check chrome to entry-review; delete legacy routes; update every link emitter; audit corrupted reviews); **34b** (F1–F6 IOU paydown — #166, #163, #164, edit-toolbar discoverability); **34c** (dev mode + interaction — #165, #156, #157); **34d** (data + content — #151, #153, #158-split; #152 folded into 34a); **34e** (v0.13.0 verification + closures — #154, #155, #159, #160, #161). PRD iterated to Drafting v4 → approved via direct file-diff bypass (the studio review surface 34a fixes is itself the gate). New rule landed alongside: `.claude/rules/agent-discipline.md` "No 'just for now' shortcuts" (commit `42eb837`) — Phase 34 is the rule's first proof-of-work; the legacy review surface is the textbook example of the failure mode. Issues: [#170](https://github.com/audiocontrol-org/deskwork/issues/170) (umbrella), [#171](https://github.com/audiocontrol-org/deskwork/issues/171) (34a structural fix). Filed during the same session: [#166](https://github.com/audiocontrol-org/deskwork/issues/166) (scrapbook + NEW NOTE uses native `window.prompt()` — F1 deleted the existing inline composer with an unredeemed "fix in F5" IOU; the trigger that motivated the new rule). |
| 33++ (F1-F6 shipped + released as v0.13.0) | Scrapbook Dispatch E (visual) — F1 through F6 complete; merged to main; **shipped as v0.13.0** (#161) | **Shipped to npm + marketplace as v0.13.0** (release commit `39a1add`, tag `ed8f3f4`, GitHub release [v0.13.0](https://github.com/audiocontrol-org/deskwork/releases/tag/v0.13.0)). Tag message: *"Scrapbook UI/UX redesign (F1-F6) + CI infrastructure rescue."* `/release` skill drove the 5-pause flow: precondition check → version (operator chose 0.13.0, was prepared as 0.12.2) → manifests bumped + committed → `make publish` (3× npm OTP, operator-side) → smoke (3 plugins clean install via marketplace path; studio boots; routes 200) → tag + atomic-push. **Operator post-release walkthrough surfaced the trigger for Phase 34**: clicking `+ NEW NOTE` opened a native `window.prompt()` modal — F1 had silently deleted the pre-existing inline composer (~80 lines, working) and replaced it with `prompt()` under a `// F5 will replace` code comment; F5's plan never scoped that work; F6 walkthrough signed "INTEGRATION VERIFIED" without clicking the button. The IOU shipped to release. Filed as [#166](https://github.com/audiocontrol-org/deskwork/issues/166); motivated the new agent-discipline rule. **Merged to main as `779e9fe` via PR [#162](https://github.com/audiocontrol-org/deskwork/pull/162); released v0.13.0 as the next session's first action.** Six-dispatch arc shipped per the plan + 4 `/frontend-design` design-review gates (G1-G4) + post-implementation `/frontend-design` reviews after every code-shipping dispatch. **F1** ([`44094ee`](https://github.com/audiocontrol-org/deskwork/commit/44094ee)): page rebuild + new markup tree (aside-LEFT, vertical card grid, per-kind ribbons, foot toolbar, 4-module client split). 330 → 331 tests. Spec-compliance + code-quality reviewers ran via `superpowers:subagent-driven-development`; 3 important fixes applied inline (10 type-cast violations replaced via `parseErrorBody`/`parseSavedItem` type-guards + `instanceof Element` narrowing; 2 silent `catch{}` fallbacks fixed; `readCtx` rewritten to read `data-site`/`data-path` attrs). **F2** ([`94ba7e9`](https://github.com/audiocontrol-org/deskwork/commit/94ba7e9)): per-kind preview refinement — md frontmatter strip via `stripFrontmatter`, JSON parse-then-stringify pretty-print, NUL-byte binary-as-text detect, empty-result short-circuit. G2 design gate signed off 3 amendments (typo fix `' '` → `'\\0'`; option-b parse-then-stringify; empty short-circuit). 331 → 338 tests. **F3** ([`6c5466d`](https://github.com/audiocontrol-org/deskwork/commit/6c5466d)): per-kind extra meta in `.scrap-card-meta` — `countLines` for md/txt, `countJsonKeys` for json, `readImageDimensions` PNG-only via IHDR header. JPEG/WebP/GIF deferred (graceful fallback, no orphan dot). 338 → 343 tests. **F4** ([`a8282b8`](https://github.com/audiocontrol-org/deskwork/commit/a8282b8)): client-side state coordination — single-expanded invariant + `syncAsideActive` + `syncUrlHash` (history.replaceState, not pushState — hash is mode within page) + `restoreFromHash` on bootstrap. Verified via 4-state behavioral sequence at 1440. 343 → 344 tests. **F5** ([`457b0a4`](https://github.com/audiocontrol-org/deskwork/commit/457b0a4)): drop zone + secret section — `renderDropZone` + `renderSecretSection` (conditional render); `renderCard` accepts `{secret?}` opt for `secret-item-N` id namespacing + "mark public" label flip; `wireDropZone` (drag+drop + click-to-pick + Enter/Space); `.scrap-drop[data-dragover="true"]` flips `dashed`→`solid` border (G3 amendment 2 — editorial "commitment imminent" signal without animation); `:focus-visible` outline (G3 amendment 1); deep-link to `#secret-item-N` correctly leaves all public aside links inactive (G3 amendment 3 verified). G3 ratified mockup section order (cards → drop → secret), no per-card secret diff (Russian-doll), drop zone IS the empty state. 344 → 348 tests. **F6** ([`6ff6617`](https://github.com/audiocontrol-org/deskwork/commit/6ff6617)): final integrated walkthrough doc + G4 sign-off ("INTEGRATION VERIFIED — issue #161 ready for operator review and closure"). All 15 mockup sections MATCH at 4 viewports. **CI infrastructure rescue (alongside merge):** discovered CI red since Phase 26 (~6 months); fixed in 3 commits — [`c1b13a9`](https://github.com/audiocontrol-org/deskwork/commit/c1b13a9) (build step before tests), [`292a9c7`](https://github.com/audiocontrol-org/deskwork/commit/292a9c7) (chmod +x dist bins after tsc emit — tsc emits 644), [`cc39363`](https://github.com/audiocontrol-org/deskwork/commit/cc39363) (`scripts/link-workspace-bins.sh` — npm 10.9 doesn't auto-link workspace bins to root `node_modules/.bin/`). Cold-build verified: 1043 passing / 0 failed / 40 skipped across all workspaces. CI green at `cc39363`. **Audit trail:** [`final-walkthrough.md`](../../superpowers/plans/2026-05-02-scrapbook-redesign-final-walkthrough.md) (section-by-section verdicts), [`design-reviews.md`](../../superpowers/plans/2026-05-02-scrapbook-redesign-design-reviews.md) (G1+G2+G3+G4 + per-dispatch post-impl reviews). **Issue closures pending operator verification post-release** (per `agent-discipline.md`): #154 / #155 / #159 / #160 / #161 — fix-landed, comments prepared, closure stays operator's call. **Non-blocking follow-ups disclosed in walkthrough:** JPEG/WebP/GIF dimensions support (file when adopters surface real need) + expanded-secret-card visual continuity (G3 explicitly recommended NOT pre-fixing; file if real friction surfaces). |
| 33++ (F1 in progress, superseded by row above) | Scrapbook Dispatch E (visual) — F1 page rebuild implemented uncommitted (#161) | **F1 work landed in working tree, not yet committed.** Subagent-driven execution per the plan: Subagent A landed F1.2 (failing test rewrite) + F1.3 (server template rewrite, 298 lines), reported DONE_WITH_CONCERNS — controller addressed 2 concerns (test fixture mtime ordering via `utimesSync`; 3 stale `api.test.ts` scrapbook tests updated to new vocabulary, 1 marked `it.skip` for F5). **G1 design gate** ran via `/frontend-design` skill — signed off the planner's CSS draft with 4 mandatory amendments: (G1.1) `body[data-review-ui="scrapbook"]` added to `editorial-review.css:124-125` body-bg selector group so paper-grain texture lands; (G1.2) `.scrap-card[data-search-out="true"] { display: none; }` for search-driven hide; (G1.3 + G1.5) `:focus-visible` outlines + `:focus-within` border-color on interactive elements; (G1.4) `prefers-reduced-motion` block disabling card hover transform. Subagent B landed F1.4 (`scrapbook.css` rewrite, 426 lines, all G1 amendments baked in) + F1.5 (client rewrite, split into 4 modules to honor 300-500 line cap: `scrapbook-client.ts` 198 lines orchestrator + `scrapbook-mutations.ts` 413 lines + `scrapbook-markdown.ts` 120 lines + `scrapbook-toast.ts` 45 lines), reported DONE_WITH_CONCERNS (module-split made without controller pre-approval; rich mutation handlers from prior 917-line client preserved; `new-note`/`upload` UX intentionally degraded for F1 — F5 restores the rich composer alongside the drop zone). **Post-F1 verification:** drove live page in Playwright at 4 viewports (1440 / 1024 / 768 / 390); all measurements match mockup (`pageGridCols: "272px 880px"` at 1440, aside-LEFT, `--er-proof-blue` ribbon for md kind, `Fraunces` aside title 25.6px, `JetBrains Mono` card name 13.6px, body bg confirmed loading SVG grain). Interactions verified: `/` shortcut focuses search, filter chips toggle aria-pressed + add `data-filtered-out`, card click flips to `data-state="expanded"` with `grid-column: 1/-1` + red-pencil border + lazy-loaded markdown body. `/frontend-design` post-F1 review surfaced **one medium-severity cascade-ordering deviation** (`@media (max-width: 64rem) { .scrap-aside { position: static } }` was placed BEFORE the `.scrap-aside { position: sticky }` base rule — both mockup and planner draft had this bug; visually OK at <=64rem because single-column has no scroll context, but during scroll the aside would pin to viewport top and main scroll beneath). **Cascade fix applied in-thread by controller** (2-line CSS reorder); verified live: at 1023px `position: "static"` (was `"sticky"`); at 1440px `position: "sticky"` (unchanged). Tests stay green at 330 passed / 0 failed / 11 skipped. **Audit trail** captured at [`2026-05-02-scrapbook-redesign-design-reviews.md`](../../superpowers/plans/2026-05-02-scrapbook-redesign-design-reviews.md) with G1 + post-F1 sign-offs. **Outstanding for F1 to ship (next session):** dispatch spec-compliance reviewer + code-quality reviewer per `superpowers:subagent-driven-development`; then commit F1 implementation as a single commit with falsifiable measurements per `.claude/rules/ui-verification.md`. Files dirty in working tree: `packages/studio/src/pages/scrapbook.ts`, `packages/studio/test/review-scrapbook-index-redesign.test.ts`, `packages/studio/test/api.test.ts`, `plugins/deskwork-studio/public/css/scrapbook.css`, `plugins/deskwork-studio/public/css/editorial-nav.css`, `plugins/deskwork-studio/public/css/editorial-review.css`, `plugins/deskwork-studio/public/src/scrapbook-client.ts`, `plugins/deskwork-studio/public/src/scrapbook-mutations.ts` (NEW), `plugins/deskwork-studio/public/src/scrapbook-markdown.ts` (NEW), `plugins/deskwork-studio/public/src/scrapbook-toast.ts` (NEW). |
| 33++ (planning) | Scrapbook Dispatch E (visual) — spec + 6-dispatch plan with /frontend-design gates (#161) | **Planned (no code yet).** Operator surfaced #161 ("Scrapbook UI/UX: Make it look and work like the mockup") after walking the post-Dispatch-E live page and confirming the prior session's "Dispatch E shipped" claim was true for FUNCTION (auto-fill grid, filter chips, search, peeks, expand-in-place, press-check tokens) but NOT for visual COMPOSITION (aside on right not left, no per-kind ribbons, cards lack mockup's vertical chrome, class vocabulary is `.scrapbook-*` long-form not mockup's `.scrap-*`). This session: spec + plan only — no code shipped. **Spec** ([`2026-05-02-scrapbook-redesign-impl-spec.md`](../../superpowers/specs/2026-05-02-scrapbook-redesign-impl-spec.md)): 13-section design + implementation specification covering aesthetic direction (locked, mirrors existing system), composition (mockup confirmed with 3 refinements: single-expanded model, expanded-state aside binding, dock-style drop zone), markup tree, migration approach (replace not morph; no compat shims), data-state model, aside cross-link architecture, folio nav extension scoped OUT, 5-dispatch decomposition, affordance compliance audit, verification protocol, constraints + risks. **Plan** ([`2026-05-02-scrapbook-redesign-dispatch-e-visual.md`](../../superpowers/plans/2026-05-02-scrapbook-redesign-dispatch-e-visual.md)): grew from 5 dispatches to 6 after operator caught a design-discipline gap (initial plan used `/frontend-design` only during planning, not during implementation or for sign-off). Amended plan mandates `/frontend-design` at four gates (G1: pre-CSS review before F1.4; G2: pre-preview-refinement before F2.2; G3: pre-secret/drop-zone before F5.2; G4: final integrated sign-off as new Dispatch F6) plus extends every dispatch's verification to require BOTH playwright AND `/frontend-design`. Audit-trail artifacts created during execution: `2026-05-02-scrapbook-redesign-design-reviews.md` (per-gate responses) + `2026-05-02-scrapbook-redesign-final-walkthrough.md` (F6's section-by-section review). 2 commits this session: [`3c186a4`](https://github.com/audiocontrol-org/deskwork/commit/3c186a4) (initial spec + plan), [`031f8e5`](https://github.com/audiocontrol-org/deskwork/commit/031f8e5) (gates + verification mandate amendment). Plan grew from ~1900 to ~2400 lines. Issue [#161](https://github.com/audiocontrol-org/deskwork/issues/161) updated with both [plan-link comment](https://github.com/audiocontrol-org/deskwork/issues/161#issuecomment-4364478610) and [amendment comment](https://github.com/audiocontrol-org/deskwork/issues/161#issuecomment-4364496518). Implementation deferred to next session — operator decision on subagent-driven vs inline execution still pending. |
| 33+ | Post-walkthrough polish + UI verification rules (#155, #159, #160) | **Shipped on `feature/deskwork-plugin`.** Operator walkthrough of the #154 redesign surfaced six follow-up bugs and one design-discipline gap. Six commits this session (`2200e61`, `6333150`, `86b155a`, `d474d35`, `b205a7c`, `7391783`) plus 2 new rule files (`.claude/rules/ui-verification.md`, `.claude/rules/affordance-placement.md`). **Bugs fixed:** [#155](https://github.com/audiocontrol-org/deskwork/issues/155) (longform strip wraps to 2 rows at desktop widths and eclipses marginalia head — `.er-strip` switched from `position: fixed` to `position: sticky; top: var(--er-folio-h)` so its actual rendered height takes space in document flow; body padding-top hack reduced from `calc(folio + 3.2rem)` to just `var(--er-folio-h)`; verified live, marginalia-head-to-strip-bottom gap went from -1.25px to +49px); manual page kicker cramped under folio (added `body[data-review-ui="manual"]` to `editorial-nav.css` padding-top selector group); asymmetric `.er-page` edges (dropped the `.er-page::before` 6px gradient + `--er-paper-4` token; both vertical edges now read uniform paper-3 hairline). **New issues filed + addressed:** [#159](https://github.com/audiocontrol-org/deskwork/issues/159) (edit-mode real-estate allocation — Source/Split/Preview + Focus mode already existed; only standalone marginalia toggle was missing) and [#160](https://github.com/audiocontrol-org/deskwork/issues/160) (edit-pane typography — editor body switched from Newsreader serif to JetBrains Mono at 0.875rem/1.5lh; CodeMirror's markdown parser was tagging YAML frontmatter as Setext H2 because the closing `---` reads as a Setext underline — fixed via `extensions: [{ remove: ['SetextHeading'] }]` on the markdown() language). **Marginalia toggle redesigned twice:** first iteration was a strip-right toolbar button (hidden in edit mode by an existing rule), second was a duplicate in the edit toolbar (worked but was inconsistent), final shape after operator pushed back on affordance principles is the on-component pull-tab pattern mirroring `.er-outline-tab`: `.er-marginalia-stow` chevron in the marginalia head when visible + `.er-marginalia-tab` vertical pull tab on the right edge when stowed; identical physical position across read AND edit modes; localStorage persistence; Shift+M keyboard. **Process:** six rounds of operator-driven corrective oversight (verification-skipping pattern + design-discipline gap) drove durable rule additions: `ui-verification.md` (non-negotiable pre-claim playwright checklist with falsifiable claims) + `affordance-placement.md` (component-attached over toolbar-attached; symmetric reveal/hide; pre-implementation gate). Both rules in `.claude/rules/` auto-load on session start. **Tests:** 334 → **338** (+4 net regression cases). **Carry forward:** Dispatch E shipped scrapbook FUNCTION (auto-fill grid, filter chips, search, expand-in-place, peeks) but NOT the mockup's visual COMPOSITION — aside is on the right instead of left, no per-kind colored top-edge ribbons, card chrome doesn't match. Operator-confirmed as an unimplemented Dispatch E (visual) — not yet scoped. **Issue closures pending operator verification (per agent-discipline rule):** #155 (fix landed, see [comment](https://github.com/audiocontrol-org/deskwork/issues/155#issuecomment-4364287662)); #159 / #160 stay open with grounded findings comments. |
| 33 | Studio UX/UI design pass — review surface + scrapbook redesign (#154) | **All five dispatches shipped on `feature/deskwork-plugin`.** This-session arc completes the architectural redesign of the review surface and scrapbook. **Dispatch A page-grid half** (commit `fe2de2d`): `.er-page` CSS Grid replaces `position: fixed` marginalia; new layout tokens (`--er-page-max`, `--er-article-col`, `--er-marginalia-col`, `--er-page-pad-x/y`, `--er-page-gap`, `--er-paper-4`); obsolete `body:has(.er-edit-mode:not([hidden])) .essay { max-width: calc(100vw - 19rem) }` workaround dropped. **Dispatch B** (commit `7ea34ff`): rotation per note (±0.35–0.4deg), hover/active straightens with -2px slide, mark→note cross-highlight via `pointerover`/`pointerout` event delegation reusing `setActiveHighlight`, `prefers-reduced-motion` skips both. **Dispatch C** (commits `5e5942f` `4002883` `b6e3bb0` `dc6e819`): split `renderEditMode` into `renderEditToolbar` (above `.er-page`) + `renderEditPanes` (inside `.er-draft-frame`); `body:has(.er-edit-toolbar:not([hidden]))` hides `.er-strip-right` + `.er-strip-center`; corrected from sticky to relative positioning to handle the strip's `flex-wrap` interaction. **Dispatch D** (commit `3d6dac5`): bottom-anchored expandable scrapbook drawer; `body[data-drawer]` height transition (4rem ↔ 22rem); peek line with truncation contract; standalone-viewer link demoted to small affordance; obsolete `@media (max-width: 900px) { display: none }` rule removed. **Dispatch E** (commits `2db7eb9` `883153c` `061329e` `b05be03`): scrapbook index rewrite — grid layout (`auto-fill, minmax(15rem, 1fr)`); filter chips (`all · md · img · json · txt · other`) with per-kind counts; search input with `/` shortcut; always-on server-side peeks (md/img/json/txt) replacing the empty-disclosure pattern; sticky aside on viewports ≥ 64rem wide and ≥ 50rem tall; per-card chrome. **Pre-existing latent bug surfaced + fixed during Dispatch E phase 3b:** `scrapbook-client.ts` exported `initScrapbook` at module top but never called it — every disclosure click + every CRUD button on the standalone scrapbook viewer was silently dead since 6b75985. Fixed with a `document.readyState`-gated bootstrap; broader audit filed as [#156](https://github.com/audiocontrol-org/deskwork/issues/156). **12 commits, +47 regression tests** across 5 dispatches; 287 → **334** studio tests passing; 0 regressions. Each dispatch verified live in Playwright at 1440px (Dispatch A also at 1024/768/390); 9 screenshots committed alongside integrations. **3 follow-up issues filed:** [#155](https://github.com/audiocontrol-org/deskwork/issues/155) (longform strip wraps to two rows in read mode and overlaps `.er-page` — pre-existing), [#156](https://github.com/audiocontrol-org/deskwork/issues/156) (`initScrapbook` bootstrap audit), [#157](https://github.com/audiocontrol-org/deskwork/issues/157) (studio dashboard does not link to scrapbook viewers — pre-existing UX discoverability gap). **Issue #154 closure pending operator visual review.** Mockups: [`review-redesign.html`](../../superpowers/frontend-design/2026-05-02-review-redesign/review-redesign.html), [`scrapbook-redesign.html`](../../superpowers/frontend-design/2026-05-02-review-redesign/scrapbook-redesign.html), [`README.md`](../../superpowers/frontend-design/2026-05-02-review-redesign/README.md). Final #154 wrap-up comment: <https://github.com/audiocontrol-org/deskwork/issues/154#issuecomment-4363592891>. **Prior session context (preserved):** Prior session: brainstorm → spec → plan → Phase 0 (5 MOOT / 2 ACTIVE) → Phase 1 glossary mechanism + working-surface restoration + 627-line refinement design doc. **This session (afternoon):** integrated the 11-issue longform-review refinement design doc in three chunked subagent dispatches of ≤4 issues each (chunking was forced by the prior session's 11-issue subagent dispatch timing out). Commits `282b383` / `e648a35` / `8eeda2f` / `9520187` (Issues 8/9/10/11 — folio hide, marginalia/scrapbook overlap fix, strip-inner content cap, responsive breakpoints + mobile drawer toggle); `9ca3890` / `c4663b8` / `ebe0600` / `661b468` (Issues 1/2/5/7 — filed-applied pill hide, Edit ⇄ glyph, two-tap kbd shortcut chips, edit-mode disclosure label); `3990e35` / `00ad749` / `06e202b` (Issues 3/4/6 — strip jargon glossary tooltips, "Reviews" → "Shortform" nav rename + active-state fix, empty-state copy tightening). 11 commits, +5 net tests (295 passing, 10 skipped). Each dispatch verified live in Playwright at desktop / tablet / phone; one dispatch had a stream-idle-timeout mid-flight and was resumed via SendMessage. **This session (evening):** brainstorm arc abandoned per operator framing (*"the brainstorming arc... produces considerably worse design results than using the frontend design plugin by itself"*); ran `/frontend-design` directly against [issue #154](https://github.com/audiocontrol-org/deskwork/issues/154) — produced two full-document HTML mockups + a 5-dispatch integration plan + diagnosis-by-concern rationale ([`docs/superpowers/frontend-design/2026-05-02-review-redesign/`](../../superpowers/frontend-design/2026-05-02-review-redesign/), commit `0995cd3`). Then shipped **Dispatch A's folio half** (commit `4c558f7`): root-cause was that `renderEditorialFolio` IS rendered server-side on every studio surface (already a single component) but `.er-strip` at `top: 0; z-index: 40` eclipsed the folio at `top: 0; z-index: 10` — global nav was invisible on review pages. Fixed by relocating folio to `position: fixed; top: 0; height: var(--er-folio-h); z-index: 60` + strip to `top: var(--er-folio-h); z-index: 40` + new italic Fraunces wordmark with `※` proof-mark + red-pencil bottom-bar active state. New layout tokens (`--er-folio-h`, `--er-page-max`, `--er-article-col`, `--er-marginalia-col`, `--er-drawer-h`). 295 studio tests pass. Verified live at 1440px on dashboard + review surface; integrated screenshots committed alongside the implementation. **Pending — Dispatches A (page-grid half) + B + C + D + E:** marginalia inside a CSS Grid column on the page (architectural fix for issue 154 concerns 1+2); marginalia behavior on the new layout; edit-mode toolbar relocation; real bottom scrapbook drawer (concern 3); scrapbook index always-on previews (concern 4). Issue #154 commented with full progress + integration plan: <https://github.com/audiocontrol-org/deskwork/issues/154#issuecomment-4363198375>. Mockups: [`review-redesign.html`](../../superpowers/frontend-design/2026-05-02-review-redesign/review-redesign.html), [`scrapbook-redesign.html`](../../superpowers/frontend-design/2026-05-02-review-redesign/scrapbook-redesign.html), [`README.md`](../../superpowers/frontend-design/2026-05-02-review-redesign/README.md). Earlier refinement doc: [`docs/superpowers/frontend-design/longform-review-refinement.md`](../../superpowers/frontend-design/longform-review-refinement.md). Spec: [`2026-05-01-studio-uxui-design.md`](../../superpowers/specs/2026-05-01-studio-uxui-design.md). Plan: [`2026-05-01-studio-uxui.md`](../../superpowers/plans/2026-05-01-studio-uxui.md). |
| 32 | Post-v0.12.0 pipeline-walk dogfood + bug-fix sweep — v0.12.1 | **Shipped v0.12.1** (this session). Source-complete arc landed via the `/release` skill; 14-issue marketplace-install verification arc closed all touched issues (#109/#110/#111/#112/#113/#117/#124/#143/#144/#145/#147/#148/#149/#150). Filed [#151](https://github.com/audiocontrol-org/deskwork/issues/151) (publish field gap) + [#152](https://github.com/audiocontrol-org/deskwork/issues/152) (entry-review CSS deferred TODO from Phase 30, addressed in Phase 33). Original phase-32-candidate entry was the prior session's work. In-thread one-session arc that exercised the entry-centric pipeline against this project's calendar, surfaced data-corruption + UX defects, and fixed all of them in the dev environment using `npm run dev` + `node_modules/.bin/deskwork` (the workflow shipped by Phase 31). 9 commits since v0.12.0; 12 issues touched. **Pipeline walk:** Ideas → Planned → ... → Published via the studio API (verified the Phase 31 surface) + via CLI (surfaced 3 fundamental bugs). **#147** CLI `approve` crashed with `TypeError` on entry-centric Ideas-stage entries (legacy `handleGetWorkflow` only) — fixed with dispatcher split mirroring `iterate.ts`. **#150** CLI `publish` wrote calendar.md without updating sidecar (legacy-only) — same fix shape; new `publishEntry` core helper. **#148** studio stage-transitions didn't regenerate calendar.md AND `doctor --check` missed the drift — extracted shared `regenerateCalendar(projectRoot)` helper called from all four entry helpers; new `calendar-sidecar` validator surfaces drift in --check mode. **#149** `doctor --fix=all` re-ran legacy migration on already-migrated state when calendar.md drifted (overwriting correct sidecar fields with stale heuristic data) — `detectLegacySchema` now gates on `.deskwork/entries/` directory presence, not calendar.md section names. **Studio UX sweep (#109/#111/#112/#117):** locale-aware `<time>` rendering via Intl.DateTimeFormat; `/api/dev/version` endpoint + masthead version display; compact empty-stage rendering (header-only, no placeholder body); status badges wrapped in `<a>` to review surface. **#124:** orphaned rename-form client deleted (dead since Phase 30 dashboard rewrite, never reaches an adopter). **Studio routing (#143/#144/#145):** bare `/dev/scrapbook/<site>` redirects to content view; bare `/dev/editorial-review` redirects to dashboard; slug-only `/dev/content/<site>/<slug>` for deeply-nested entries redirects to canonical deep path. **Already moot (verified):** #110 (uniform UUID-keyed row links from Phase 30) and #113 (site-filter chrome removed in dashboard rewrite). **Process:** new agent-discipline rule committed — "no issue closure until verified in a formally-installed release" (commit `4e38c53`); supersedes the prior agent-filed-vs-customer-filed distinction. **Tests:** 959 passing (was 933 baseline, +26 regression cases); 0 regressions. End-state: 9 commits since `08c1248` (`82c1bd6`…`0726b47`). |
| 31 | Local dev workflow + post-v0.11.1 dogfood fixes — v0.12.0 | **Shipped.** Brainstorm + plan + 35-task inline execution in one session. **Phase 0:** Vite-mounted-in-Hono dev workflow with HMR (`npm run dev --workspace @deskwork/studio` → tsx --watch + Vite middleware → HMR-on-save). Production code path unchanged; new `clientScriptTag`/`viteClientTag` helpers + `layout.ts` integration. **Phase 1 (#140 + #141):** added `Entry.artifactPath` schema field; `migrateCalendar` reads `sourceFile` from legacy ingest journal + iteration counts/`reviewState` from legacy pipeline-workflow records; doctor's `file-presence` validator + studio's `entry-resolver` consume `artifactPath` when present; `iterate.ts` + `entry-resolver.ts` read `contentDir` from `.deskwork/config.json` (was hardcoded `'docs/'`); `missing-frontmatter-id` SKIP_DIRS allows scrapbook stage artifacts; iterate gains a same-disk-as-last-iteration guard; iteration-history validator relaxed to fail only on `journalN > sidecarN` (the corruption direction). This project's calendar live-migrated clean. **Phase 2 (#146):** core entry-stage helpers (`approveEntryStage`/`blockEntry`/`cancelEntry`/`inductEntry`); studio POST endpoints `/api/dev/editorial-review/entry/:entryId/{approve,block,cancel,induct}`; `entry-review-client.ts` wires the buttons; `/dev/editorial-review/<uuid>` route delegates to entry-review surface when uuid is an entry sidecar. End-to-end Ideas → Planned → Ideas via studio API verified live. **Phase 3 (#139):** rewrote `printUsage()` along Phase 30's verb structure; deleted 9 retired-verb source files (`plan/outline/draft/pause/resume/review-*`); SUBCOMMANDS shrunk 20 → 11. **Phase 4 (#137 + #138):** dropped PATH-source from `versions_referenced()`; filtered registry walk by `scope`/`projectPath` in both `versions_referenced` and `prune_registry`. **Phase 5:** smoke-redesign extended; MIGRATING.md v0.12.0 entry; shipped via the `/release` skill. End-state: ~30 commits since `ac5d2f0`; ~915 tests passing across the workspace; 6 issues closed (#137, #138, #139, #140, #141, #146). |
| 30 | Deskwork pipeline redesign — entry-centric calendar with universal verbs | **Shipped v0.11.1.** Foundational rearchitecture in 7 phases / 42 tasks (subagent-driven). Eight stages (Ideas → Planned → Outlining → Drafting → Final → Published; Blocked + Cancelled off-pipeline). Per-entry JSON sidecars at `.deskwork/entries/<uuid>.json` as SSOT; `calendar.md` regenerated by `deskwork doctor`. Nine new universal verbs (skill prose); 9 retired CLI verbs gated by stable-error stub. `iterateEntry` helper (sidecar + journal + atomic write). Doctor expanded with 9 validation categories + repair classes. Studio dashboard rewritten (1029→503 lines across 5 files) for eight stages + per-row state; entry-uuid keyed review surface; Compositor's Manual rewritten. Migration via `deskwork doctor --check`/`--fix=all` (one-shot). Adopter walkthrough at MIGRATING.md (top section). **v0.11.0 abandoned** (smoke-failed: `zod` transitive missing — `@deskwork/core` workspace test passed via hoisting, marketplace install couldn't resolve); fix in `78afda2`, re-released as v0.11.1. End-state: 53 commits since `2d1a31a`; 422 core + 147+29 skipped CLI + 241+10 skipped studio tests (skipped are retirement-collateral, preserved with comments). Tasks 40 (audiocontrol dry-run) deferred — operator-driven. |
| 28 | Durable cache-restore script + auto-repair hook — v0.10.1 | **Shipped.** Customer-blocking [#131](https://github.com/audiocontrol-org/deskwork/issues/131) — Claude Code's plugin cache at `~/.claude/plugins/cache/deskwork/<plugin>/<version>/` gets evicted between sessions, leaving PATH wired against directories that no longer exist. New `scripts/repair-install.sh` lives at the marketplace clone path (durable across cache eviction); restores cache subtrees from the clone + prunes stale registry entries. Modes: default, `--quiet` (silent on healthy state, ~150ms; for SessionStart hooks), `--check` (read-only). Version banner when not `--quiet`. `deskwork repair-install` becomes a thin TS shell-out wrapper. README adopter snippet documents the SessionStart hook. New rule in `agent-discipline.md`: "Adopter-facing scripts have a stable CLI contract." End-to-end verified live by simulated cache wipe. Surfaced during this session's recursive dogfood — using deskwork to review the post-release acceptance design surfaced #131 because the operator couldn't leave margin notes (review-surface client JS 404 → cache eviction). |
| 26+++ | Wildcard inter-package dep fix — v0.9.7 | **Shipped.** Pinned `@deskwork/core` in `packages/cli/package.json` and `packages/studio/package.json` (was `"*"`); extended `scripts/bump-version.ts` (renamed `plugin-shell-package-json` kind → `lockstep-package-json`, applied to inter-package deps); added 4 manifest-shape regression cases in `packages/cli/test/customize-skill.test.ts`. Closes [#101](https://github.com/audiocontrol-org/deskwork/issues/101). End-to-end verified in marketplace install. |
| 26++ | Release-skill discipline + post-v0.9.6 dogfood | **v0.9.6 shipped via the new five-pause `/release` flow.** Phase 26f's npm-publish step landed (`ac6a987`). Post-ship dogfood closed five Phase-23-era obsolete issues ([#55](https://github.com/audiocontrol-org/deskwork/issues/55), [#77](https://github.com/audiocontrol-org/deskwork/issues/77), [#78](https://github.com/audiocontrol-org/deskwork/issues/78), [#79](https://github.com/audiocontrol-org/deskwork/issues/79), [#80](https://github.com/audiocontrol-org/deskwork/issues/80)) and surfaced three new packaging defects: [#99](https://github.com/audiocontrol-org/deskwork/issues/99) (intake form silent-collapse on copy), [#101](https://github.com/audiocontrol-org/deskwork/issues/101) (`@deskwork/{cli,studio}` declare `@deskwork/core: "*"` wildcard — adopter customize broken in marketplace install), [#102](https://github.com/audiocontrol-org/deskwork/issues/102) (`customize templates` cross-plugin-shell architectural seam). `dw-lifecycle` integrated into the release-blocking smoke (`f1ddcb7`) — adds `dw-lifecycle:dw-lifecycle` to `PLUGIN_BIN_PAIRS` + adds `--help`/`-h`/`help` handling to its CLI dispatcher (was returning "Unknown subcommand" exit 1). `/release` Pause 3 publish-step UX fixed (`d087fa6`): operator now runs `make publish` in their own terminal (the agent's Bash tool can't accept interactive 2FA OTPs), then confirms; new `assert-published <version>` helper verifies all three packages actually landed before continuing to smoke. Two commits ride along in next release. |

### Key Links

- Design spec: `docs/superpowers/specs/2026-04-20-deskwork-plugin-design.md`
- Feature definition: `/tmp/feature-definition-deskwork-plugin.md`
- Pull request: https://github.com/audiocontrol-org/deskwork/pull/1
- Bird's-eye content view mockup (Phase 16d): [`mockups/birds-eye-content-view.html`](mockups/birds-eye-content-view.html) — Writer's Catalog aesthetic; three states (top-level, drilldown, drilldown with selected node).
