---
name: feature-ship
description: "Prepare a feature for merge by verifying completion, running tests and review, pushing the branch, and opening a PR without merging it."
---

# Feature Ship

DEPRECATED as a repo-wide feature workflow entry point. Use the active
stack-control feature artifacts and the portable release/ship surfaces rather
than this old `.agents` path.
