---
description: "Copy a built-in dw-lifecycle template (markdown) or scope-discovery default (TS / YAML) into .dw-lifecycle/ so the project can tailor it."
---

Invoke the `customize` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
