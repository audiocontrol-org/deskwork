---
description: "Re-run discovery against an operator complaint and surface the new surfaces the original scope-inventory missed"
---

Invoke the `scope-widen` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
