---
description: "Mark a single clone-group with a disposition + reason; refuses `--as refactor` as a forcing function"
---

Invoke the `dispose-clone` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
