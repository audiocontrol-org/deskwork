---
description: "Cross-module fleet matrix of canonical-primitive adoption across parallel top-level modules"
---

Invoke the `check-module-symmetry` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
