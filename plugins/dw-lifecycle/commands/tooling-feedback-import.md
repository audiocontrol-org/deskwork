---
description: "Promote closure-marked TF entries from per-feature tooling-feedback.md files into the scope-discovery audit-log"
---

Invoke the `tooling-feedback-import` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
