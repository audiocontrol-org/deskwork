---
description: "Legacy alias for /dw-lifecycle:check-clones (renamed in Phase 6 Task 2; both names work)"
---

Invoke the `detect-clones` skill from the `dw-lifecycle` plugin via the Skill tool. The skill is a back-compat alias that points at `/dw-lifecycle:check-clones`; both subcommand names dispatch to the same handler.
