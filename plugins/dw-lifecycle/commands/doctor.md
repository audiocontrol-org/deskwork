---
description: "Audit binding metadata across calendar/journal/docs/issues; opt-in --fix"
---

Invoke the `doctor` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
