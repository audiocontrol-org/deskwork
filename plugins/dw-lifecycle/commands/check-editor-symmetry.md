---
description: "Deprecated alias for /dw-lifecycle:check-module-symmetry (Phase 25 rename; removal target v0.37.0)"
---

Invoke the `check-module-symmetry` skill from the `dw-lifecycle` plugin via the Skill tool. The skill is a deprecation-warning alias that points at `/dw-lifecycle:check-module-symmetry`; both subcommand names dispatch to the same handler.
