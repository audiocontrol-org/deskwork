---
description: "Pre-commit gate detecting silent non-pending → pending disposition transitions in clones.yaml"
---

Invoke the `check-disposition-survivor` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
