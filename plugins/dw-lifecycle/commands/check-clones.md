---
description: "Run the jscpd-backed clone detector against the project; surface NEW clone groups against the committed baseline"
---

Invoke the `check-clones` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
