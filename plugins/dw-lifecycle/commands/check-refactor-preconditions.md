---
description: "Commit-msg gate verifying Step 0a/0b refactor preconditions for `Closes clones.yaml <id>` commits"
---

Invoke the `check-refactor-preconditions` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
