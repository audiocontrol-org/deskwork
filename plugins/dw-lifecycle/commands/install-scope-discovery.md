---
description: "Bootstrap .dw-lifecycle/scope-discovery/ in the adopting project; copy templates and seed empty registries"
---

Invoke the `install-scope-discovery` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
