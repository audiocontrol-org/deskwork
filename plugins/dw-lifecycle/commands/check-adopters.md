---
description: "Find files that should import a canonical primitive but don't, per adopter-manifests.yaml"
---

Invoke the `check-adopters` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
