---
description: "Verify acceptance criteria; open PR; stop at PR creation (operator owns merge)"
---

Invoke the `ship` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
