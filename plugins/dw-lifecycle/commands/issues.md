---
description: "Create parent + per-phase GitHub issues from workplan; back-fill issue links"
---

Invoke the `issues` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
