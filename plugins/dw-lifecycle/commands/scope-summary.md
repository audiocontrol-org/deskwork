---
description: "Per-surface tally of pending and dispositioned clone groups in clones.yaml"
---

Invoke the `scope-summary` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
