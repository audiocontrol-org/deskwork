---
description: "Reverse install-scope-discovery-hooks and install-agent-prompts; drift-check before removing managed files"
---

Invoke the `uninstall-scope-discovery-hooks` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
