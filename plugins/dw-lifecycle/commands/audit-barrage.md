---
description: "Fire multiple CLI audit tools in parallel against a feature diff; capture per-model findings for triage"
---

Invoke the `audit-barrage` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
