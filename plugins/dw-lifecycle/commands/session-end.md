---
description: "Append journal entry; update feature docs; commit documentation changes"
---

Invoke the `session-end` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
