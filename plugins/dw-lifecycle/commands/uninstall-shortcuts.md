---
description: "Remove the user-level slash-command shortcuts for /dw-lifecycle:* commands"
---

Invoke the `uninstall-shortcuts` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
