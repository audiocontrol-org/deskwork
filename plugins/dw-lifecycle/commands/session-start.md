---
description: "Bootstrap session: read workplan + journal + open issues; report context"
---

Invoke the `session-start` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
