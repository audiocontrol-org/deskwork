---
description: "Fan parallel discovery agents over a feature's PRD + module-root and emit a validated scope-manifest.yaml"
---

Invoke the `scope-inventory` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
