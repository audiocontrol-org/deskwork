---
description: "Run the full scope-discovery adversarial harness suite via vitest"
---

Invoke the `validate-scope-discovery` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
