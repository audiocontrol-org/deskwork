---
description: "Wire a pre-commit hook that runs the scope-discovery gate chain (clones, anti-patterns, adopters, refactor-preconditions, editor-symmetry)"
---

Invoke the `install-scope-discovery-hooks` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
