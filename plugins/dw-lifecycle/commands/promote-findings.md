---
description: "Walk audit-log.md for Status: open entries and scope them into the workplan with a TDD-first task shape (propose → operator fills disposition → apply)"
---

Invoke the `promote-findings` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
