---
description: "Remove branch + worktree (infrastructure-only)"
---

Invoke the `teardown` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
