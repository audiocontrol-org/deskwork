---
description: "Scan the source tree for legacy shapes registered in anti-patterns.yaml and report findings"
---

Invoke the `check-anti-patterns` skill from the `dw-lifecycle` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
