#!/usr/bin/env bash
# deskwork governance orchestration — STUB (skeleton).
# Real orchestration (git diff -> audit-barrage-render -> audit-barrage -> lift)
# is implemented in T007. As a skeleton it exits non-zero so the T006 smoke
# fails RED before the implementation lands.
set -euo pipefail
echo "govern.sh: not implemented yet (skeleton)" >&2
exit 1
