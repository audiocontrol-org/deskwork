# Tooling Feedback


## session-end 2026-06-10
- backlog import-github is all-or-nothing: no label/path/predicate filter, so a scoped import (only deskwork-plugin issues out of 128 open) required a hand-rolled per-issue capture loop instead of the built-in importer
- stackctl backlog rejects --help with exit 2 (unknown flag); subaction usage is only discoverable via the SKILL.md, not the CLI itself
