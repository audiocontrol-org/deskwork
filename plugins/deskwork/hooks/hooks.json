{
  "description": "deskwork-studio bridge autostart — emits a SessionStart directive instructing the agent to dispatch /deskwork:listen when the project has studioBridge.enabled = true in .deskwork/config.json.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/bridge-autostart.mjs\""
          }
        ]
      }
    ]
  }
}
