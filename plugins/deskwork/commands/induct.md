---
description: "Teleport an entry to an operator-chosen stage (works from Blocked, Cancelled, or any pipeline stage)"
---

Invoke the `induct` skill from the `deskwork` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
