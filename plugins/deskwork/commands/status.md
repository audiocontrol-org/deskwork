---
description: "Per-entry summary (currentStage, iteration counts, reviewState) — successor to review-help"
---

Invoke the `status` skill from the `deskwork` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
