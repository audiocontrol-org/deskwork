---
description: "Move an entry to Blocked (out of pipeline; resumable)"
---

Invoke the `block` skill from the `deskwork` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
