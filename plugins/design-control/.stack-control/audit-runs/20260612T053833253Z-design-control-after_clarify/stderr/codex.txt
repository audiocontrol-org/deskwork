Reading prompt from stdin...
OpenAI Codex v0.136.0
--------
workdir: /Users/orion/work/deskwork-work/design-control
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: medium
reasoning summaries: none
session id: 019eba56-d5cc-7900-abfb-4027cd77fd97
--------
user
# Audit-barrage — multi-model audit prompt template

You are an **independent audit reviewer** firing as part of a multi-model audit barrage. Your siblings (other CLIs running this same prompt in parallel) emit their own findings independently; the operator triages all of your outputs side-by-side after every model has settled. Your job is to surface the kinds of defects listed under **What to look for** below, in the work product captured under **Under audit**.

You are NOT collaborating with the other models. You write what you see. The cross-model genetic diversity comes from each of you reporting independently.

## Feature under audit

design-control

## Feature scope (workplan / PRD summary)

Governance pass over the just-implemented work for feature 'design-control', diffed against 0391a0c0. The differentiated back half audits a plan it did not author or execute.

## Commit subjects in the audited range

76041df8 docs(design-control): disposition AUDIT-20260611-35..39 — round-4 barrage findings fixed; backlog TASK-20
2ba353ac fix(design-control): CommonMark closing hashes + the `+` bullet marker parse correctly; nesting approximation stated (round-4 barrage, schema trio)
a5a8345f fix(design-control): leading discriminator required in liveness matching; @keyframes steps are not preludes (round-4 barrage)
8fad5abe docs(design-control): disposition AUDIT-20260611-30..34 — round-3 barrage findings fixed
fd1f6bfa fix(design-control): combinator-spacing normalization + drive-relative path rejection (round-3 barrage, final pair)
cb9542f4 fix(design-control): code blocks are inert to the spec parser; line-attempt guard calibrated to id shape (round-3 barrage, parser pair)
725389bc fix(design-control): pin the plugin tsconfig in every bin shim — the documented repo-root invocation works (round-3 barrage, HIGH)
ca2046f2 docs(design-control): disposition AUDIT-20260611-22..29 — round-2 barrage findings fixed; backlog TASK-19
258d6476 fix(design-control): stable capability statements in source comments + agent-authored workplan annotations; AUDIT-21 scope recorded (round-2 barrage, codex-03 + claude-05)
165e69b7 fix(design-control): machine-rooted css paths are malformed — specs stay portable with their collection (round-2 barrage, codex-02)
ee05786d fix(design-control): canonical value comparison replaces strip-to-empty in liveness matching (round-2 barrage, selector pair)
b4284b33 fix(design-control): generalize + calibrate the rule-attempt guard; single-pass for all excluded sections (round-2 barrage, heading-guard trio)
b36b90b7 docs(design-control): disposition AUDIT-20260611-15..21 — all seven Phase 2 barrage findings fixed
03ae9291 fix(design-control): capability statements replace temporal deferral phrasing in operator-facing surfaces (AUDIT-20260611-21)
76a06559 fix(design-control): functional pseudo-class args don't satisfy liveness — exclusion is not styling (AUDIT-20260611-18)
d4c26551 fix(design-control): single-pass defect surfacing — no fix-and-rerun finding waves (AUDIT-20260611-19, AUDIT-20260611-20)
76e1cefd fix(design-control): normalize liveness query like the prelude — quoted attribute selectors are linkable (AUDIT-20260611-17)
7a3c37be fix(design-control): normalize curly-apostrophe field keys — don’t (U+2019) is don't (AUDIT-20260611-16)
9f5d9a46 fix(design-control): heading-level typo guard — near-miss rule headings are findings, never silent drops (AUDIT-20260611-15)
bc650cad docs(design-control): check off Phase 2 tasks; roadmap phase-2 → in-flight
4650ee66 feat(design-control): translate-design-language authoring skill (Phase 2)
5853f83f feat(design-control): static link-liveness + bin/check-design-spec (Phase 2)
b2659452 feat(design-control): design-language spec schema + example-presence validation (Phase 2)


## Recent audit-log excerpt (prior findings on this feature)

Use this to avoid re-reporting findings that have already been triaged. If a finding was previously dispositioned (`closed`, `won't-fix`, `accepted-trade-off`), don't re-litigate the disposition; only surface a new instance if the underlying shape regressed.


Blast radius: low — the failure direction is a loud exit-1 false refusal with an actionable message, and the author can converge by anchoring the rule to the leaf selector (`.icon`), which does match (verified: leaf queries pass even against `&`-joined preludes). The cost is a confusing reword round-trip plus a contract that quietly under-delivers on nested codebases. Minimal fix: add nesting to the stated approximations in both the module doc and SKILL.md ("link the leaf selector for nested rules"); fuller fix: maintain a prelude ancestor stack in `collectSelectorPreludes` and emit composed preludes with `&` substitution, pinned by the two probes above as RED tests.

### AUDIT-20260611-37 — `+`-marker field bullets are silently inert — the resulting missing-* findings misattribute the cause, the exact no-invisible-cause failure the module's own doc names

Finding-ID: AUDIT-20260611-37
Status:     fixed-2ba353ac7958ce89ee71873a9edc34c6ac684666
Severity:   low
Surface:    plugins/design-control/src/design-language/schema.ts:61 (`FIELD_BULLET_RE = /^[-*]\s+…/`), :91 (`BULLET_SHAPE_RE = /^[-*]\s/`)

CommonMark defines three bullet list markers — `-`, `*`, and `+` — but both bullet regexes admit only `-` and `*`. A rule authored with `+` bullets parses with every field dropped as inert prose: verified empirically, a complete rule (`+ kind:`, `+ css:`, `+ example:`, `+ do:`) returns `findings: [missing-kind, missing-css-link, missing-example, missing-guidance]` and zero rules. The verdict is loud, but every finding names a *false cause* — the author is told the rule "has no kind: field" while staring at one. This is precisely the failure shape the module's own header rules out for field keys (schema.ts:29-31: silently dropping a misspelled `example:` "would otherwise fabricate a missing-example rejection with no visible cause") and that the AUDIT-15/-31/-32 series killed for headings and declarations — one syntax level over, at the list-marker. The `+`-bullet lines also bypass the `unknown-field` typo guard and, on ≥4-space-indented lines, the `BULLET_SHAPE_RE` carve-out (schema.ts:302), so an indented `+ css:` bullet is treated as indented code.

Blast radius: low — always loud, never a silent wrong outcome; the cost is a baffling diagnostic and a guaranteed round-trip for authors (or formatters) that prefer `+` markers. Fix is one character in each class (`[-*+]`), plus corpus pins for a `+`-bulleted rule parsing identically to a `-`-bulleted one.

### AUDIT-20260611-38 — Keyframe step selectors (`from` / `to` / `0%`) are collected as matchable preludes

Finding-ID: AUDIT-20260611-38
Status:     fixed-a5a8345f039d438941d84c971d078016fe4d38b7
Severity:   informational
Surface:    plugins/design-control/src/design-language/link-liveness.ts:69-109 (`collectSelectorPreludes` descends into all at-rule blocks uniformly)

The at-rule descent that correctly makes `@media`-housed rules count also descends into `@keyframes`, whose inner blocks are step selectors, not element rules — so `from`/`to`/percentage preludes enter the matchable set. Verified empirically: query `from` vs `@keyframes spin { from { } to { } }` → `true`. Note this instance would survive the AUDIT-BARRAGE-claude-01 discriminator fix (the `from` prelude genuinely begins with a bare ident), so it's worth one exclusion line (`@keyframes` blocks contribute no preludes) whenever that fix lands.

Blast radius: effectively nil as-written — no plausible design-language rule anchors to `from`, `to`, or `0%` as a selector, so this is context for the prelude-collection contract rather than a defect a consumer would hit. Recorded so the scope of "selector prelude" is stated rather than discovered.

---

**Summary for triage:** 4 findings, 0 blocking, 0 high, 1 medium. The medium (claude-01) is the round's one silent-green: the boundary check validates *that* an ident sits at a selector boundary but never *which discriminator* introduces it, so every dropped-dot typo self-certifies green — the same unanchored-text root the AUDIT-17/-18/round-2 canonicalization series has been chipping at, one axis (selector-kind) over. Claude-02 and claude-03 are loud false-refusal hygiene with misleading or missing contract statements; claude-04 is scope context that should ride along with the claude-01 fix. All four were confirmed by executing the shipped module against fixtures, not by reading alone, so RED-first regression tests are cheap for each. Everything else I checked came back clean: the round-3 fixes hold as committed (fence inertness, line-attempt id-shape calibration, combinator normalization including `||` and the `~=` guard, drive-relative path rejection, tsconfig-pinned shims across all three bins), the tasks.md check-offs match the shipped behavior, and the committed audit-run artifacts follow the prior `da2ed12c` disposition.

### AUDIT-20260611-39 — Closing ATX hashes become part of the rule id

Finding-ID: AUDIT-20260611-39
Status:     fixed-2ba353ac7958ce89ee71873a9edc34c6ac684666
Severity:   medium
Surface:    plugins/design-control/src/design-language/schema.ts:53-54, plugins/design-control/src/design-language/schema.ts:310-325

`HEADING_RE` captures the raw text after the opening hashes, and `parseDesignSpec` immediately feeds `heading[1].trim()` into `RULE_HEADING_RE`. That means a valid Markdown ATX heading with a closing sequence, such as `### rule: ink-primary ###`, parses as rule id `ink-primary ###` instead of `ink-primary`. The tests only cover headings without closing hashes, so this common Markdown spelling is not pinned.

Blast radius is medium: the spec can go green while downstream consumers of `spec.rules` receive the wrong stable rule id, and the duplicate-id guard can be bypassed by mixing `### rule: ink` with `### rule: ink ###`. A reasonable correction is to normalize ATX heading text before rule parsing by stripping a valid closing hash sequence per Markdown rules, then add regression coverage for both id extraction and duplicate detection.


## Under audit

The actual code under review. Read it carefully. The findings you emit must be anchored to specific files + line ranges in this diff (or call out a missing surface that should be in the diff but isn't).



## What to look for

- **Correctness bugs** — logic errors, off-by-one, null/undefined paths, race conditions, missing error handling, swallowed exceptions.
- **Design issues** — coupling between layers that should be independent, leaking abstractions, primitives that should compose but don't, configuration that should be data ending up as code.
- **Missed edge cases** — what happens with empty input? Maximum input? Concurrent calls? Partial failure? Network unavailability? Operator interrupt mid-operation? What is the behavior on a fresh install vs. an upgrade?
- **Code-quality concerns** — files growing past a reasonable cap, names that don't reveal intent, dead code, duplicated logic, magic numbers without explanation, tests that don't test the contract they claim to test.
- **Cross-cutting impact** — does this diff touch a surface that other surfaces depend on? Are those other surfaces updated? Are migrations needed? Are doctor rules / schemas / validators updated to match the new shape?
- **Documentation drift** — does the README / SKILL.md / PRD describe the behavior the code actually implements? If the spec changed, did the implementation? If the implementation changed, did the spec?
- **Operator-discipline traps** — placeholder comments, swallowed errors, hardcoded paths/values that should be configurable, fallbacks that hide failure modes, mock data outside test code. These are bug-factories per project guidelines.

## Output format

For each finding you surface, emit ONE markdown block in this exact shape:

```
### <heading: one-line summary of the finding>

Finding-ID: AUDIT-BARRAGE-<your-model-name>-<NN>
Status:     open
Severity:   <blocking | high | medium | low | informational>
Surface:    <repo-relative-path:line-range> OR <description of the surface if not anchored to a single file>

<one-to-three paragraphs of body: what the finding is, why it matters, what evidence you relied on, what a reasonable fix would look like. Be specific. Cite line numbers from the diff. If the finding is structural / cross-file, name every file affected.>
```

Number the findings sequentially (`-01`, `-02`, ...).

**Severity — rate each finding by downstream blast-radius:** the consequence if a downstream consumer acts on the audited surface *as written*. The consumer may be an adopter running the code, or — especially for a spec — an AI agent building **unattended** from it, with no human to catch a wrong reading. Rate by what would actually happen if this shipped as-is, **not by how alarming the finding feels**. State the blast-radius reasoning in the finding body for every finding, at every level.

- `blocking` — acting on it as-written breaks the feature's stated goals in obvious ways; OR (for a spec) the more natural reading an agent reaches first is the wrong one, so it will likely be built wrong by default and nothing in the artifact corrects it.
- `high` — a correctness/safety defect a consumer will hit; OR a spec contradiction/ambiguity where the readings are roughly equally plausible and the artifact doesn't disambiguate — an agent might build either, including the wrong one.
- `medium` — a design issue that compounds over time; OR a spec inconsistency a reasonable consumer would resolve correctly anyway (readings barely diverge, or context makes the intended one obvious).
- `low` — hygiene; cosmetic wording with no behavioral or implementation consequence.
- `informational` — context worth seeing, not itself a defect.

**Calibrate by consequence, not by alarm.** A genuine contradiction a reader would obviously resolve the right way is at most `medium`. A quietly-plausible wrong reading an agent would actually build is `high`/`blocking` even if it looks minor. A spec's internal consistency is load-bearing — it is the input to an unattended build.

## If you find nothing — say so explicitly

If you walk the diff carefully and find no findings worth surfacing, emit ONE block in this shape instead:

```
### No findings

Finding-ID: AUDIT-BARRAGE-<your-model-name>-CLEAN
Status:     open
Severity:   informational
Surface:    (the entire diff)

I walked the diff for the feature named above and found no findings worth surfacing. My specific reasoning: <three-to-five sentences explaining what you checked, why those checks came back clean, and what you would have flagged if it had been present.>
```

**Do not pad with weak findings.** A confident "I checked X, Y, Z and they are clean for these reasons" is more useful to the operator than three vague low-severity notes. The cross-model diversity gives the operator independent signal; an empty clean report from your CLI is itself a signal when paired with findings from your siblings.

## Hard constraints

- **No deferral phrases.** Don't write phrases like "fix later", "address in a follow-up", or other commitments to deferred work. The dispatch-wrapper rejects these as bug-factories. If you spot a deferral phrase IN the diff, surface it as a finding.
- **Anchor findings to evidence.** A finding that says "this might be a problem" without naming the specific file + line is not actionable. Name the surface, quote the relevant code, explain what's wrong.
- **One issue per finding block.** Don't bundle multiple concerns into one entry; the operator triages each block as a discrete signal.
- **Provenance is your model name.** Replace `<your-model-name>` in the Finding-ID with the CLI you are (`claude`, `codex`, `gemini`, etc.). This is how the operator joins findings across models.

codex
I’ll use the feature-review workflow as the local review frame, then inspect the audited diff directly against `0391a0c0` and report only actionable findings in the requested block format.
exec
/bin/zsh -lc "sed -n '1,220p' .agents/skills/feature-review/SKILL.md" in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
---
name: feature-review
description: "Review recent feature changes for bugs, regressions, and missing tests, then report findings without fixing them."
---

# Feature Review

1. Determine review scope from uncommitted, staged, and branch-vs-main changes.
2. Read the changed files and workplan context.
3. Review for:
   - bugs
   - regressions
   - missing tests
   - guideline violations
4. Report findings first, ordered by severity, with file references.
5. Do not fix issues in this step.

codex
I’m going to read the changed surfaces first, not just the commit subjects, because several prior fixes are parser edge cases where the actual regression risk lives in tests and contract wording.
exec
/bin/zsh -lc 'git diff --name-only 0391a0c0..HEAD' in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
plugins/design-control/.stack-control/audit-runs/20260611T123117674Z-design-control-after_clarify/INDEX.md
plugins/design-control/.stack-control/audit-runs/20260611T123117674Z-design-control-after_clarify/PROMPT.md
plugins/design-control/.stack-control/audit-runs/20260611T123117674Z-design-control-after_clarify/claude.md
plugins/design-control/.stack-control/audit-runs/20260611T123117674Z-design-control-after_clarify/codex.md
plugins/design-control/.stack-control/audit-runs/20260611T123117674Z-design-control-after_clarify/stderr/claude.txt
plugins/design-control/.stack-control/audit-runs/20260611T123117674Z-design-control-after_clarify/stderr/codex.txt
plugins/design-control/.stack-control/audit-runs/20260611T123117674Z-design-control-after_clarify/tip.sha
plugins/design-control/.stack-control/audit-runs/20260611T131139811Z-design-control-after_clarify/INDEX.md
plugins/design-control/.stack-control/audit-runs/20260611T131139811Z-design-control-after_clarify/PROMPT.md
plugins/design-control/.stack-control/audit-runs/20260611T131139811Z-design-control-after_clarify/claude.md
plugins/design-control/.stack-control/audit-runs/20260611T131139811Z-design-control-after_clarify/codex.md
plugins/design-control/.stack-control/audit-runs/20260611T131139811Z-design-control-after_clarify/stderr/claude.txt
plugins/design-control/.stack-control/audit-runs/20260611T131139811Z-design-control-after_clarify/stderr/codex.txt
plugins/design-control/.stack-control/audit-runs/20260611T131139811Z-design-control-after_clarify/tip.sha
plugins/design-control/.stack-control/audit-runs/20260611T134728627Z-design-control-after_clarify/INDEX.md
plugins/design-control/.stack-control/audit-runs/20260611T134728627Z-design-control-after_clarify/PROMPT.md
plugins/design-control/.stack-control/audit-runs/20260611T134728627Z-design-control-after_clarify/claude.md
plugins/design-control/.stack-control/audit-runs/20260611T134728627Z-design-control-after_clarify/codex.md
plugins/design-control/.stack-control/audit-runs/20260611T134728627Z-design-control-after_clarify/stderr/claude.txt
plugins/design-control/.stack-control/audit-runs/20260611T134728627Z-design-control-after_clarify/stderr/codex.txt
plugins/design-control/.stack-control/audit-runs/20260611T134728627Z-design-control-after_clarify/tip.sha
plugins/design-control/.stack-control/audit-runs/20260611T141406686Z-design-control-after_clarify/INDEX.md
plugins/design-control/.stack-control/audit-runs/20260611T141406686Z-design-control-after_clarify/PROMPT.md
plugins/design-control/.stack-control/audit-runs/20260611T141406686Z-design-control-after_clarify/claude.md
plugins/design-control/.stack-control/audit-runs/20260611T141406686Z-design-control-after_clarify/codex.md
plugins/design-control/.stack-control/audit-runs/20260611T141406686Z-design-control-after_clarify/stderr/claude.txt
plugins/design-control/.stack-control/audit-runs/20260611T141406686Z-design-control-after_clarify/stderr/codex.txt
plugins/design-control/.stack-control/audit-runs/20260611T141406686Z-design-control-after_clarify/tip.sha
plugins/design-control/.stack-control/backlog/tasks/task-19 - Temporal-in-v1-phrasing-in-lint-allowlist-comments-Latin-only-rationale.md
plugins/design-control/.stack-control/backlog/tasks/task-20 - Liveness-composed-prelude-matching-for-native-CSS-nesting-ancestor-stack-substitution.md
plugins/design-control/ROADMAP.md
plugins/design-control/bin/check-design-spec
plugins/design-control/bin/check-wireframe
plugins/design-control/bin/wireframe-provenance
plugins/design-control/skills/translate-design-language/SKILL.md
plugins/design-control/specs/001-design-control/audit-log.md
plugins/design-control/specs/001-design-control/tasks.md
plugins/design-control/src/__tests__/authoring/check-wireframe-shim.test.ts
plugins/design-control/src/__tests__/design-language/check-design-spec-shim.test.ts
plugins/design-control/src/__tests__/design-language/check-spec-file.test.ts
plugins/design-control/src/__tests__/design-language/link-liveness.test.ts
plugins/design-control/src/__tests__/design-language/schema.test.ts
plugins/design-control/src/__tests__/provenance/wireframe-provenance-shim.test.ts
plugins/design-control/src/design-language/check-design-spec-cli.ts
plugins/design-control/src/design-language/check-spec-file.ts
plugins/design-control/src/design-language/index.ts
plugins/design-control/src/design-language/link-liveness.ts
plugins/design-control/src/design-language/rule-attempt.ts
plugins/design-control/src/design-language/schema.ts
plugins/design-control/src/design-language/selector-canon.ts
plugins/design-control/src/design-language/types.ts

exec
/bin/zsh -lc 'git diff --stat 0391a0c0..HEAD' in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
 .../INDEX.md                                       |    29 +
 .../PROMPT.md                                      |  1533 ++
 .../claude.md                                      |    60 +
 .../codex.md                                       |    21 +
 .../stderr/claude.txt                              |     0
 .../stderr/codex.txt                               |  2308 ++
 .../tip.sha                                        |     1 +
 .../INDEX.md                                       |    29 +
 .../PROMPT.md                                      |  5976 ++++++
 .../claude.md                                      |    62 +
 .../codex.md                                       |    32 +
 .../stderr/claude.txt                              |     0
 .../stderr/codex.txt                               |  6986 +++++++
 .../tip.sha                                        |     1 +
 .../INDEX.md                                       |    29 +
 .../PROMPT.md                                      | 19762 +++++++++++++++++
 .../claude.md                                      |    49 +
 .../codex.md                                       |    21 +
 .../stderr/claude.txt                              |     0
 .../stderr/codex.txt                               | 20940 +++++++++++++++++++
 .../tip.sha                                        |     1 +
 .../INDEX.md                                       |    29 +
 .../PROMPT.md                                      |   149 +
 .../claude.md                                      |    49 +
 .../codex.md                                       |    10 +
 .../stderr/claude.txt                              |     0
 .../stderr/codex.txt                               |  2871 +++
 .../tip.sha                                        |     1 +
 ...lint-allowlist-comments-Latin-only-rationale.md |    19 +
 ...tive-CSS-nesting-ancestor-stack-substitution.md |    22 +
 plugins/design-control/ROADMAP.md                  |     2 +-
 plugins/design-control/bin/check-design-spec       |    28 +
 plugins/design-control/bin/check-wireframe         |     4 +-
 plugins/design-control/bin/wireframe-provenance    |     4 +-
 .../skills/translate-design-language/SKILL.md      |   113 +
 .../specs/001-design-control/audit-log.md          |   309 +
 .../specs/001-design-control/tasks.md              |    29 +-
 .../authoring/check-wireframe-shim.test.ts         |    48 +
 .../design-language/check-design-spec-shim.test.ts |    83 +
 .../design-language/check-spec-file.test.ts        |   221 +
 .../design-language/link-liveness.test.ts          |   486 +
 .../src/__tests__/design-language/schema.test.ts   |   805 +
 .../provenance/wireframe-provenance-shim.test.ts   |    28 +
 .../src/design-language/check-design-spec-cli.ts   |    12 +
 .../src/design-language/check-spec-file.ts         |    96 +
 .../design-control/src/design-language/index.ts    |    31 +
 .../src/design-language/link-liveness.ts           |   295 +
 .../src/design-language/rule-attempt.ts            |    98 +
 .../design-control/src/design-language/schema.ts   |   433 +
 .../src/design-language/selector-canon.ts          |   218 +
 .../design-control/src/design-language/types.ts    |   107 +
 51 files changed, 64433 insertions(+), 7 deletions(-)

exec
/bin/zsh -lc 'git status --short' in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
 D docs/1.0/001-IN-PROGRESS/command-shortcuts/README.md
 D docs/1.0/001-IN-PROGRESS/command-shortcuts/prd.md
 D docs/1.0/001-IN-PROGRESS/command-shortcuts/workplan.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/2026-05-03-issue-158-ux-audit.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/2026-05-03-phase-34-ship-pass-implementation-audit.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/2026-05-03-phase-34a-implementation-audit.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/2026-05-03-phase-34b-implementation-audit.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/2026-05-03-phase-34e-implementation-audit.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/2026-05-04-open-issue-tranche-proposal.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/38-0-blast-radius-review.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/39-sites-to-lanes-blueprint.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/README.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/audit-log.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/implementation-summary.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/mockups/birds-eye-content-view.html
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/mockups/design-audit-v0.5.0.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/mockups/editorial-nav-and-index.html
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/mockups/studio-unified.html
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/phase-34-prd-review.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/phase-34a-task-breakdown.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/post-pivot-review-audit.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/prd.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/01-dev-editorial-studio-1512.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/02-dev-index-1512.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/03-dev-content-1512.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/04-dev-shortform-1512.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/05-dev-help-manual-1512.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/06-dev-scrapbook-baseline-1512.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/07-dashboard-after-ingest-c93b8b3e.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/08-entry-review-c93b8b3e-direct.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/09-dashboard-with-press-queue-1512.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/10-dashboard-press-queue-scrolled.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/11-dashboard-press-queue-mobile-800.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/12-review-surface-tables-after-gfm.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/13-review-surface-table-rendered.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/14-tables-styled-marginalia-visible.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/15-tables-marginalia-stowed.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/16-tables-marginalia-visible-final.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/17-tables-final.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/18-tables-final-table-view.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/19-tables-final-stowed.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/20-thesis-on-review-surface.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/21-images-render-on-review-surface-v4.png
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/scrapbook/plan.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/tooling-feedback.md
 D docs/1.0/001-IN-PROGRESS/deskwork-plugin/workplan.md
 D docs/1.0/001-IN-PROGRESS/deskwork-triage/README.md
 D docs/1.0/001-IN-PROGRESS/deskwork-triage/prd.md
 D docs/1.0/001-IN-PROGRESS/deskwork-triage/workplan.md
 D docs/1.0/001-IN-PROGRESS/document-primitives/audit-log.md
 D docs/1.0/001-IN-PROGRESS/dw-lifecycle/2026-05-03-implementation-audit.md
 D docs/1.0/001-IN-PROGRESS/dw-lifecycle/2026-05-03-post-remediation-audit.md
 D docs/1.0/001-IN-PROGRESS/dw-lifecycle/2026-05-03-prd-conformance-audit.md
 D docs/1.0/001-IN-PROGRESS/dw-lifecycle/README.md
 D docs/1.0/001-IN-PROGRESS/dw-lifecycle/design.md
 D docs/1.0/001-IN-PROGRESS/dw-lifecycle/workplan.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/README.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/audit-log.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/dogfood-handoff.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/prd.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260525T110802Z-1cdd0f/adopter-manifest-checker.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260525T110802Z-1cdd0f/args.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260525T110802Z-1cdd0f/clone-detector-reader.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260525T110802Z-1cdd0f/editor-symmetry.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260525T110802Z-1cdd0f/pattern-matrix.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260525T110802Z-1cdd0f/prd-themed-pattern-hunter.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260525T110802Z-1cdd0f/regime-holdout-detector.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260525T110802Z-1cdd0f/synthesis.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260525T110802Z-1cdd0f/ui-route-enumerator.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260528T061038Z-b5fc2d/adopter-manifest-checker.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260528T061038Z-b5fc2d/args.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260528T061038Z-b5fc2d/clone-detector-reader.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260528T061038Z-b5fc2d/editor-symmetry.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260528T061038Z-b5fc2d/pattern-matrix.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260528T061038Z-b5fc2d/prd-themed-pattern-hunter.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260528T061038Z-b5fc2d/regime-holdout-detector.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260528T061038Z-b5fc2d/synthesis.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/runs/20260528T061038Z-b5fc2d/ui-route-enumerator.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T061143Z-89b7a1/args.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T061143Z-89b7a1/augmented-prd.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T061143Z-89b7a1/clone-detector-reader.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T061143Z-89b7a1/complaint.txt
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T061143Z-89b7a1/delta.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T061143Z-89b7a1/new-manifest.yaml
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T061143Z-89b7a1/pattern-matrix.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T061143Z-89b7a1/prd-themed-pattern-hunter.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T061143Z-89b7a1/synthesis.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T061143Z-89b7a1/ui-route-enumerator.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T082055Z-220e45/args.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T082055Z-220e45/augmented-prd.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T082055Z-220e45/clone-detector-reader.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T082055Z-220e45/complaint.txt
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T082055Z-220e45/delta.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T082055Z-220e45/new-manifest.yaml
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T082055Z-220e45/pattern-matrix.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T082055Z-220e45/prd-themed-pattern-hunter.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T082055Z-220e45/synthesis.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T082055Z-220e45/ui-route-enumerator.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T090129Z-4f2251/args.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T090129Z-4f2251/augmented-prd.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T090129Z-4f2251/clone-detector-reader.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T090129Z-4f2251/complaint.txt
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T090129Z-4f2251/delta.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T090129Z-4f2251/new-manifest.yaml
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T090129Z-4f2251/pattern-matrix.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T090129Z-4f2251/prd-themed-pattern-hunter.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T090129Z-4f2251/synthesis.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T090129Z-4f2251/ui-route-enumerator.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T094458Z-a64941/args.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T094458Z-a64941/augmented-prd.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T094458Z-a64941/clone-detector-reader.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T094458Z-a64941/complaint.txt
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T094458Z-a64941/delta.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T094458Z-a64941/new-manifest.yaml
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T094458Z-a64941/pattern-matrix.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T094458Z-a64941/prd-themed-pattern-hunter.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T094458Z-a64941/synthesis.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-inventory/widen-runs/20260528T094458Z-a64941/ui-route-enumerator.json
 D docs/1.0/001-IN-PROGRESS/graphical-entries/scope-manifest.yaml
 D docs/1.0/001-IN-PROGRESS/graphical-entries/tooling-feedback.md
 D docs/1.0/001-IN-PROGRESS/graphical-entries/workplan.md
 D docs/1.0/001-IN-PROGRESS/hygiene/README.md
 D docs/1.0/001-IN-PROGRESS/hygiene/audit-log.md
 D docs/1.0/001-IN-PROGRESS/hygiene/issue-closure-audit-2026-05-29.md
 D docs/1.0/001-IN-PROGRESS/hygiene/prd.md
 D docs/1.0/001-IN-PROGRESS/hygiene/tooling-feedback.md
 D docs/1.0/001-IN-PROGRESS/hygiene/workplan-archive.md
 D docs/1.0/001-IN-PROGRESS/hygiene/workplan.md
 D docs/1.0/001-IN-PROGRESS/pluggable-lifecycle-providers/README.md
 D docs/1.0/001-IN-PROGRESS/pluggable-lifecycle-providers/audit-log.md
 D docs/1.0/001-IN-PROGRESS/pluggable-lifecycle-providers/design.md
 D docs/1.0/001-IN-PROGRESS/pluggable-lifecycle-providers/feature-definition.md
 D docs/1.0/001-IN-PROGRESS/pluggable-lifecycle-providers/govern-consolidation-design.md
 D docs/1.0/001-IN-PROGRESS/pluggable-lifecycle-providers/prd.md
 D docs/1.0/001-IN-PROGRESS/pluggable-lifecycle-providers/stack-control-roadmap.md
 D docs/1.0/001-IN-PROGRESS/pluggable-lifecycle-providers/stack-control-thesis.md
 D docs/1.0/001-IN-PROGRESS/pluggable-lifecycle-providers/tooling-feedback.md
 D docs/1.0/001-IN-PROGRESS/pluggable-lifecycle-providers/workplan.md
 D docs/1.0/001-IN-PROGRESS/scope-discovery/README.md
 D docs/1.0/001-IN-PROGRESS/scope-discovery/audit-barrage-cli-notes.md
 D docs/1.0/001-IN-PROGRESS/scope-discovery/audit-log.md
 D docs/1.0/001-IN-PROGRESS/scope-discovery/paper-test-graphical-entries.md
 D docs/1.0/001-IN-PROGRESS/scope-discovery/phase-11-acceptance.md
 D docs/1.0/001-IN-PROGRESS/scope-discovery/phase-25-inventory.md
 D docs/1.0/001-IN-PROGRESS/scope-discovery/prd.md
 D docs/1.0/001-IN-PROGRESS/scope-discovery/scope-inventory-graphical-entries.yaml
 D docs/1.0/001-IN-PROGRESS/scope-discovery/workplan-archive.md
 D docs/1.0/001-IN-PROGRESS/scope-discovery/workplan.md
?? plugins/design-control/.stack-control/audit-runs/20260612T053833253Z-design-control-after_clarify/

codex
The working tree has a large set of unrelated deletions plus a new audit-run directory; I’m ignoring those and reviewing the committed range the prompt names, `0391a0c0..HEAD`.
exec
/bin/zsh -lc "nl -ba plugins/design-control/src/design-language/schema.ts | sed -n '1,520p'" in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
     1	/**
     2	 * Markdown schema parser/validator for the design-language spec convention
     3	 * (Phase 2, axis A — pure text → structure; NO filesystem, NO engine).
     4	 *
     5	 * Convention (hand-authorable):
     6	 *   - a rule is declared by an ATX heading whose text is `rule: <id>`
     7	 *     (any heading level; a CommonMark closing hash sequence —
     8	 *     `### rule: <id> ###` — is stripped before the id is read); the rule's
     9	 *     section runs to the next heading;
    10	 *   - fields are single-line bullets `- <key>: <value>` (any CommonMark
    11	 *     bullet marker: `-` / `*` / `+`) with the CLOSED key set
    12	 *     `kind` / `css` / `example` / `do` / `don't` (a curly apostrophe U+2019 in
    13	 *     the key is normalized to ASCII `'`, so smart-quoted `don’t:` is accepted);
    14	 *   - `css: <path> <selector>` — first token is the file path (relative to the
    15	 *     spec file; machine-rooted paths are rejected, `../` traversal is allowed
    16	 *     — see {@link isNonPortableCssPath}), the remainder is the selector
    17	 *     (descendant selectors allowed);
    18	 *   - other prose (paragraphs, capitalised-key bullets) is inert;
    19	 *   - code blocks are inert (AUDIT round-3 claude-02): a fenced block
    20	 *     (``` or ~~~, ≥3 markers, info string allowed; the closer is a line of
    21	 *     ONLY the same marker repeated at least the opener's length — pragmatic
    22	 *     CommonMark, indented/nested-in-list fences not modeled) suspends ALL
    23	 *     parsing, so a documentation example containing `### rule: phantom`
    24	 *     never becomes a live rule. Likewise a ≥4-space-indented (or
    25	 *     tab-indented) NON-bullet line is indented code and skipped; indented
    26	 *     FIELD BULLETS still record, so nested-list authoring keeps working.
    27	 *
    28	 * Validation per rule: kind from the closed vocabulary; ≥1 css link; ≥1
    29	 * example (structural presence only — this validator does not establish
    30	 * example truthfulness; that is the separate `spec-truthfulness` axis,
    31	 * scope decision recorded in specs/001-design-control/spec.md); ≥1
    32	 * do/don't guidance line. A lowercase
    33	 * single-word bullet key outside the closed set is an `unknown-field` finding
    34	 * (typo guard) — silently dropping a misspelled `example:` would otherwise
    35	 * fabricate a missing-example rejection with no visible cause. The same
    36	 * philosophy applies to rule DECLARATIONS: an attempted `rule: <id>` that
    37	 * misses the strict form — wrong case (`Rule: x`), spaced colon (`rule : x`),
    38	 * missing colon (`rule x`), setext heading, or bare paragraph line — is a
    39	 * `malformed-rule-heading` finding — never silently demoted to inert
    40	 * structure, which would drop the whole intended rule with zero findings
    41	 * (or merge its bullets into the preceding rule). The attempt classifiers
    42	 * live in `@/design-language/rule-attempt`.
    43	 */
    44	
    45	import { classifyHeadingAttempt, classifyLineAttempt } from '@/design-language/rule-attempt';
    46	import {
    47	  RULE_KINDS,
    48	  type CssLink,
    49	  type DesignRuleKind,
    50	  type DesignSpecFinding,
    51	  type DesignSpecParseResult,
    52	  type DesignSpecRule,
    53	  type RuleScopedCssLink,
    54	} from '@/design-language/types';
    55	
    56	const HEADING_RE = /^#{1,6}\s+(.*)$/;
    57	const RULE_HEADING_RE = /^rule:\s*(.*)$/;
    58	
    59	/**
    60	 * Strip a valid ATX closing sequence from heading text (CommonMark): a
    61	 * trailing run of `#` preceded by a space (`rule: ink ###` → `rule: ink`),
    62	 * or a text of ONLY hashes (`### ###` has empty text). A `#` glued to text
    63	 * (`rule: a#b`) is content, not a closing sequence. Runs BEFORE all rule /
    64	 * near-miss classification, so the spelling never leaks into a rule id —
    65	 * `### rule: ink ###` and `### rule: ink` name the SAME id and the
    66	 * duplicate-id guard sees them collide (AUDIT-round4-codex-01).
    67	 */
    68	function stripAtxClosingSequence(text: string): string {
    69	  if (/^#+$/.test(text)) {
    70	    return '';
    71	  }
    72	  return text.replace(/\s+#+$/, '');
    73	}
    74	
    75	/**
    76	 * A field bullet: lowercase single-word key (apostrophe allowed: `don't`).
    77	 * U+2019 (’) is also admitted — smart-quote editors substitute it in prose —
    78	 * and normalized to ASCII `'` before the known-key check, so `don’t:` records
    79	 * like `don't:` and a misspelled curly key still hits the typo guard.
    80	 * All three CommonMark bullet markers (`-` / `*` / `+`) are admitted — a
    81	 * `+`-bulleted rule must parse like a `-`-bulleted one, never drop its fields
    82	 * as inert prose (AUDIT-round4-claude-03).
    83	 */
    84	const FIELD_BULLET_RE = /^[-*+]\s+([a-z][a-z'’]*)\s*:\s*(.*)$/;
    85	
    86	/**
    87	 * `css:` paths are RELATIVE TO THE SPEC FILE — the portability contract
    88	 * (AUDIT-round2-codex-02). A machine-rooted path (POSIX `/...`, Windows
    89	 * `C:\...` / `C:/...` or drive-RELATIVE `C:styles.css` (resolves against the
    90	 * drive's current directory on Windows, as a literal filename on POSIX — a
    91	 * css path has no legitimate single-letter-colon prefix), UNC `\\...` or
    92	 * root-relative `\...`, `~`-expanded home) goes green on its author's
    93	 * machine — `resolve(baseDir, path)` IGNORES baseDir for an absolute path —
    94	 * while the spec it lives in is collection content that travels; everywhere
    95	 * else the link misleads or breaks. Such a path is rejected at the SCHEMA
    96	 * level as `malformed-css-link` (a structurally-rejected token, like the
    97	 * missing-selector case — it never enters cssLinks/auxiliaryCssLinks).
    98	 * Parent traversal (`../`) is DELIBERATELY accepted: a spec legitimately
    99	 * references author CSS elsewhere in the repository tree — the portability
   100	 * boundary is the repository/collection, not the spec's own directory.
   101	 */
   102	const NON_PORTABLE_CSS_PATH_RE = /^(?:[/\\]|~|[a-zA-Z]:)/;
   103	
   104	/** True iff `path` is machine-rooted (see {@link NON_PORTABLE_CSS_PATH_RE}). */
   105	export function isNonPortableCssPath(path: string): boolean {
   106	  return NON_PORTABLE_CSS_PATH_RE.test(path);
   107	}
   108	
   109	/** Opening/closing run of a code fence: ≥3 backticks or ≥3 tildes. */
   110	const FENCE_RUN_RE = /^(`{3,}|~{3,})/;
   111	/** ≥4-space (or tab) indentation: CommonMark indented code. */
   112	const INDENTED_CODE_RE = /^(?: {4}|\t)/;
   113	/** Any bullet shape (`-` / `*` / `+`) — indented bullets are list nesting, not indented code. */
   114	const BULLET_SHAPE_RE = /^[-*+]\s/;
   115	
   116	/** An open fenced code block: marker char + opener run length. */
   117	interface OpenFence {
   118	  readonly char: string;
   119	  readonly length: number;
   120	}
   121	
   122	/**
   123	 * Classify `trimmed` as a fence opener. Pragmatic CommonMark: any run of ≥3
   124	 * backticks/tildes opens a fence; trailing text is the info string (the
   125	 * CommonMark no-backticks-in-backtick-info-string rule is not modeled).
   126	 */
   127	function openingFence(trimmed: string): OpenFence | undefined {
   128	  const run = FENCE_RUN_RE.exec(trimmed);
   129	  if (run === null) {
   130	    return undefined;
   131	  }
   132	  return { char: run[1][0], length: run[1].length };
   133	}
   134	
   135	/**
   136	 * True iff `trimmed` closes `fence`: a line of ONLY the same marker char,
   137	 * repeated at least the opener's length (CommonMark: closers carry no info
   138	 * string and must be at least as long as the opener).
   139	 */
   140	function closesFence(fence: OpenFence, trimmed: string): boolean {
   141	  const run = FENCE_RUN_RE.exec(trimmed);
   142	  return run !== null && run[1] === trimmed && run[1][0] === fence.char && run[1].length >= fence.length;
   143	}
   144	
   145	const KNOWN_KEYS = ['kind', 'css', 'example', 'do', "don't"] as const;
   146	type FieldKey = (typeof KNOWN_KEYS)[number];
   147	
   148	function isKnownKey(key: string): key is FieldKey {
   149	  return (KNOWN_KEYS as readonly string[]).includes(key);
   150	}
   151	
   152	function isRuleKind(value: string): value is DesignRuleKind {
   153	  return (RULE_KINDS as readonly string[]).includes(value);
   154	}
   155	
   156	/** A rule section under one `rule:` heading, before validation. */
   157	interface RawRuleSection {
   158	  readonly id: string;
   159	  readonly headingLine: number;
   160	  kind?: string;
   161	  readonly cssLinks: CssLink[];
   162	  readonly examples: string[];
   163	  readonly dos: string[];
   164	  readonly donts: string[];
   165	}
   166	
   167	function newSection(id: string, headingLine: number): RawRuleSection {
   168	  return { id, headingLine, cssLinks: [], examples: [], dos: [], donts: [] };
   169	}
   170	
   171	interface FieldSink {
   172	  readonly section: RawRuleSection;
   173	  readonly findings: DesignSpecFinding[];
   174	}
   175	
   176	function recordField(sink: FieldSink, key: FieldKey, value: string, line: number): void {
   177	  const { section, findings } = sink;
   178	  if (value === '') {
   179	    findings.push({
   180	      rule: 'empty-field',
   181	      message: `Field "${key}:" has an empty value.`,
   182	      ruleId: section.id,
   183	      line,
   184	    });
   185	    return;
   186	  }
   187	  switch (key) {
   188	    case 'kind':
   189	      section.kind = value;
   190	      return;
   191	    case 'css': {
   192	      const spaceAt = value.search(/\s/);
   193	      if (spaceAt === -1) {
   194	        findings.push({
   195	          rule: 'malformed-css-link',
   196	          message: `css link "${value}" names a file but no selector — expected "css: <path> <selector>".`,
   197	          ruleId: section.id,
   198	          line,
   199	        });
   200	        return;
   201	      }
   202	      const path = value.slice(0, spaceAt);
   203	      if (isNonPortableCssPath(path)) {
   204	        findings.push({
   205	          rule: 'malformed-css-link',
   206	          message:
   207	            `css link path "${path}" is machine-rooted — paths are relative to the spec file, ` +
   208	            `so the spec stays portable with its collection; an absolute, drive-prefixed, or ` +
   209	            `~-prefixed path resolves only on its author's machine. Use a spec-relative path ` +
   210	            `("../" traversal within the repository is allowed).`,
   211	          ruleId: section.id,
   212	          line,
   213	        });
   214	        return;
   215	      }
   216	      section.cssLinks.push({
   217	        path,
   218	        selector: value.slice(spaceAt).trim(),
   219	      });
   220	      return;
   221	    }
   222	    case 'example':
   223	      section.examples.push(value);
   224	      return;
   225	    case 'do':
   226	      section.dos.push(value);
   227	      return;
   228	    case "don't":
   229	      section.donts.push(value);
   230	      return;
   231	  }
   232	}
   233	
   234	function validateSection(section: RawRuleSection, findings: DesignSpecFinding[]): DesignSpecRule | undefined {
   235	  const problems: DesignSpecFinding[] = [];
   236	  const at = { ruleId: section.id, line: section.headingLine };
   237	  if (section.kind === undefined) {
   238	    problems.push({ rule: 'missing-kind', message: `Rule "${section.id}" has no "kind:" field.`, ...at });
   239	  } else if (!isRuleKind(section.kind)) {
   240	    problems.push({
   241	      rule: 'unknown-kind',
   242	      message: `Rule "${section.id}" has kind "${section.kind}" — expected one of: ${RULE_KINDS.join(', ')}.`,
   243	      ...at,
   244	    });
   245	  }
   246	  if (section.cssLinks.length === 0) {
   247	    problems.push({
   248	      rule: 'missing-css-link',
   249	      message: `Rule "${section.id}" links to no live CSS — every rule needs ≥1 "css: <path> <selector>".`,
   250	      ...at,
   251	    });
   252	  }
   253	  if (section.examples.length === 0) {
   254	    problems.push({
   255	      rule: 'missing-example',
   256	      message: `Rule "${section.id}" carries zero example references — every rule needs ≥1 "example:".`,
   257	      ...at,
   258	    });
   259	  }
   260	  if (section.dos.length === 0 && section.donts.length === 0) {
   261	    problems.push({
   262	      rule: 'missing-guidance',
   263	      message: `Rule "${section.id}" has neither a "do:" nor a "don't:" guidance line.`,
   264	      ...at,
   265	    });
   266	  }
   267	  findings.push(...problems);
   268	  if (problems.length > 0 || section.kind === undefined || !isRuleKind(section.kind)) {
   269	    return undefined;
   270	  }
   271	  return {
   272	    id: section.id,
   273	    kind: section.kind,
   274	    cssLinks: section.cssLinks,
   275	    examples: section.examples,
   276	    dos: section.dos,
   277	    donts: section.donts,
   278	  };
   279	}
   280	
   281	/**
   282	 * Parse + structurally validate a design-language spec. Pure: text in,
   283	 * structure + findings out. `spec.rules` carries only the structurally-valid
   284	 * rules; every defect is a finding (never a silent drop).
   285	 *
   286	 * Single-pass surfacing (no finding waves): a section excluded from
   287	 * `spec.rules` is still INSPECTED, never skipped —
   288	 *   - every excluded-section kind (duplicate-id, malformed-rule-heading
   289	 *     near-miss, setext/paragraph declaration attempt) is parsed into a
   290	 *     throwaway section so its field-level defects (unknown-field /
   291	 *     empty-field / malformed-css-link) surface alongside the section-level
   292	 *     finding. Per-rule validation (missing-kind / missing-example / …)
   293	 *     deliberately does NOT fire for these — they are not rules;
   294	 *   - syntactically-usable css links of invalid, duplicate, and attempt
   295	 *     sections are returned as `auxiliaryCssLinks` so the liveness axis can
   296	 *     check them in the same run.
   297	 */
   298	export function parseDesignSpec(markdown: string): DesignSpecParseResult {
   299	  const findings: DesignSpecFinding[] = [];
   300	  const sections: RawRuleSection[] = [];
   301	  // Throwaway sections (duplicates + declaration attempts): inspected for
   302	  // field-level defects + auxiliary css links, excluded from `spec.rules`.
   303	  const throwawaySections: RawRuleSection[] = [];
   304	  const seenIds = new Set<string>();
   305	  let current: RawRuleSection | undefined;
   306	  let fence: OpenFence | undefined;
   307	
   308	  const lines = markdown.split('\n');
   309	  for (let i = 0; i < lines.length; i += 1) {
   310	    const line = lines[i].trimEnd();
   311	    const lineNo = i + 1;
   312	    const trimmed = line.trim();
   313	    // Code blocks are inert (see module doc): inside a fence, every line —
   314	    // headings, bullets, attempt shapes, setext lookahead — is skipped.
   315	    if (fence !== undefined) {
   316	      if (closesFence(fence, trimmed)) {
   317	        fence = undefined;
   318	      }
   319	      continue;
   320	    }
   321	    // A ≥4-space-indented non-bullet line is indented code, also inert
   322	    // (field bullets are column-0 by convention; indented BULLETS still
   323	    // parse so nested-list authoring keeps working). Checked before the
   324	    // fence opener so an indented ``` is code content, not a fence.
   325	    if (INDENTED_CODE_RE.test(line) && !BULLET_SHAPE_RE.test(trimmed)) {
   326	      continue;
   327	    }
   328	    const opened = openingFence(trimmed);
   329	    if (opened !== undefined) {
   330	      fence = opened;
   331	      continue;
   332	    }
   333	    const heading = HEADING_RE.exec(trimmed);
   334	    if (heading !== null) {
   335	      current = undefined;
   336	      const headingText = stripAtxClosingSequence(heading[1].trim());
   337	      const ruleHeading = RULE_HEADING_RE.exec(headingText);
   338	      if (ruleHeading === null) {
   339	        const attempt = classifyHeadingAttempt(headingText);
   340	        if (attempt !== undefined) {
   341	          findings.push({ rule: 'malformed-rule-heading', message: attempt.message, line: lineNo });
   342	          // Inspect the attempt's body too (mirrors the duplicate branch).
   343	          current = newSection(attempt.attemptedId, lineNo);
   344	          throwawaySections.push(current);
   345	        }
   346	        continue;
   347	      }
   348	      const id = ruleHeading[1].trim();
   349	      if (id === '') {
   350	        findings.push({
   351	          rule: 'malformed-rule-heading',
   352	          message: `Rule heading at line ${lineNo} has no id — expected "rule: <id>".`,
   353	          line: lineNo,
   354	        });
   355	        // Best-effort attribution: the heading text is all the id there is.
   356	        current = newSection(headingText, lineNo);
   357	        throwawaySections.push(current);
   358	        continue;
   359	      }
   360	      if (seenIds.has(id)) {
   361	        findings.push({
   362	          rule: 'duplicate-rule-id',
   363	          message: `Rule id "${id}" is declared more than once.`,
   364	          ruleId: id,
   365	          line: lineNo,
   366	        });
   367	        // Parse the duplicate into a throwaway section (NOT pushed to
   368	        // `sections`, no `seenIds` effect) so its field bullets are still
   369	        // inspected — field-level findings + auxiliary css links surface in
   370	        // this pass instead of after the author renames the id and reruns.
   371	        current = newSection(id, lineNo);
   372	        throwawaySections.push(current);
   373	        continue;
   374	      }
   375	      seenIds.add(id);
   376	      current = newSection(id, lineNo);
   377	      sections.push(current);
   378	      continue;
   379	    }
   380	    const lineAttempt = classifyLineAttempt(trimmed, lines[i + 1]);
   381	    if (lineAttempt !== undefined) {
   382	      // A declaration in the wrong syntax (setext heading / bare paragraph):
   383	      // flag it AND start a throwaway section so its bullets are inspected
   384	      // instead of silently merging into the preceding rule's section.
   385	      findings.push({ rule: 'malformed-rule-heading', message: lineAttempt.message, line: lineNo });
   386	      current = newSection(lineAttempt.attemptedId, lineNo);
   387	      throwawaySections.push(current);
   388	      continue;
   389	    }
   390	    if (current === undefined) {
   391	      continue;
   392	    }
   393	    const bullet = FIELD_BULLET_RE.exec(trimmed);
   394	    if (bullet === null) {
   395	      continue;
   396	    }
   397	    const key = bullet[1].replace(/’/g, "'");
   398	    if (!isKnownKey(key)) {
   399	      findings.push({
   400	        rule: 'unknown-field',
   401	        message: `Unknown field "${key}:" — known fields are: ${KNOWN_KEYS.join(', ')}.`,
   402	        ruleId: current.id,
   403	        line: lineNo,
   404	      });
   405	      continue;
   406	    }
   407	    recordField({ section: current, findings }, key, bullet[2].trim(), lineNo);
   408	  }
   409	
   410	  if (sections.length === 0) {
   411	    findings.push({
   412	      rule: 'no-rules',
   413	      message: 'The spec declares no rules — expected ≥1 "rule: <id>" heading.',
   414	    });
   415	  }
   416	
   417	  const rules: DesignSpecRule[] = [];
   418	  const auxiliaryCssLinks: RuleScopedCssLink[] = [];
   419	  for (const section of sections) {
   420	    const rule = validateSection(section, findings);
   421	    if (rule !== undefined) {
   422	      rules.push(rule);
   423	      continue;
   424	    }
   425	    // Invalid housing, usable links: the liveness axis still checks them.
   426	    auxiliaryCssLinks.push(...section.cssLinks.map((link) => ({ ruleId: section.id, link })));
   427	  }
   428	  for (const section of throwawaySections) {
   429	    auxiliaryCssLinks.push(...section.cssLinks.map((link) => ({ ruleId: section.id, link })));
   430	  }
   431	
   432	  return { ok: findings.length === 0, spec: { rules }, findings, auxiliaryCssLinks };
   433	}

exec
/bin/zsh -lc "nl -ba plugins/design-control/src/design-language/selector-canon.ts | sed -n '1,280p'" in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
     1	/**
     2	 * Selector-text canonicalization primitives for link-liveness
     3	 * (AUDIT-round2-codex-01, AUDIT-round2-claude-04).
     4	 *
     5	 * The liveness matcher compares a query selector against source selector
     6	 * preludes as TEXT. These helpers make that comparison honest in both
     7	 * directions: attribute values are rewritten to ONE canonical spelling
     8	 * (double-quoted) so quote style never produces a false dead AND distinct
     9	 * values never conflate into a false green; functional pseudo-class arguments
    10	 * are preserved for comparison (or stripped/blanked by the matcher when the
    11	 * query does not name them — an exclusion argument or attribute value is not
    12	 * a selector definition); whitespace is normalized identically on both sides.
    13	 *
    14	 * Remaining accepted approximations (stated, not silent):
    15	 * - Escape sequences inside quoted values are kept verbatim, not decoded —
    16	 *   `[a='it\'s']` and `[a="it's"]` spell the same value but do not unify.
    17	 * - Whitespace inside functional-pseudo arguments is collapsed to single
    18	 *   spaces (and trimmed at parens/commas/combinator-shaped characters), not
    19	 *   erased — the combinator pass strips spacing around `+`, so
    20	 *   `:nth-child(2n+1)` and `:nth-child(2n + 1)` DO unify, while the
    21	 *   `-`-spelled forms (`:nth-child(2n-1)` vs `(2n - 1)`) do not.
    22	 * - Attribute names are matched as plain idents; namespaced attribute
    23	 *   selectors (`[svg|href]`) are left un-canonicalized (compared verbatim).
    24	 * - CSS nesting is not composed: the liveness matcher sees each nested
    25	 *   prelude FLAT at its own depth (see `collectSelectorPreludes` in
    26	 *   link-liveness.ts), so on nested sources rules must anchor to the leaf
    27	 *   selector — a composed query like `.btn .icon` never matches
    28	 *   `.btn { .icon {} }` (AUDIT-round4-claude-02).
    29	 */
    30	
    31	/** True for characters that extend a CSS ident (would change the selector). */
    32	export function isIdentChar(ch: string | undefined): boolean {
    33	  return ch !== undefined && /[A-Za-z0-9_-]/.test(ch);
    34	}
    35	
    36	/**
    37	 * Copy a string literal verbatim starting at `text[start]` (an opening quote),
    38	 * walking `\` escapes. Returns the copied literal (closing quote included when
    39	 * present) and the index just past it.
    40	 */
    41	export function copyStringLiteral(text: string, start: number): { copied: string; end: number } {
    42	  const quote = text[start];
    43	  let copied = quote;
    44	  let i = start + 1;
    45	  while (i < text.length && text[i] !== quote) {
    46	    copied += text[i];
    47	    if (text[i] === '\\' && i + 1 < text.length) {
    48	      copied += text[i + 1];
    49	      i += 2;
    50	    } else {
    51	      i += 1;
    52	    }
    53	  }
    54	  if (i < text.length) {
    55	    copied += quote;
    56	    i += 1;
    57	  }
    58	  return { copied, end: i };
    59	}
    60	
    61	/** Strip CSS comments; string-literal contents are copied verbatim. */
    62	export function stripComments(text: string): string {
    63	  let out = '';
    64	  let i = 0;
    65	  while (i < text.length) {
    66	    const ch = text[i];
    67	    if (ch === '/' && text[i + 1] === '*') {
    68	      const end = text.indexOf('*/', i + 2);
    69	      i = end === -1 ? text.length : end + 2;
    70	      continue;
    71	    }
    72	    if (ch === '"' || ch === "'") {
    73	      const literal = copyStringLiteral(text, i);
    74	      out += literal.copied;
    75	      i = literal.end;
    76	      continue;
    77	    }
    78	    out += ch;
    79	    i += 1;
    80	  }
    81	  return out;
    82	}
    83	
    84	/**
    85	 * One attribute selector: name, optional operator + value (double-quoted,
    86	 * single-quoted, or unquoted) + optional case-sensitivity flag.
    87	 */
    88	const ATTRIBUTE_SELECTOR =
    89	  /\[\s*([A-Za-z0-9_-]+)\s*(?:([~|^$*]?=)\s*(?:"((?:[^"\\]|\\.)*)"|'((?:[^'\\]|\\.)*)'|([^\s\]'"]+))(?:\s+([iIsS]))?\s*)?\]/g;
    90	
    91	/**
    92	 * Rewrite every attribute selector to one canonical spelling:
    93	 * `[attr=val]` / `[attr='val']` / `[attr="val"]` → `[attr="val"]` (value text
    94	 * preserved; flag lowercased; internal whitespace dropped). Different values
    95	 * therefore never conflate, and quote styles never diverge.
    96	 */
    97	export function canonicalizeAttributeSelectors(text: string): string {
    98	  let out = '';
    99	  let last = 0;
   100	  ATTRIBUTE_SELECTOR.lastIndex = 0;
   101	  let match = ATTRIBUTE_SELECTOR.exec(text);
   102	  while (match !== null) {
   103	    out += text.slice(last, match.index);
   104	    const name: string = match[1];
   105	    const operator: string | undefined = match[2];
   106	    if (operator === undefined) {
   107	      out += `[${name}]`;
   108	    } else {
   109	      const value: string = match[3] ?? match[4] ?? match[5] ?? '';
   110	      const flag: string | undefined = match[6];
   111	      out += `[${name}${operator}"${value}"${flag === undefined ? '' : ` ${flag.toLowerCase()}`}]`;
   112	    }
   113	    last = match.index + match[0].length;
   114	    match = ATTRIBUTE_SELECTOR.exec(text);
   115	  }
   116	  return out + text.slice(last);
   117	}
   118	
   119	/**
   120	 * Blank the VALUES of canonicalized attribute selectors (`[a="x"]` → `[a=""]`).
   121	 * Used on the haystack when the query names no attribute selector, so a bare
   122	 * query never matches text that exists only as an attribute value — an
   123	 * attribute value is not a selector definition. Run AFTER
   124	 * {@link canonicalizeAttributeSelectors} (it only sees double-quoted values).
   125	 */
   126	export function blankAttributeValues(text: string): string {
   127	  return text.replace(/(\[[A-Za-z0-9_-]+[~|^$*]?=)"(?:[^"\\]|\\.)*"/g, '$1""');
   128	}
   129	
   130	/**
   131	 * Blank the NAMES of attribute selectors (`[ghost]` → `[]`, `[a=""]` → `[=""]`).
   132	 * Companion to {@link blankAttributeValues}, for the same haystack mode: when
   133	 * the query names no attribute selector, a bare query must not match text
   134	 * that exists only as an attribute NAME — `ghost` is not defined by
   135	 * `[ghost] {}` (AUDIT-round4-claude-01). Run AFTER
   136	 * {@link blankAttributeValues} (its regex keys on the name).
   137	 */
   138	export function blankAttributeNames(text: string): string {
   139	  return text.replace(/\[[A-Za-z0-9_-]+/g, '[');
   140	}
   141	
   142	/**
   143	 * Strip the CONTENTS of functional pseudo-class arguments (delimiters stay):
   144	 * `:not(.ghost)` → `:not()`, for any `:<ident>(` / `::<ident>(`, balancing
   145	 * nested parens (`:not(:is(.a))` → `:not()`) and skipping string literals so
   146	 * parens inside quoted values do not unbalance the walk. A selector that
   147	 * appears only as an exclusion or matcher argument is not styled by the rule,
   148	 * so it must not satisfy a liveness query that never named the argument.
   149	 */
   150	export function stripFunctionalPseudoArgs(text: string): string {
   151	  let out = '';
   152	  let i = 0;
   153	  while (i < text.length) {
   154	    const ch = text[i];
   155	    if (ch === '"' || ch === "'") {
   156	      const literal = copyStringLiteral(text, i);
   157	      out += literal.copied;
   158	      i = literal.end;
   159	      continue;
   160	    }
   161	    if (ch === ':') {
   162	      let identStart = i + 1;
   163	      if (text[identStart] === ':') {
   164	        identStart += 1;
   165	      }
   166	      let identEnd = identStart;
   167	      while (identEnd < text.length && isIdentChar(text[identEnd])) {
   168	        identEnd += 1;
   169	      }
   170	      if (identEnd > identStart && text[identEnd] === '(') {
   171	        out += text.slice(i, identEnd + 1);
   172	        let depth = 1;
   173	        i = identEnd + 1;
   174	        while (i < text.length && depth > 0) {
   175	          if (text[i] === '"' || text[i] === "'") {
   176	            i = copyStringLiteral(text, i).end;
   177	            continue;
   178	          }
   179	          if (text[i] === '(') {
   180	            depth += 1;
   181	          } else if (text[i] === ')') {
   182	            depth -= 1;
   183	          }
   184	          i += 1;
   185	        }
   186	        if (depth === 0) {
   187	          out += ')';
   188	        }
   189	        continue;
   190	      }
   191	    }
   192	    out += ch;
   193	    i += 1;
   194	  }
   195	  return out;
   196	}
   197	
   198	/**
   199	 * Normalize selector whitespace for textual comparison: runs collapse to one
   200	 * space; spaces inside paren delimiters, around commas, and around the
   201	 * non-descendant combinators (`>` `+` `~` `||`) drop; ends trim. Prettier
   202	 * writes spaced combinators, so `.a>.b` and `.a > .b` must compare equal — a
   203	 * CSS reformat must never flip a green link dead. The `~` of the `~=`
   204	 * attribute operator is safe here: {@link canonicalizeAttributeSelectors}
   205	 * runs first and emits `[a~="x"]` with no surrounding spaces, so the
   206	 * combinator pass is an identity on it. Applied identically to query and
   207	 * haystack, so descendant-combinator, combinator, and argument-list spacing
   208	 * differences never produce a false dead.
   209	 */
   210	export function normalizeSelectorWhitespace(text: string): string {
   211	  return text
   212	    .replace(/\s+/g, ' ')
   213	    .replace(/\(\s+/g, '(')
   214	    .replace(/\s+\)/g, ')')
   215	    .replace(/\s*,\s*/g, ',')
   216	    .replace(/\s*(\|\||[>+~])\s*/g, '$1')
   217	    .trim();
   218	}

exec
/bin/zsh -lc "nl -ba plugins/design-control/src/design-language/link-liveness.ts | sed -n '1,380p'" in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
     1	/**
     2	 * Static link-liveness for design-language spec rules (Phase 2, axis B).
     3	 *
     4	 * Each rule's `css: <path> <selector>` link must point at an author-written
     5	 * CSS file in which the selector is DEFINED. The check is STATIC — pure file
     6	 * reads against source, no app boot, no engine (round-6 M1: "authoring
     7	 * artifacts only / no capture dependency").
     8	 *
     9	 * Scope: this check validates selectors in author-written `.css` sources
    10	 * only. Utility-framework, CSS-in-JS, and hashed CSS-Modules links do
    11	 * not establish link-liveness — they are recorded as `skipped` (visible in
    12	 * the result and in CLI output), never silently dropped and never fabricated
    13	 * into a dead-link verdict. Liveness ≠ truthfulness: a resolving selector
    14	 * does not establish that the live CSS still matches the rule's described
    15	 * intent — `spec-truthfulness` is a separate axis this check does not
    16	 * perform (scope decision recorded in specs/001-design-control/spec.md).
    17	 *
    18	 * "Defined in source" is implemented as: the selector appears, ident-boundary
    19	 * exact, inside some selector prelude of the file — preludes are the text
    20	 * runs that precede `{` after comments are stripped and at-rule preludes are
    21	 * excluded (their blocks are descended into, so a rule inside `@media` counts;
    22	 * `content: ".ghost"` and commented-out rules do not). Both sides are
    23	 * canonicalized before comparison (AUDIT-round2-codex-01 / -claude-04):
    24	 * attribute values rewrite to one double-quoted spelling with the value text
    25	 * PRESERVED, so `input[type=text]` matches `input[type="text"]` while
    26	 * `[data-state="open"]` never matches `[data-state="closed"]`; functional
    27	 * pseudo-class arguments (`:not(.ghost)`, `:is(...)`, `:where(...)`,
    28	 * `:has(...)`, `:nth-child(...)`) are compared when the QUERY names them and
    29	 * excluded from the scan when it does not — a class that exists only as an
    30	 * exclusion, matcher argument, or attribute value has no styling of its own,
    31	 * so it must not count as a live anchor for a query that never named it.
    32	 * Remaining accepted approximations are stated in `selector-canon.ts`
    33	 * (verbatim escapes; collapsed-not-erased argument whitespace).
    34	 */
    35	
    36	import { readFileSync } from 'node:fs';
    37	import { resolve } from 'node:path';
    38	import { isNonPortableCssPath } from '@/design-language/schema';
    39	import {
    40	  blankAttributeNames,
    41	  blankAttributeValues,
    42	  canonicalizeAttributeSelectors,
    43	  copyStringLiteral,
    44	  isIdentChar,
    45	  normalizeSelectorWhitespace,
    46	  stripComments,
    47	  stripFunctionalPseudoArgs,
    48	} from '@/design-language/selector-canon';
    49	import type {
    50	  CssLink,
    51	  DesignSpecFinding,
    52	  ParsedDesignSpec,
    53	  RuleScopedCssLink,
    54	} from '@/design-language/types';
    55	
    56	/** A link outside the validated .css scope, recorded visibly. */
    57	export interface SkippedLink {
    58	  readonly ruleId: string;
    59	  readonly link: CssLink;
    60	  readonly reason: 'non-css-target';
    61	}
    62	
    63	export interface LivenessResult {
    64	  /** True iff findings is empty (skipped links do not fail the check). */
    65	  readonly ok: boolean;
    66	  readonly findings: readonly DesignSpecFinding[];
    67	  readonly skipped: readonly SkippedLink[];
    68	}
    69	
    70	/**
    71	 * `@keyframes` at-rule names, vendor-prefix tolerant (`@-webkit-keyframes`,
    72	 * `@-moz-keyframes`, …). Their blocks are skipped wholesale by
    73	 * {@link collectSelectorPreludes}.
    74	 */
    75	const KEYFRAMES_AT_RULE = /^@(?:-[a-z]+-)?keyframes\b/i;
    76	
    77	/**
    78	 * Return the index just past the `}` matching the `{` at `css[open]`,
    79	 * balancing nested braces and skipping comments and string literals (a brace
    80	 * inside either is content, not structure). Unclosed input runs to the end.
    81	 */
    82	function skipBlock(css: string, open: number): number {
    83	  let depth = 1;
    84	  let i = open + 1;
    85	  while (i < css.length && depth > 0) {
    86	    const ch = css[i];
    87	    if (ch === '/' && css[i + 1] === '*') {
    88	      const end = css.indexOf('*/', i + 2);
    89	      i = end === -1 ? css.length : end + 2;
    90	      continue;
    91	    }
    92	    if (ch === '"' || ch === "'") {
    93	      i = copyStringLiteral(css, i).end;
    94	      continue;
    95	    }
    96	    if (ch === '{') {
    97	      depth += 1;
    98	    } else if (ch === '}') {
    99	      depth -= 1;
   100	    }
   101	    i += 1;
   102	  }
   103	  return i;
   104	}
   105	
   106	/**
   107	 * Collect selector preludes: text runs preceding `{`, at every nesting depth,
   108	 * excluding at-rule preludes (which are descended into, not matched against)
   109	 * — with one descent exception: `@keyframes` blocks (vendor-prefixed forms
   110	 * included) are skipped WHOLE, contributing no preludes, because their step
   111	 * "selectors" (`from` / `to` / percentages) are animation waypoints, not
   112	 * element selectors a design rule can anchor to (AUDIT-round4-claude-04).
   113	 * Preludes are matched FLAT, not ancestor-composed (AUDIT-round4-claude-02):
   114	 * a nested source `.btn { .icon {} }` contributes the preludes `.btn` and
   115	 * `.icon` — never the composed `.btn .icon` — and `&:hover` stays `&:hover`,
   116	 * never `.btn:hover`; on nested sources anchor rules to the leaf selector.
   117	 * Comments are stripped; string-literal CONTENTS are copied verbatim (their
   118	 * structural characters are content, never braces/semicolons) so attribute
   119	 * values survive into preludes for canonical comparison. Prelude buffers
   120	 * reset on `{`, `}`, and `;` so declaration text (`content: ".ghost"`) never
   121	 * leaks into selector position.
   122	 */
   123	function collectSelectorPreludes(css: string): string[] {
   124	  const preludes: string[] = [];
   125	  let buffer = '';
   126	  let i = 0;
   127	  while (i < css.length) {
   128	    const ch = css[i];
   129	    if (ch === '/' && css[i + 1] === '*') {
   130	      const end = css.indexOf('*/', i + 2);
   131	      i = end === -1 ? css.length : end + 2;
   132	      continue;
   133	    }
   134	    if (ch === '"' || ch === "'") {
   135	      const literal = copyStringLiteral(css, i);
   136	      buffer += literal.copied;
   137	      i = literal.end;
   138	      continue;
   139	    }
   140	    if (ch === '{') {
   141	      const prelude = buffer.trim();
   142	      buffer = '';
   143	      if (KEYFRAMES_AT_RULE.test(prelude)) {
   144	        i = skipBlock(css, i);
   145	        continue;
   146	      }
   147	      if (prelude !== '' && !prelude.startsWith('@')) {
   148	        preludes.push(prelude);
   149	      }
   150	    } else if (ch === '}' || ch === ';') {
   151	      buffer = '';
   152	    } else {
   153	      buffer += ch;
   154	    }
   155	    i += 1;
   156	  }
   157	  return preludes;
   158	}
   159	
   160	/**
   161	 * True iff `selector` appears ident-boundary exact inside some selector
   162	 * prelude of `css`, with both sides canonicalized identically: attribute
   163	 * values rewrite to one double-quoted spelling with their text PRESERVED
   164	 * (quote style never diverges; `[data-state="open"]` never matches
   165	 * `[data-state="closed"]`), and whitespace normalizes (descendant
   166	 * combinators, argument spacing).
   167	 *
   168	 * Two-mode scan, derived from the query (a value or argument the query never
   169	 * named is not a definition — exclusion is not styling, AUDIT-20260611-18):
   170	 * - Query WITHOUT functional pseudo args: haystack args are stripped, so
   171	 *   `.ghost` does not match `.real:not(.ghost)`. A query WITH them compares
   172	 *   argument text — `.real:not(.ghost)` matches only a `:not(.ghost)` source,
   173	 *   never `:not(.other)`.
   174	 * - Query WITHOUT an attribute selector: haystack attribute values AND names
   175	 *   are blanked, so `.ghost` does not match `[data-icon=".ghost"]`, `ghost`
   176	 *   does not match `[class~=ghost]`, and `ghost` does not match `[ghost]`
   177	 *   (a name is no more a definition than a value, AUDIT-round4-claude-01).
   178	 *   A query WITH one compares names and values.
   179	 *
   180	 * A query that BEGINS with an ident character is a type selector, so a hit
   181	 * preceded by `.` or `#` in the haystack is a different selector (a class or
   182	 * id whose name merely echoes the query — the dropped-leading-dot trap) and
   183	 * is rejected; `header` matches `header {}` but never `.header` / `#header`
   184	 * (AUDIT-round4-claude-01).
   185	 */
   186	export function cssDefinesSelector(css: string, selector: string): boolean {
   187	  const canonicalQuery = canonicalizeAttributeSelectors(stripComments(selector));
   188	  const compareArgs = canonicalQuery.includes('(');
   189	  const compareAttrValues = canonicalQuery.includes('[');
   190	  // Stripping/blanking the query itself would be a no-op in the modes where
   191	  // they apply (no parens / no brackets to act on), so normalize directly.
   192	  const query = normalizeSelectorWhitespace(canonicalQuery);
   193	  if (query === '') {
   194	    return false;
   195	  }
   196	  const typeSelectorQuery = isIdentChar(query[0]);
   197	  for (const prelude of collectSelectorPreludes(css)) {
   198	    let haystack = canonicalizeAttributeSelectors(prelude);
   199	    if (!compareArgs) {
   200	      haystack = stripFunctionalPseudoArgs(haystack);
   201	    }
   202	    if (!compareAttrValues) {
   203	      haystack = blankAttributeNames(blankAttributeValues(haystack));
   204	    }
   205	    haystack = normalizeSelectorWhitespace(haystack);
   206	    let from = 0;
   207	    while (true) {
   208	      const at = haystack.indexOf(query, from);
   209	      if (at === -1) {
   210	        break;
   211	      }
   212	      const before = haystack[at - 1];
   213	      if (
   214	        !isIdentChar(before) &&
   215	        !isIdentChar(haystack[at + query.length]) &&
   216	        !(typeSelectorQuery && (before === '.' || before === '#'))
   217	      ) {
   218	        return true;
   219	      }
   220	      from = at + 1;
   221	    }
   222	  }
   223	  return false;
   224	}
   225	
   226	/**
   227	 * Check a flat list of rule-scoped css links against source. This is the
   228	 * liveness core: it carries no opinion about the HOUSING of a link, so the
   229	 * file-level check can run it over the valid rules' links AND the parse
   230	 * result's `auxiliaryCssLinks` (links of structurally-invalid / duplicate
   231	 * sections) in the same pass. Paths resolve relative to `baseDir` (the spec
   232	 * file's directory). Missing file → `dead-link-file`; selector not defined →
   233	 * `dead-link-selector`; non-.css target → skipped.
   234	 *
   235	 * Machine-rooted paths THROW (AUDIT-round2-codex-02): `resolve(baseDir, path)`
   236	 * ignores baseDir for an absolute path, so the check would read the author's
   237	 * machine-local file and fabricate a verdict that does not travel.
   238	 * {@link parseDesignSpec} rejects such paths as `malformed-css-link` before
   239	 * they reach this seam, so a machine-rooted link arriving here is a caller
   240	 * contract violation (this function is exported and callable with hand-built
   241	 * links) — fail loud rather than return a nonportable green/red.
   242	 */
   243	export function checkCssLinkLiveness(
   244	  links: readonly RuleScopedCssLink[],
   245	  baseDir: string,
   246	): LivenessResult {
   247	  const findings: DesignSpecFinding[] = [];
   248	  const skipped: SkippedLink[] = [];
   249	  for (const { ruleId, link } of links) {
   250	    if (isNonPortableCssPath(link.path)) {
   251	      throw new Error(
   252	        `Rule "${ruleId}" css link path ${JSON.stringify(link.path)} is machine-rooted — ` +
   253	          `css link paths are relative to the spec file, and resolve() would ignore baseDir for ` +
   254	          `an absolute path, checking a machine-local file the spec cannot portably name. ` +
   255	          `parseDesignSpec rejects these as malformed-css-link; fix the call site to pass ` +
   256	          `spec-relative paths ("../" traversal within the repository is allowed).`,
   257	      );
   258	    }
   259	    if (!link.path.toLowerCase().endsWith('.css')) {
   260	      skipped.push({ ruleId, link, reason: 'non-css-target' });
   261	      continue;
   262	    }
   263	    const absolute = resolve(baseDir, link.path);
   264	    let css: string;
   265	    try {
   266	      css = readFileSync(absolute, 'utf8');
   267	    } catch {
   268	      findings.push({
   269	        rule: 'dead-link-file',
   270	        message: `Rule "${ruleId}" links to "${link.path}" which does not resolve to a readable file (looked at ${absolute}).`,
   271	        ruleId,
   272	      });
   273	      continue;
   274	    }
   275	    if (!cssDefinesSelector(css, link.selector)) {
   276	      findings.push({
   277	        rule: 'dead-link-selector',
   278	        message: `Rule "${ruleId}" links to selector "${link.selector}" which is not defined in "${link.path}".`,
   279	        ruleId,
   280	      });
   281	    }
   282	  }
   283	  return { ok: findings.length === 0, findings, skipped };
   284	}
   285	
   286	/**
   287	 * Check every valid rule's css links against source — the spec-shaped wrapper
   288	 * over {@link checkCssLinkLiveness} (same per-link contract).
   289	 */
   290	export function checkLinkLiveness(spec: ParsedDesignSpec, baseDir: string): LivenessResult {
   291	  const links = spec.rules.flatMap((rule) =>
   292	    rule.cssLinks.map((link) => ({ ruleId: rule.id, link })),
   293	  );
   294	  return checkCssLinkLiveness(links, baseDir);
   295	}

exec
/bin/zsh -lc "nl -ba plugins/design-control/src/design-language/check-spec-file.ts | sed -n '1,180p'" in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
     1	/**
     2	 * File-level entry to the design-language spec check — the enforcement seam
     3	 * the `/design-control:translate-design-language` skill (and the
     4	 * `bin/check-design-spec` shim) route EVERY spec draft through, hand-authored
     5	 * or engine-accelerated alike. Mirrors `@/authoring/lint-file` for wireframes:
     6	 * a thin composition of the existing axes, no parallel validation path.
     7	 */
     8	
     9	import { readFileSync } from 'node:fs';
    10	import { dirname, resolve } from 'node:path';
    11	import {
    12	  checkCssLinkLiveness,
    13	  checkLinkLiveness,
    14	  type SkippedLink,
    15	} from '@/design-language/link-liveness';
    16	import { parseDesignSpec } from '@/design-language/schema';
    17	import type { DesignSpecFinding, ParsedDesignSpec } from '@/design-language/types';
    18	
    19	export interface DesignSpecCheckResult {
    20	  /** True iff findings is empty (skipped links stay green but visible). */
    21	  readonly ok: boolean;
    22	  readonly spec: ParsedDesignSpec;
    23	  /** Schema findings followed by link-liveness findings. */
    24	  readonly findings: readonly DesignSpecFinding[];
    25	  readonly skipped: readonly SkippedLink[];
    26	}
    27	
    28	/**
    29	 * Check a design-language spec FILE: read it, validate the markdown schema,
    30	 * then check link-liveness with css paths resolved against the spec file's
    31	 * own directory. Liveness runs over the valid rules AND the parse result's
    32	 * auxiliary css links (links housed in structurally-invalid / duplicate
    33	 * sections), so a schema defect and a dead link on the same section surface
    34	 * in ONE run — never in fix-and-rerun waves. Fails loud on an unreadable
    35	 * file — a missing spec is an error, never a clean verdict.
    36	 */
    37	export function checkDesignSpecFile(filePath: string): DesignSpecCheckResult {
    38	  const absolute = resolve(filePath);
    39	  const markdown = readFileSync(absolute, 'utf8');
    40	  const parsed = parseDesignSpec(markdown);
    41	  const baseDir = dirname(absolute);
    42	  const liveness = checkLinkLiveness(parsed.spec, baseDir);
    43	  const auxiliary = checkCssLinkLiveness(parsed.auxiliaryCssLinks, baseDir);
    44	  const findings = [...parsed.findings, ...liveness.findings, ...auxiliary.findings];
    45	  return {
    46	    ok: findings.length === 0,
    47	    spec: parsed.spec,
    48	    findings,
    49	    skipped: [...liveness.skipped, ...auxiliary.skipped],
    50	  };
    51	}
    52	
    53	/** Line-oriented output sink, injected so the CLI core is testable. */
    54	export interface CliIo {
    55	  out(line: string): void;
    56	  err(line: string): void;
    57	}
    58	
    59	const USAGE = 'usage: check-design-spec <design-language-spec.md>';
    60	
    61	/**
    62	 * CLI core behind `bin/check-design-spec`. Exit-code contract (the skill's
    63	 * gate, same shape as check-wireframe):
    64	 *   0 — spec green (zero findings; skipped links are reported but green)
    65	 *   1 — findings present, or the file could not be read (descriptive error;
    66	 *       never a fabricated verdict)
    67	 *   2 — usage error
    68	 */
    69	export function runCheckDesignSpec(argv: readonly string[], io: CliIo): number {
    70	  if (argv.length !== 1) {
    71	    io.err(USAGE);
    72	    return 2;
    73	  }
    74	  const filePath = argv[0];
    75	  let result: DesignSpecCheckResult;
    76	  try {
    77	    result = checkDesignSpecFile(filePath);
    78	  } catch (error) {
    79	    io.err(error instanceof Error ? error.message : String(error));
    80	    return 1;
    81	  }
    82	  for (const skip of result.skipped) {
    83	    io.out(
    84	      `note: rule "${skip.ruleId}" link "${skip.link.path} ${skip.link.selector}" is a non-CSS target — reported unchecked, does not establish link-liveness (this check validates selectors in author-written .css sources only; CSS-in-JS / utility-framework / CSS-Modules targets are outside its validated scope).`,
    85	    );
    86	  }
    87	  if (result.ok) {
    88	    io.out(`${filePath}: spec green — 0 findings (${result.spec.rules.length} rule(s))`);
    89	    return 0;
    90	  }
    91	  for (const finding of result.findings) {
    92	    io.err(`${finding.rule}${finding.ruleId ? ` (rule: ${finding.ruleId})` : ''}: ${finding.message}`);
    93	  }
    94	  io.err(`${filePath}: ${result.findings.length} finding(s)`);
    95	  return 1;
    96	}

exec
/bin/zsh -lc "nl -ba plugins/design-control/src/design-language/rule-attempt.ts | sed -n '1,180p'" in /Users/orion/work/deskwork-work/design-control
exec
/bin/zsh -lc "nl -ba plugins/design-control/src/design-language/check-design-spec-cli.ts | sed -n '1,80p'" in /Users/orion/work/deskwork-work/design-control
exec
/bin/zsh -lc "nl -ba plugins/design-control/src/design-language/types.ts | sed -n '1,150p'" in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
     1	/**
     2	 * Process entry for `bin/check-design-spec`. All behavior lives in
     3	 * {@link runCheckDesignSpec} (tested directly); this file only wires argv and
     4	 * the process exit code.
     5	 */
     6	
     7	import { runCheckDesignSpec } from '@/design-language/check-spec-file';
     8	
     9	process.exitCode = runCheckDesignSpec(process.argv.slice(2), {
    10	  out: (line) => console.log(line),
    11	  err: (line) => console.error(line),
    12	});

 succeeded in 0ms:
     1	/**
     2	 * Rule-declaration ATTEMPT detection for the design-language schema
     3	 * (extracted from `schema.ts` to keep that module under the file-size cap).
     4	 *
     5	 * The convention's only valid declaration is an ATX heading `rule: <id>`.
     6	 * Everything here classifies the NEAR MISSES — text that an author plainly
     7	 * meant as a declaration but that misses the strict form — so the parser can
     8	 * surface a `malformed-rule-heading` finding instead of silently demoting the
     9	 * intended rule to inert prose (and, worse, merging its bullets into the
    10	 * preceding rule's section).
    11	 *
    12	 * Trigger calibration (deliberately narrow): a heading counts as an attempt
    13	 * only when it carries a colon after the `rule` word (`Rule: x`, `rule : x`)
    14	 * OR is exact-lowercase `rule` followed by exactly one id-shaped token
    15	 * (`rule missing-colon`). Multi-word prose headings (`Rule of thumb`,
    16	 * `Rule kinds`) stay inert — reserving every heading that merely STARTS with
    17	 * "rule" would structurally forbid ordinary prose headings.
    18	 *
    19	 * Non-heading lines count only when they are line-initial exact-lowercase
    20	 * `rule: <id>` with a SINGLE id-shaped token and nothing after — the
    21	 * unambiguous declaration shape in the wrong syntax (setext heading or bare
    22	 * paragraph). The id-shape constraint mirrors the heading-level calibration
    23	 * (AUDIT round-3 claude-03): lowercase prose sentences like
    24	 * `rule: never introduce raw hex blues outside the tokens.` stay inert —
    25	 * a multi-word value is guidance prose, not a declaration. Capitalised prose
    26	 * like "Rule: always X." and mid-line mentions of "rule:" also stay inert.
    27	 */
    28	
    29	/** A classified declaration attempt: finding message + attribution id. */
    30	export interface RuleAttempt {
    31	  /** Complete `malformed-rule-heading` finding message. */
    32	  readonly message: string;
    33	  /** Best-effort id from the attempted declaration, for finding attribution. */
    34	  readonly attemptedId: string;
    35	}
    36	
    37	/** Colon after the rule word, any case / spacing: `Rule: x`, `rule : x`. */
    38	const COLON_ATTEMPT_RE = /^rule\s*:/i;
    39	/** Space between `rule` and the colon: `rule :x`, `Rule : x`. */
    40	const SPACED_COLON_RE = /^rule\s+:/i;
    41	/** Exact-lowercase `rule` + exactly one id-shaped token: `rule missing-colon`. */
    42	const BARE_ID_ATTEMPT_RE = /^rule\s+[\w-]+$/;
    43	/**
    44	 * Line-initial exact-lowercase declaration shape on a non-heading line:
    45	 * exactly one id-shaped token after the colon (`rule: beta`). Multi-word
    46	 * lowercase prose (`rule: never use raw hex.`) is NOT an attempt.
    47	 */
    48	const LINE_ATTEMPT_RE = /^rule\s*:\s*[\w-]+\s*$/;
    49	/** A setext underline: a line of only `=` or only `-` characters. */
    50	const SETEXT_UNDERLINE_RE = /^(?:=+|-+)$/;
    51	
    52	/** Best-effort id from the attempted heading text (`Rule: x` → `x`). */
    53	function deriveAttemptedId(text: string): string {
    54	  const id = /^rule\s*:?\s*(.*)$/i.exec(text)?.[1].trim() ?? '';
    55	  return id === '' ? text : id;
    56	}
    57	
    58	/**
    59	 * Classify an ATX heading's text (already known NOT to match the strict
    60	 * `rule: <id>` form) as a declaration attempt, naming the ACTUAL mismatch.
    61	 */
    62	export function classifyHeadingAttempt(headingText: string): RuleAttempt | undefined {
    63	  const colonAttempt = COLON_ATTEMPT_RE.test(headingText);
    64	  if (!colonAttempt && !BARE_ID_ATTEMPT_RE.test(headingText)) {
    65	    return undefined;
    66	  }
    67	  let offence: string;
    68	  if (SPACED_COLON_RE.test(headingText)) {
    69	    offence = 'has a space between "rule" and the ":"';
    70	  } else if (colonAttempt) {
    71	    offence = 'the "rule:" prefix must be lowercase';
    72	  } else {
    73	    offence = 'missing the ":" after "rule"';
    74	  }
    75	  return {
    76	    message: `Heading "${headingText}" looks like a rule heading but ${offence} — expected "rule: <id>".`,
    77	    attemptedId: deriveAttemptedId(headingText),
    78	  };
    79	}
    80	
    81	/**
    82	 * Classify a non-heading line as a declaration attempt: a line-initial
    83	 * `rule: <id>` (single id-shaped token, nothing after) rendered as a setext
    84	 * heading (underlined by `---`/`===`) or as a bare paragraph. Both direct the
    85	 * author to the documented ATX syntax. Lowercase prose with a multi-word
    86	 * value stays inert (AUDIT round-3 claude-03).
    87	 */
    88	export function classifyLineAttempt(lineText: string, nextLine: string | undefined): RuleAttempt | undefined {
    89	  if (!LINE_ATTEMPT_RE.test(lineText)) {
    90	    return undefined;
    91	  }
    92	  const attemptedId = deriveAttemptedId(lineText);
    93	  const isSetext = nextLine !== undefined && SETEXT_UNDERLINE_RE.test(nextLine.trim());
    94	  const message = isSetext
    95	    ? `Setext heading "${lineText}" attempts a rule declaration — rule headings use ATX syntax: "### rule: ${attemptedId}".`
    96	    : `Line "${lineText}" looks like a rule declaration but is not a heading — declare it as an ATX heading: "### rule: ${attemptedId}".`;
    97	  return { message, attemptedId };
    98	}

 succeeded in 0ms:
     1	/**
     2	 * Shared types for the design-language spec convention (Phase 2).
     3	 *
     4	 * A design-language spec is a HAND-AUTHORABLE markdown artifact — the visual
     5	 * *letter* reference of the design-control discipline (the lo-fi wireframe is
     6	 * the UX *spirit*). The schema is allowlist-shaped like the rest of this
     7	 * plugin: a closed kind vocabulary, a closed field-key set (typos surface as
     8	 * findings, never silently drop), and per-rule structural requirements —
     9	 * ≥1 live-CSS link, ≥1 example reference, ≥1 do/don't guidance line.
    10	 *
    11	 * Extracted so the pure-text schema axis (`schema.ts`) and the fs-backed
    12	 * link-liveness axis (`link-liveness.ts`) share one taxonomy without a cycle
    13	 * (mirrors `@/lint/types`).
    14	 */
    15	
    16	/**
    17	 * Closed vocabulary of rule kinds, single-sourced as a `const ... as const`
    18	 * array (mirroring `ENGINE_METHODS` / `FAILURE_MODES`): palette / type /
    19	 * spacing tokens + the signature-component vocabulary.
    20	 */
    21	export const RULE_KINDS = ['palette', 'type', 'spacing', 'component'] as const;
    22	
    23	export type DesignRuleKind = (typeof RULE_KINDS)[number];
    24	
    25	/**
    26	 * A rule's link to live CSS: a path to an author-written CSS file (relative to
    27	 * the spec file) plus the selector the rule is anchored to. The selector may be
    28	 * multi-token (descendant combinators); the path is the first whitespace-free
    29	 * token of the `css:` field value. Machine-rooted paths (absolute, drive-letter,
    30	 * UNC, `~`-prefixed) never appear here — the schema rejects them as
    31	 * `malformed-css-link` (portability: the spec travels with its collection);
    32	 * `../` traversal within the repository is allowed.
    33	 */
    34	export interface CssLink {
    35	  readonly path: string;
    36	  readonly selector: string;
    37	}
    38	
    39	/** One structurally-valid rule parsed out of a design-language spec. */
    40	export interface DesignSpecRule {
    41	  /** The id from the `### rule: <id>` heading. */
    42	  readonly id: string;
    43	  /** Kind from the closed {@link RULE_KINDS} vocabulary. */
    44	  readonly kind: DesignRuleKind;
    45	  /** ≥1 live-CSS link (the link-liveness axis verifies each). */
    46	  readonly cssLinks: readonly CssLink[];
    47	  /** ≥1 example reference (structural presence only; does not establish example truthfulness). */
    48	  readonly examples: readonly string[];
    49	  /** `do:` guidance lines. */
    50	  readonly dos: readonly string[];
    51	  /** `don't:` guidance lines. */
    52	  readonly donts: readonly string[];
    53	}
    54	
    55	/** Finding taxonomy across both axes (schema structure + link-liveness). */
    56	export type DesignSpecFindingRule =
    57	  // axis A — markdown schema structure
    58	  | 'no-rules'
    59	  | 'malformed-rule-heading'
    60	  | 'duplicate-rule-id'
    61	  | 'missing-kind'
    62	  | 'unknown-kind'
    63	  | 'missing-css-link'
    64	  | 'malformed-css-link'
    65	  | 'missing-example'
    66	  | 'missing-guidance'
    67	  | 'unknown-field'
    68	  | 'empty-field'
    69	  // axis B — static link-liveness against author-written CSS source
    70	  | 'dead-link-file'
    71	  | 'dead-link-selector';
    72	
    73	export interface DesignSpecFinding {
    74	  readonly rule: DesignSpecFindingRule;
    75	  readonly message: string;
    76	  /** The spec rule the finding is about, when rule-scoped. */
    77	  readonly ruleId?: string;
    78	  /** 1-based markdown source line, when known. */
    79	  readonly line?: number;
    80	}
    81	
    82	/** The parsed spec: structurally-valid rules only (invalid rules are findings). */
    83	export interface ParsedDesignSpec {
    84	  readonly rules: readonly DesignSpecRule[];
    85	}
    86	
    87	/** A css link paired with the id of the spec section that declared it. */
    88	export interface RuleScopedCssLink {
    89	  readonly ruleId: string;
    90	  readonly link: CssLink;
    91	}
    92	
    93	export interface DesignSpecParseResult {
    94	  /** True iff findings is empty. */
    95	  readonly ok: boolean;
    96	  readonly spec: ParsedDesignSpec;
    97	  readonly findings: readonly DesignSpecFinding[];
    98	  /**
    99	   * Syntactically-usable css links housed in sections EXCLUDED from
   100	   * `spec.rules` (structurally-invalid rules, duplicate-id sections, and
   101	   * malformed-rule-heading declaration attempts). They still feed the
   102	   * link-liveness axis so a schema defect and a dead link on the same section
   103	   * surface in ONE checker run, not in waves — downstream consumers of
   104	   * `spec.rules` never see the invalid housing.
   105	   */
   106	  readonly auxiliaryCssLinks: readonly RuleScopedCssLink[];
   107	}

exec
/bin/zsh -lc "nl -ba plugins/design-control/bin/check-design-spec | sed -n '1,80p'" in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
     1	#!/bin/sh
     2	# check-design-spec <design-language-spec.md> — validate a design-language spec:
     3	# markdown schema (closed kinds, css links, example-presence, do/don't) + static
     4	# link-liveness against author-written CSS (no app boot). Exit codes: 0 green,
     5	# 1 findings/error, 2 usage. Logic lives in src/design-language/check-spec-file.ts
     6	# (tested); this shim only locates the workspace tsx runner and dispatches.
     7	set -eu
     8	
     9	PLUGIN_ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
    10	
    11	dir="$PLUGIN_ROOT"
    12	TSX=""
    13	while [ "$dir" != "/" ]; do
    14	  if [ -x "$dir/node_modules/.bin/tsx" ]; then
    15	    TSX="$dir/node_modules/.bin/tsx"
    16	    break
    17	  fi
    18	  dir=$(dirname -- "$dir")
    19	done
    20	
    21	if [ -z "$TSX" ]; then
    22	  echo "check-design-spec: tsx runner not found in any node_modules/.bin above $PLUGIN_ROOT — run npm install first" >&2
    23	  exit 1
    24	fi
    25	
    26	# --tsconfig pins the plugin's own tsconfig (and its @/* alias); without it tsx
    27	# resolves from the caller's cwd and the documented repo-root invocation crashes.
    28	exec "$TSX" --tsconfig "$PLUGIN_ROOT/tsconfig.json" "$PLUGIN_ROOT/src/design-language/check-design-spec-cli.ts" "$@"

codex
I’ve finished the first pass over the new parser, liveness checker, shims, and type contract. I’m probing a few edge cases now so any finding I emit is based on observable behavior, not just a suspicious line.
exec
/bin/zsh -lc "node --input-type=module -e \"import { parseDesignSpec } from './plugins/design-control/dist/nonexistent.js'\"" in /Users/orion/work/deskwork-work/design-control
 exited 1 in 0ms:
node:internal/modules/esm/resolve:274
    throw new ERR_MODULE_NOT_FOUND(
          ^

Error [ERR_MODULE_NOT_FOUND]: Cannot find module '/Users/orion/work/deskwork-work/design-control/plugins/design-control/dist/nonexistent.js' imported from /Users/orion/work/deskwork-work/design-control/[eval1]
    at finalizeResolution (node:internal/modules/esm/resolve:274:11)
    at moduleResolve (node:internal/modules/esm/resolve:859:10)
    at defaultResolve (node:internal/modules/esm/resolve:983:11)
    at #cachedDefaultResolve (node:internal/modules/esm/loader:717:20)
    at ModuleLoader.resolve (node:internal/modules/esm/loader:694:38)
    at ModuleLoader.getModuleJobForImport (node:internal/modules/esm/loader:308:38)
    at ModuleJob._link (node:internal/modules/esm/module_job:183:49) {
  code: 'ERR_MODULE_NOT_FOUND',
  url: 'file:///Users/orion/work/deskwork-work/design-control/plugins/design-control/dist/nonexistent.js'
}

Node.js v22.19.0

codex
One quick import probe used the wrong target path, so I’m switching to the repo’s own `tsx` runner and temporary fixtures for behavior checks.
exec
/bin/zsh -lc 'ls plugins/design-control/node_modules/.bin/tsx node_modules/.bin/tsx 2>/dev/null' in /Users/orion/work/deskwork-work/design-control
 exited 1 in 0ms:
node_modules/.bin/tsx

exec
/bin/zsh -lc "sed -n '1,220p' plugins/design-control/package.json" in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
{
  "name": "@deskwork/plugin-design-control",
  "version": "0.37.0",
  "private": true,
  "description": "Workspace package for the design-control Claude Code plugin",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit && vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "parse5": "^7.3.0",
    "tsx": "^4.21.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}

exec
/bin/zsh -lc "find . -maxdepth 3 -name package.json -print | sed -n '1,20p'" in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
./plugins/stack-control/package.json
./plugins/deskwork-studio/package.json
./plugins/design-control/package.json
./plugins/dw-lifecycle/package.json
./plugins/deskwork/package.json
./node_modules/queue-microtask/package.json
./node_modules/is-plain-obj/package.json
./node_modules/tinyglobby/package.json
./node_modules/hast-util-heading-rank/package.json
./node_modules/constantinople/package.json
./node_modules/zod/package.json
./node_modules/reusify/package.json
./node_modules/zwitch/package.json
./node_modules/obug/package.json
./node_modules/mdurl/package.json
./node_modules/tldts/package.json
./node_modules/pug-lexer/package.json
./node_modules/jstransformer/package.json
./node_modules/micromark-core-commonmark/package.json
./node_modules/micromark-util-sanitize-uri/package.json

codex
The changed command shims are using the repo-root runner layout correctly in this checkout. I’m checking parser/liveness outcomes with real fixture files next, because that’s where false-green cases tend to hide.
codex
`tsx` tried to create its IPC socket under the macOS per-user temp directory and the sandbox denied it. I’m rerunning with `TMPDIR=/private/tmp`, which is one of the writable roots here.
codex
The behavior probe is blocked by the sandbox’s refusal to let `tsx` open its local IPC pipe, so I’m not going to claim runtime confirmation from that path. The source-level evidence is still precise enough for at least one parser finding.
exec
/bin/zsh -lc 'rg -n "empty selector|malformed-css-link|non-CSS|skipped|css:" plugins/design-control/src/__tests__/design-language plugins/design-control/skills/translate-design-language/SKILL.md' in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
plugins/design-control/skills/translate-design-language/SKILL.md:34:- css: styles/studio.css .btn-primary
plugins/design-control/skills/translate-design-language/SKILL.md:42:- `css: <path> <selector>` — ≥1 per rule; the path is relative to the spec
plugins/design-control/skills/translate-design-language/SKILL.md:94:     a skipped link is visible scope, not silent coverage.
plugins/design-control/src/__tests__/design-language/check-design-spec-shim.test.ts:53:- css: studio.css .btn-primary
plugins/design-control/src/__tests__/design-language/check-design-spec-shim.test.ts:72:- css: studio.css .ghost
plugins/design-control/src/__tests__/design-language/link-liveness.test.ts:4: * Each rule's `css: <path> <selector>` link must resolve to an author-written
plugins/design-control/src/__tests__/design-language/link-liveness.test.ts:11: * recorded as skipped, visibly, never silently dropped and never fabricated
plugins/design-control/src/__tests__/design-language/link-liveness.test.ts:430:  it('a rule following a skipped @keyframes block is still live — the skip is exact', () => {
plugins/design-control/src/__tests__/design-language/link-liveness.test.ts:472:describe('checkLinkLiveness — validated-scope boundary (non-CSS targets, visible)', () => {
plugins/design-control/src/__tests__/design-language/link-liveness.test.ts:473:  it('records a non-.css target as skipped — no finding, never silent', () => {
plugins/design-control/src/__tests__/design-language/link-liveness.test.ts:479:    expect(result.skipped).toHaveLength(1);
plugins/design-control/src/__tests__/design-language/link-liveness.test.ts:480:    expect(result.skipped[0]).toMatchObject({
plugins/design-control/src/__tests__/design-language/schema.test.ts:6: * with bullet fields (`kind:` / `css:` / `example:` / `do:` / `don't:`). The
plugins/design-control/src/__tests__/design-language/schema.test.ts:25:- css: styles/studio.css .btn-primary
plugins/design-control/src/__tests__/design-language/schema.test.ts:34:- css: styles/studio.css .masthead
plugins/design-control/src/__tests__/design-language/schema.test.ts:35:- css: styles/chrome.css .masthead-rule
plugins/design-control/src/__tests__/design-language/schema.test.ts:74:- css: styles/studio.css .masthead nav a
plugins/design-control/src/__tests__/design-language/schema.test.ts:88:- css: styles/studio.css .stack
plugins/design-control/src/__tests__/design-language/schema.test.ts:109:- css: styles/studio.css .btn-primary
plugins/design-control/src/__tests__/design-language/schema.test.ts:121:- css: styles/studio.css .btn-primary
plugins/design-control/src/__tests__/design-language/schema.test.ts:138:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:149:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:170:- css: styles/studio.css
plugins/design-control/src/__tests__/design-language/schema.test.ts:174:    ).toContain('malformed-css-link');
plugins/design-control/src/__tests__/design-language/schema.test.ts:181:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:191:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:197:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:211:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:222:- css: styles/studio.css .masthead
plugins/design-control/src/__tests__/design-language/schema.test.ts:244:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:256:- css: styles/studio.css .masthead
plugins/design-control/src/__tests__/design-language/schema.test.ts:262:- css: styles/studio.css .serif
plugins/design-control/src/__tests__/design-language/schema.test.ts:274:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:287:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:301:- css: styles/studio.css .serif
plugins/design-control/src/__tests__/design-language/schema.test.ts:313:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:326:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:340:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:355:- css: styles/studio.css
plugins/design-control/src/__tests__/design-language/schema.test.ts:362:    expect(rules).toContain('malformed-css-link');
plugins/design-control/src/__tests__/design-language/schema.test.ts:369:- css: styles/studio.css .ghost
plugins/design-control/src/__tests__/design-language/schema.test.ts:383:    // The duplicate carries only `css:` — missing-kind/example/guidance would
plugins/design-control/src/__tests__/design-language/schema.test.ts:389:- css: styles/studio.css .ghost
plugins/design-control/src/__tests__/design-language/schema.test.ts:400:- css: styles/studio.css .ghost
plugins/design-control/src/__tests__/design-language/schema.test.ts:417:- css: /Users/alice/project/styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:422:    const finding = result.findings.find((f) => f.rule === 'malformed-css-link');
plugins/design-control/src/__tests__/design-language/schema.test.ts:431:- css: C:\\styles\\studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:435:    expect(findings).toContain('malformed-css-link');
plugins/design-control/src/__tests__/design-language/schema.test.ts:441:- css: C:/styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:445:    expect(findings).toContain('malformed-css-link');
plugins/design-control/src/__tests__/design-language/schema.test.ts:454:- css: C:styles.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:458:    expect(findings).toContain('malformed-css-link');
plugins/design-control/src/__tests__/design-language/schema.test.ts:464:- css: c:styles.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:468:    expect(findings).toContain('malformed-css-link');
plugins/design-control/src/__tests__/design-language/schema.test.ts:474:- css: \\\\server\\share\\studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:478:    expect(findings).toContain('malformed-css-link');
plugins/design-control/src/__tests__/design-language/schema.test.ts:484:- css: ~/project/styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:488:    expect(findings).toContain('malformed-css-link');
plugins/design-control/src/__tests__/design-language/schema.test.ts:494:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:499:- css: /Users/alice/project/styles/studio.css .ghost
plugins/design-control/src/__tests__/design-language/schema.test.ts:503:    expect(rules).toContain('malformed-css-link');
plugins/design-control/src/__tests__/design-language/schema.test.ts:510:- css: ../shared/styles.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:525:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:537:- css: styles/phantom.css .ghost
plugins/design-control/src/__tests__/design-language/schema.test.ts:597:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:623:- css: styles/studio.css .btn-primary
plugins/design-control/src/__tests__/design-language/schema.test.ts:636:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:642:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:652:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:664:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:681:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:692:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:704:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:731:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:738:- css: styles/studio.css .ghost
plugins/design-control/src/__tests__/design-language/schema.test.ts:760:- css: styles/studio.css .ghost
plugins/design-control/src/__tests__/design-language/schema.test.ts:773:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/schema.test.ts:790:- css: styles/studio.css .btn
plugins/design-control/src/__tests__/design-language/check-spec-file.test.ts:49:- css: studio.css .btn-primary
plugins/design-control/src/__tests__/design-language/check-spec-file.test.ts:78:- css: studio.css .real
plugins/design-control/src/__tests__/design-language/check-spec-file.test.ts:83:- css: studio.css .ghost
plugins/design-control/src/__tests__/design-language/check-spec-file.test.ts:107:- css: studio.css .ghost
plugins/design-control/src/__tests__/design-language/check-spec-file.test.ts:126:- css: studio.css .real
plugins/design-control/src/__tests__/design-language/check-spec-file.test.ts:132:- css: studio.css .ghost
plugins/design-control/src/__tests__/design-language/check-spec-file.test.ts:144:  it('records a non-css auxiliary link as skipped, never silently dropped', () => {
plugins/design-control/src/__tests__/design-language/check-spec-file.test.ts:152:- css: styles.ts .btn
plugins/design-control/src/__tests__/design-language/check-spec-file.test.ts:158:    expect(result.skipped).toHaveLength(1);
plugins/design-control/src/__tests__/design-language/check-spec-file.test.ts:159:    expect(result.skipped[0]).toMatchObject({
plugins/design-control/src/__tests__/design-language/check-spec-file.test.ts:182:- css: studio.css .ghost
plugins/design-control/src/__tests__/design-language/check-spec-file.test.ts:204:  it('reports skipped non-css targets visibly while staying green', () => {
plugins/design-control/src/__tests__/design-language/check-spec-file.test.ts:212:- css: styles.ts .btn

exec
/bin/zsh -lc 'rg -n "fix later|follow[- ]?up|address later|later|defer|deferred|future|not yet|TODO|production-ready" plugins/design-control/src plugins/design-control/skills/translate-design-language plugins/design-control/bin plugins/design-control/specs/001-design-control plugins/design-control/ROADMAP.md' in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
plugins/design-control/ROADMAP.md:32:- deferred-until: GATED evidence-spike: built last, after the v1-scaffold ships (Phases 1-4); advisory until its adversarial falsification set passes
plugins/design-control/ROADMAP.md:42:- deferred-until: post-v1 operator decision (captured out-of-v1 scope: styleguide gallery, design-barrage, WebKit/iOS, waypoint auto-fire, lint profiles, a11y review, broader pixel regression, studio-design migration)
plugins/design-control/specs/001-design-control/audit-log.md:11:- If work is deferred, change `Status:` to `acknowledged-<ref>` with the issue, workplan section, or operator-approved plan.
plugins/design-control/specs/001-design-control/audit-log.md:15:- `open` — reported; not yet resolved
plugins/design-control/specs/001-design-control/audit-log.md:16:- `acknowledged-<ref>` — accepted but deferred
plugins/design-control/specs/001-design-control/audit-log.md:49:- 1e deferral language removed from types.ts/preflight.ts/index.ts comments; grep over engine-adapter/*.ts for deferral words returns zero hits.
plugins/design-control/specs/001-design-control/audit-log.md:258:Disposition override: slush-merged under -01; cross-model agreement (two independent models flagged it) = high-confidence. claude framed it as the axis-1 boundary for tasks 6–7; codex as a MEDIUM the rule under-delivers on. Closed now (cheap) rather than deferred, per scope-don't-defer.
plugins/design-control/specs/001-design-control/audit-log.md:280:The round-1 fix narrowed value-shape scanning by wrapping the whole block (control-char, data:, script:, external-resource) in `if (attr === 'href')`. That made `RESOURCE_URL_ATTRS` (a per-tag `Record`) effectively dead — its branch can only fire when `attr === 'href'` — and rested the data:/scheme narrowing on the implicit, unenforced assumption that href is the only URL-bearing allowed attr. If a future task added a non-href URL attr (`img src`, `source srcset`, `a ping`), its value would pass completely unscanned, silently weakening the "lint green ⇒ lo-fi" guarantee with no signal.
plugins/design-control/specs/001-design-control/audit-log.md:315:Disposition (present-tense invariant, not a defer — reworded per AUDIT-20260606-09/codex-02): the `URL_ATTRS` doc comment now states a COMPLETE invariant: every URL-bearing attribute in the allowlist is a member of `URL_ATTRS`, so its values are scanned. That invariant holds today and is non-vacuous — the allowlist's only URL attr is `href`, which is present in `URL_ATTRS`. The RESOURCE direction is test-enforced (`RESOURCE_URL_ATTRS ⊆ URL_ATTRS`); the non-resource direction (`a ping`, `form action`, `q cite`) is vacuously satisfied because no such attr is in the allowlist. Machine-checking the non-resource direction (deriving `URL_ATTRS` from URL-tagged allowlist entries) is tracked as filed enhancement **#428** — not promised in a code comment. There is no outstanding code change while the invariant holds. (Reworded under AUDIT-20260606-14, then -17, to replace the future-patch promise and then the residual scheduling language with a bare #428 reference.)
plugins/design-control/specs/001-design-control/audit-log.md:319:This matters because AUDIT-04's own stated motivation names `a ping` as a non-href URL attr to worry about — and `ping` is a navigation-beacon attribute, *not* resource-loading, so a future dev would add it to `TAG_ATTRS['a']` (allowlist) without touching `RESOURCE_URL_ATTRS`. Its value would then pass completely unscanned for `data:`/`javascript:`/control-char schemes, and **no test would fail**. Same for `form action`, `blockquote/q cite`, `area href`. The fix's audit-log claim ("adding a resource attr without scheme coverage now fails a test instead of shipping a latent gap") is true but narrower than AUDIT-04's framing implies — the general "URL attr" coupling it set out to close is only closed for the resource-loading subset.
plugins/design-control/specs/001-design-control/audit-log.md:374:### AUDIT-20260606-09 — diff contained explicit deferred-work wording in the audit log and source comment
plugins/design-control/specs/001-design-control/audit-log.md:381:Disposition override: slush-pile; a real (LOW) operator-discipline catch — my AUDIT-07 disposition + `URL_ATTRS` comment used deferral phrasing ("parked," "warranted the moment," "becomes actionable"), which `.claude/rules/agent-discipline.md` § "Just for now is bullshit" treats as bug-factory language. Fixed in this commit — both reworded to a present-tense, complete invariant ("every URL-bearing allowlist attr is in `URL_ATTRS`"; the non-resource direction is vacuously satisfied today) with the enforcement clause stated as part of the invariant, no deferral framing.
plugins/design-control/specs/001-design-control/audit-log.md:383:The hard constraints for this audit say to surface deferral phrases in the diff. The new audit-log disposition said the structural half "is parked with a CONCRETE TRIGGER," and the `URL_ATTRS` comment said the robust fix "is warranted the moment the allowlist grows such an attr." Those were documented deferrals, even though carefully scoped — now reworded to a present-tense invariant + enforcement clause.
plugins/design-control/specs/001-design-control/audit-log.md:404:### AUDIT-20260606-14 — Reworded URL_ATTRS "invariant" still encodes a triggered future structural fix with no tracking artifact
plugins/design-control/specs/001-design-control/audit-log.md:411:Disposition override: slush-pile; a real cross-model LOW — the AUDIT-09 reword still left a future-structural-change promise in the comment ("the patch that first adds… replaces this hand-maintained coupling"), the IOU-in-a-comment shape the "Just for now is bullshit" rule names. Properly terminated in this commit: filed the structural-derivation work as #428, dropped the IOU sentence, and the comment now states the present invariant + a bare `tracked in #428` reference (a pointer, not a promise). The AUDIT-07 disposition below is likewise updated to reference #428.
plugins/design-control/specs/001-design-control/audit-log.md:413:The AUDIT-20260606-09 rework replaced the earlier "parked with a CONCRETE TRIGGER" language, but the substance of the deferral survives the rewrite. allowlist.ts lines 77-81: *"The patch that first adds a non-resource URL attr to the allowlist adds it here in the same change AND **replaces this hand-maintained coupling with URL-tagged allowlist entries** that derive this set."* That clause describes the robust structural fix AUDIT-07 actually asked for (derive `URL_ATTRS` from URL-tagged allowlist entries so `allowlist → scanning` is machine-checkable) as conditional future work, gated on a trigger, recorded only in a code comment — no GitHub issue, no workplan task. The same shape is in the audit-log disposition.
plugins/design-control/specs/001-design-control/audit-log.md:415:Per the project's own `.claude/rules/agent-discipline.md` § "Just for now is bullshit", a deferral must terminate in one of: addressed now, filed as an issue with link, scoped into the workplan, or an explicit operator decision with acceptance criteria — and "a code comment promising future work" is explicitly named as the failure mode, not a valid disposition. The hard constraints for this audit also direct surfacing deferral phrasing found in the diff. The rewording is genuinely better (the *present* state is described as a complete-while-vacuous invariant, which is accurate), so the latent coupling itself is correctly characterized — but the structural fix is still IOU'd in a comment rather than tracked. I flag it acknowledging it was already triaged once under AUDIT-09; the recommendation is narrow: either file the structural-derivation work as an issue and reference the number in the comment, or drop the "the patch that first adds… replaces this hand-maintained coupling" sentence entirely (the vacuous-today invariant stands on its own without promising the future refactor).
plugins/design-control/specs/001-design-control/audit-log.md:441:The two new tests cover exactly one stronger-algo shape (`sha384-other <pin>` → rejected) and one same-algo shape (`sha256-decoyAAAA <pin>` → accepted). The `hasStrongerAlgo` regex matches `sha512-` too (line 151), but nothing exercises it — a regression that dropped `512` from the alternation would pass the suite while silently letting a sha512-overriding integrity through the membership branch. Likewise there is no test for the stronger-algo-*only* case (`integrity="sha384-foo"` with no sha256 token at all), which routes through the `hasStrongerAlgo` branch and produces the "overrides the pinned" message rather than the "does not assert the pinned" message — the two message branches at 156-158 are a behavior contract and only one is asserted. Add: (a) a sha512-token rejection case, and (b) a stronger-algo-only case asserting `stylesheet-sri-mismatch`. This is hygiene, not a live bug — the implementation is correct for these inputs today; the gap is that the test suite wouldn't catch a future narrowing of the regex or a swap of the two message branches.
plugins/design-control/specs/001-design-control/audit-log.md:443:I checked the security-critical direction (no false-negative / no path that greenlights an unenforced pin), the `#428` reference (issue exists and is OPEN — disposition grounded), the regex for backtracking/anchoring issues (clean), the empty/garbage-integrity edge cases (all flagged correctly), and the audit-log rewordings for residual deferral phrasing (the AUDIT-07 reword and the allowlist comment now terminate in the `#428` pointer, no IOU). Those are clean; the two findings above are the only ones worth surfacing.
plugins/design-control/specs/001-design-control/audit-log.md:445:### AUDIT-20260606-17 — URL_ATTRS comment still contains conditional future-work wording
plugins/design-control/specs/001-design-control/audit-log.md:454:The diff removes the untracked promise shape, but the replacement still says the machine-checking work is “tracked in #428 — to be done when the allowlist first gains a non-resource URL attr.” That is still conditional future-work wording in a code comment, and the audit prompt’s hard constraint explicitly says to surface deferral phrasing found in the diff.
plugins/design-control/specs/001-design-control/audit-log.md:527:Disposition: WON'T-DO (accepted observation, no change planned — reworded per AUDIT-20260606-26 to drop deferral phrasing). Position-less findings are the established, consistent pattern across this entire lint; cross-node duplicate `disallowed-codepoint` messages match that pattern and are neither a regression nor a correctness bug. No code change. Original auditor note follows.
plugins/design-control/specs/001-design-control/audit-log.md:560:This is **not a correctness bug** — the behavior is now consistent between NFC and NFD input (NFD `Ǎ` and precomposed `Ǎ` both fail because U+01CD is genuinely off-allowlist), which is the correct invariant. I'm flagging it only because the docstring reads as an unconditional "NFD accented Latin now passes" claim, and a future reader could mistake a legitimately-off-allowlist NFD failure for a regression of this fix. A one-clause qualifier ("…for accents whose precomposed form is in the allowlisted ranges") would make the boundary explicit and prevent that misread.
plugins/design-control/specs/001-design-control/audit-log.md:562:### AUDIT-20260606-26 — AUDIT-20260606-23 disposition uses soft-deferral phrasing ("not done now" / "optional future enhancement") without an issue link
plugins/design-control/specs/001-design-control/audit-log.md:569:Disposition override: slush-pile; a real cross-model LOW — my AUDIT-23 disposition reintroduced the deferral phrasing ("not done now", "optional future enhancement") that the project's "Just for now is bullshit" rule forbids and that I'd been closing elsewhere this session. Fixed in this commit — the AUDIT-23 disposition is reworded to a definitive WON'T-DO ("no change planned"; position-less findings are the established pattern), no scheduling or speculative-work language.
plugins/design-control/specs/001-design-control/audit-log.md:571:The new AUDIT-23 disposition records an informational observation and twice frames the remediation as deferred-but-coming: "Global per-document message dedup or positional context is an optional future enhancement at the `checkText`/walk boundary; **not done now**" (line 521) and "that is a small enhancement at the `checkText`/walk boundary" (line 526). Per the project's own discipline (`.claude/rules/agent-discipline.md` § "Just for now is bullshit") and the prior triage note at the top of this audit-log that explicitly flagged conditional future-work wording ("to be done when…"), "not done now" reads as the same soft-IOU smell — it implies the work *will* happen without any tracking surface to ensure it does.
plugins/design-control/specs/001-design-control/audit-log.md:573:The audit prompt's hard constraint also directs surfacing deferral phrasing found in the diff. An informational "no code change" disposition is legitimate, but the wording should commit one way or the other: either file an issue and reference it (`tracked in #NNN`), or state it as an accepted observation that is *not* planned ("position-less findings are the established pattern for this lint; no change planned"). Rewriting to drop "not done now" / "future enhancement" and adopting won't-do/issue-linked framing keeps the record from reintroducing the exact operator-discipline smell the surrounding entries have been closing.
plugins/design-control/specs/001-design-control/audit-log.md:1408:- Two of the loop's own earlier dispositions were REVERSED on later evidence
plugins/design-control/specs/001-design-control/audit-log.md:1421:  restored fleet is the natural post-447 follow-up.
plugins/design-control/specs/001-design-control/audit-log.md:1431:### AUDIT-20260611-01 — Provenance mode can be silently rewritten — a later `recordDrivingWireframe` call overwrites a `derived` record and launders the exact claim the module exists to prevent
plugins/design-control/specs/001-design-control/audit-log.md:1438:`writeProvenance` unconditionally `writeFileSync`s `<surfaceId>.provenance.json`. Nothing in `recordDrivingWireframe` or `recordDerivation` checks whether a sidecar already exists, so a surface recorded as `derived` (with its snapshot + hash baseline) can be flipped to `driving` with a single later call — after which `wireframeDroveImplementation` returns `true` and `checkDerivedAcceptance` passes unconditionally (lines 144-146 short-circuit on non-derived mode). The module's own header (lines 15-17) states the design goal: *"provenance distinguishes the two modes precisely so the claim cannot be laundered through acceptance."* The overwrite path is a one-call laundering vector that bypasses acceptance entirely. The derivation-time snapshot is also silently orphaned (the `.derived-snapshot.html` stays on disk but nothing references it), erasing the audit trail.
plugins/design-control/specs/001-design-control/audit-log.md:1473:Blast radius: a downstream consumer (the future referee, or any "did a wireframe drive this change?" check) gets a `true` that is unfalsifiable against the on-disk artifact. Nothing breaks today, but every claim recorded under this schema is permanently unverifiable, and a later schema fix can't retro-bind old records — the cost compounds with adoption, hence medium. Fix: record the wireframe's filename and sha256 at `recordDrivingWireframe` time (the wireframe file already exists at that point per SKILL.md step ordering — lint at step 5 precedes provenance at step 6), and have `wireframeDroveImplementation` (or a sibling verifier) check the hash the way `checkDerivedAcceptance` checks the snapshot.
plugins/design-control/specs/001-design-control/audit-log.md:1484:Blast radius: no behavioral consequence — the value lands in `createdAt` correctly — but this is an exported public API (`@/provenance`), so the misleading name propagates to every future caller and to the eventual `bin/` verb's flag naming. Hence low. Fix: rename to `createdAt?` (or `at?`) in both record functions for symmetry, while the call-site count is still two test files.
plugins/design-control/specs/001-design-control/audit-log.md:1506:The blast radius is medium: normal consumers will usually succeed, but the failure mode creates ambiguous provenance state in the operator’s artifact directory. A later unattended agent or operator may see the snapshot and assume derivation was recorded, while `loadProvenance` fails loud because the sidecar is absent. A reasonable fix is to write both artifacts via temporary files and atomic renames, with cleanup on failure, or to write the sidecar first with a temporary snapshot reference and only promote both names once both writes have succeeded.
plugins/design-control/specs/001-design-control/audit-log.md:1519:Blast radius: medium. The governance discipline's value rests on run records being durable and discoverable; an uncommitted record cited by a commit message is a provenance gap in the provenance tooling itself, and the absolute-path diff rendering will mislead every future cross-model auditor fed this payload. Fix: commit the run directory (matching 9c0d556c's pattern), and make the diff-builder either exclude untracked files or render them with correct repo-relative prefixes.
plugins/design-control/specs/001-design-control/audit-log.md:1528:The run produced AUDIT-BARRAGE-codex-01 (`wireframeFile` can escape the provenance directory) and AUDIT-BARRAGE-codex-02 (`recordDerivation` can clobber an existing snapshot before the sidecar commit fails). A grep of `audit-log.md` finds no disposition for either. The run failed the ≥2-emitting-models floor, which legitimately refuses the *round* — but the findings exist, were captured to disk, and I have independently verified both defects are present in `src/provenance/derived.ts` as written (see claude-03 and claude-04 below, which are my independent confirmations). Per the project's scope-don't-defer rule, a captured finding with no disposition is precisely the parked-defect failure mode the audit-log exists to prevent; the floor refusal is being silently treated as if it also voided the findings.
plugins/design-control/specs/001-design-control/audit-log.md:1539:Commit 896be642 added `assertPortableSurfaceId` at every surfaceId path-building entry, with a zod-side defense on load. But `recordDrivingWireframe` then does `join(input.dir, input.wireframeFile)` (line 172) with `wireframeFile` constrained only to `z.string().min(1)` (line 75), and `verifyDrivingWireframe` re-joins the stored value at line 344. A `wireframeFile` of `../outside.html`, a nested `sub/file.html`, or a planted sidecar carrying a traversal path binds driving provenance to — and later "verifies" — an artifact outside the operator-chosen wireframes directory. SKILL.md (step 6) promises the filename is "relative to `<wireframes-dir>`" but nothing enforces it. This is the exact shape AUDIT-20260611-02 fixed for surfaceId, applied asymmetrically.
plugins/design-control/specs/001-design-control/audit-log.md:1583:The file seeded into the design-control nested installation opens with "project override for graphical-entries" and carries that feature's gemini-failure statistics (16 of 17 runs across "the graphical-entries Phase 0 audit cycle") and Phase 12 Task 8 history verbatim. Only the final comment block (claude 300→900, specs/014) belongs to this installation. A future reader tuning this config inherits another feature's rationale as if it were locally measured evidence.
plugins/design-control/specs/001-design-control/audit-log.md:1663:### AUDIT-20260611-21 — The audited diff introduces explicit deferral language into the skill and checker contract
plugins/design-control/specs/001-design-control/audit-log.md:1670:text in tasks.md and spec.md deliberately RETAINS the operator-approved "named-deferred"
plugins/design-control/specs/001-design-control/audit-log.md:1676:The diff repeatedly encodes “not validated in v1” / “named-deferred” / “out of v1 scope” language in the operator-facing skill, CLI output, and workplan. The audit prompt’s hard constraint rejects deferral phrases because they become bug-factory commitments in unattended workflows; here they are not just comments, they appear in the user-facing validation output (`check-spec-file.ts:68-71`) and in the skill’s instructions about what the operator may present (`SKILL.md:80-82`).
plugins/design-control/specs/001-design-control/audit-log.md:1678:Blast radius is low because the scope boundary is visible and intentional, and the code does not hide skipped links. The operational risk is documentation discipline: agents may normalize presenting partially unchecked specs as “green” because the deferral is built into the happy path. A reasonable fix is to replace temporal deferral phrasing with a stable capability statement, such as “non-CSS targets are reported as unchecked notes and do not establish link-liveness.”
plugins/design-control/specs/001-design-control/audit-log.md:1689:The AUDIT-15 fix added a heading-level typo guard, but the guard lives entirely inside the ATX-heading branch — a markdown **setext** heading (`rule: beta` underlined with `---` or `===`) renders as a real heading to the author yet never matches `HEADING_RE`. Verified empirically: a spec with valid `### rule: alpha` followed by setext `rule: beta\n----------` and beta's field bullets returns `ok: true, findings: []` with ONE rule — alpha — now carrying **beta's css link and example merged into it** (`css: [.alpha, .beta]`, `examples: 2`). This is worse than a plain silent drop: the intended rule disappears with zero findings AND the surviving rule's parsed content is corrupted, so a dead `.beta` selector would later be misattributed to alpha, and a live one yields a fully green verdict (`spec green — 0 findings (1 rule(s))`) for a spec the author believes declares two rules. This is exactly the silent-green direction the module's headline guarantee ("never silently kept or dropped") and the just-landed near-miss guard exist to kill — one heading syntax over.
plugins/design-control/specs/001-design-control/audit-log.md:1724:Blast radius: low — a visible exit-1 false refusal, and the realistic authoring path (copying the selector verbatim from the CSS file) sidesteps it; it bites when the author types the selector from memory or the CSS is later reformatted by a tool that changes quote style (Prettier normalizes to double quotes — a formatting commit would flip previously-green links to dead). Fix: normalize attribute-value delimiters on both sides (e.g. rewrite `['"]?` content-stripped values to a canonical empty `""`, treating `[attr=]`, `[attr=""]`, `[attr='']` identically), pinned by a RED test per quote-style pair.
plugins/design-control/specs/001-design-control/audit-log.md:1726:### AUDIT-20260611-26 — AUDIT-21 is dispositioned "fixed" but one of its cited surfaces — tasks.md — still carries the temporal deferral phrasing
plugins/design-control/specs/001-design-control/audit-log.md:1731:Surface:    plugins/design-control/specs/001-design-control/tasks.md:176-191 ("is **not validated in v1** (named-deferred)", "Runtime dead-CSS + spec-truthfulness are named-deferred", "visible v1 scope"); audit-log disposition for AUDIT-20260611-21
plugins/design-control/specs/001-design-control/audit-log.md:1733:Commit 03ae9291 replaced temporal deferral phrasing with capability statements in SKILL.md and the CLI output (verified in the final-state diff: "reported as unchecked notes and do not establish link-liveness", "outside its validated scope" — clean), and the audit log records AUDIT-21 as `fixed-03ae9291`. But the original codex finding explicitly anchored three surfaces, including `tasks.md:176-191`, and the workplan's task text retains "not validated in v1 (named-deferred)" and "named-deferred" verbatim in the final state. The workplan is the artifact the next implementing agent reads to understand the gate's contract, so the bug-factory mechanism the finding named — agents normalizing "deferred" as the contract — survives on one of the three cited surfaces while the disposition claims all seven findings fixed.
plugins/design-control/specs/001-design-control/audit-log.md:1735:Blast radius: low — the two surfaces an operator actually touches at run time (skill body, CLI output) are fixed, and a workplan is partially version-bound history where naming v1 is defensible; the defect is disposition accuracy (a `fixed` status whose cited surface is unchanged), which matters in this project precisely because the audit log is the instrument future rounds use to decide what not to re-report. Fix: either reword the tasks.md scope sentences to the same capability framing, or amend the AUDIT-21 disposition to record the deliberate exclusion of tasks.md so the narrowed scope is auditable.
plugins/design-control/specs/001-design-control/audit-log.md:1772:Blast radius is low because the runtime behavior is visible and the CLI now uses stable capability language. The issue is documentation discipline: future agents reading the workplan or comments can reintroduce the same phrasing into operator-facing surfaces. A reasonable correction is to use capability wording consistently, matching the skill and CLI text.
plugins/design-control/specs/001-design-control/audit-log.md:1794:`parseDesignSpec` walks raw lines with no awareness of fenced (``` ```` ```) or indented code blocks, so markdown that *renders as an inert code example* is parsed as live spec structure. Verified empirically through the CLI: a spec with one real rule plus a fenced ```markdown example containing `### rule: phantom` (the SKILL.md-style authoring example, css link pointing at a live selector) reports **`spec green — 0 findings (2 rule(s))`** — the documentation example becomes a real parsed rule, silently. The same blindness drives the loud direction: an *indented* code line `rule: sample` inside an authoring example produces a false `malformed-rule-heading` (verified, exit 1). Note the trap is self-modeling: SKILL.md's own convention section teaches the format via exactly such a fenced example; an author who pastes that preamble into their spec for future maintainers gets either a phantom rule (if the example's selector happens to be live, as SKILL.md's `.btn-primary` would be in this very project) or spurious dead-link findings on documentation text.
plugins/design-control/src/lint/allowlist.ts:141: * value-shape checks on — NOT a hardcoded `'href'` literal — so a future
plugins/design-control/specs/001-design-control/spec.md:96:  gate. This replaces "single Claude shim now, cross-model later."
plugins/design-control/specs/001-design-control/spec.md:276:Named-deferred (see Out of Scope): **runtime dead-CSS detection** and **spec-truthfulness**
plugins/design-control/specs/001-design-control/spec.md:316:The schema **also defines the fields later referee controls need** (baseline + candidate paths,
plugins/design-control/specs/001-design-control/spec.md:344:  regions (that is `spec-truthfulness`, named-deferred).
plugins/design-control/specs/001-design-control/spec.md:419:      *matches* live UI is `spec-truthfulness`, named-deferred). The spec is **hand-authorable** —
plugins/design-control/specs/001-design-control/spec.md:476:## Out of Scope (v1 — captured, named-deferred; not hidden)
plugins/design-control/specs/001-design-control/spec.md:479:  deferral.** Pixel regression, if ever needed, uses an **existing** tool (Playwright
plugins/design-control/specs/001-design-control/spec.md:489:  deferred.
plugins/design-control/specs/001-design-control/spec.md:493:  (validated on the deskwork studio). Resolving link-liveness for those stacks is deferred.
plugins/design-control/specs/001-design-control/spec.md:497:  only; a semantic-stability sampling check is named-deferred — NOT a Phase 5 obligation** (no
plugins/design-control/specs/001-design-control/spec.md:498:  dangling "resolve later" commitment).
plugins/design-control/specs/001-design-control/scope-inventory/runs/20260606T170123Z-ea21d3/editor-symmetry.md:3:Rows are adoption conventions declared in `.dw-lifecycle/scope-discovery/adopter-manifests.yaml`; columns are parallel top-level modules under `plugins/design-control/src/`. Cells show adoption status: `✓ N/N` = all files in the module matching the manifest glob import the canonical path; `⚠ A/E (H holdout(s))` = partial adoption with `H` files holding out; `✗` = the module was targeted by the glob but has zero matched files or zero adopters; `⏳ A/E (T tracked)` = adopter set has only tracked-holdouts (deferred-but-known migrations, each with a `tracked_holdouts:` entry naming the follow-up issue; gate-passing, NOT masked as ✓); `—` = the manifest does not target this module (n/a).
plugins/design-control/specs/001-design-control/scope-inventory/runs/20260606T170123Z-ea21d3/synthesis.md:12:Operator action: review the 17 discovered candidate cluster(s) + 0 novel-shape candidate finding(s) BEFORE treating this run as "all clear." A non-zero candidate count is the signal the catalog is not yet exhaustive.
plugins/design-control/src/__tests__/lint/check-mockup-lofi.test.ts:169:  // value-shape checks on URL_ATTRS. If a future task adds a resource attr to
plugins/design-control/src/lint/check-mockup-lofi.ts:378:    // hardcoded 'href' keeps coverage in lockstep with the allowlist so a future
plugins/design-control/specs/001-design-control/tooling-feedback.md:57:**Repro:** The barrage produced 9 structured findings (claude-01..06, codex-01..03). `audit-barrage-lift --feature design-control --run-dir <run-dir> --apply` collapsed them to 4 audit-log entries. AUDIT-20260605-01's `Finding-ID` line reads `(claude-01 + claude-03 + claude-04 + codex-01 + codex-03; cross-model)` — i.e. it merged FIVE distinct findings (EngineMethod single-sourcing, preflight remedy hardcoding the default adapter, the `[0,1]` confidence check duplicated three times, method/envelope type-binding, and deferral language in source comments) into one entry whose **body describes only the EngineMethod single-sourcing issue.** A fixer reading only AUDIT-01's body would fix one of five real defects and mark the entry `fixed`, silently dropping the other four.
plugins/design-control/specs/001-design-control/mockups/sketch-kit/DECISION.md:35:## Consequences captured (not deferred)
plugins/design-control/src/provenance/derived.ts:21: * `derived` record can never be silently flipped to `driving` by a later
plugins/design-control/src/provenance/derived.ts:185: * writing fails loud. Without this, a later `recordDrivingWireframe` call
plugins/design-control/src/provenance/derived.ts:238: * so a later wholesale replacement of the wireframe is tamper-evident
plugins/design-control/specs/001-design-control/tasks.md:130:      cross-model re-validation is the natural follow-up once 447 lands.
plugins/design-control/specs/001-design-control/tasks.md:178:      / hashed CSS-Modules resolution is **not validated in v1** (named-deferred). Runtime dead-CSS +
plugins/design-control/specs/001-design-control/tasks.md:179:      spec-truthfulness are named-deferred.
plugins/design-control/specs/001-design-control/tasks.md:191:      live UI is `spec-truthfulness` (named-deferred).
plugins/design-control/specs/001-design-control/tasks.md:242:- [ ] **Schema also DEFINES the later referee-control fields** — baseline+candidate paths,
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191707Z-9fe621/augmented-prd.md:96:  gate. This replaces "single Claude shim now, cross-model later."
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191707Z-9fe621/augmented-prd.md:276:Named-deferred (see Out of Scope): **runtime dead-CSS detection** and **spec-truthfulness**
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191707Z-9fe621/augmented-prd.md:316:The schema **also defines the fields later referee controls need** (baseline + candidate paths,
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191707Z-9fe621/augmented-prd.md:344:  regions (that is `spec-truthfulness`, named-deferred).
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191707Z-9fe621/augmented-prd.md:419:      *matches* live UI is `spec-truthfulness`, named-deferred). The spec is **hand-authorable** —
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191707Z-9fe621/augmented-prd.md:476:## Out of Scope (v1 — captured, named-deferred; not hidden)
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191707Z-9fe621/augmented-prd.md:479:  deferral.** Pixel regression, if ever needed, uses an **existing** tool (Playwright
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191707Z-9fe621/augmented-prd.md:489:  deferred.
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191707Z-9fe621/augmented-prd.md:493:  (validated on the deskwork studio). Resolving link-liveness for those stacks is deferred.
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191707Z-9fe621/augmented-prd.md:497:  only; a semantic-stability sampling check is named-deferred — NOT a Phase 5 obligation** (no
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191707Z-9fe621/augmented-prd.md:498:  dangling "resolve later" commitment).
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191855Z-eb67eb/augmented-prd.md:96:  gate. This replaces "single Claude shim now, cross-model later."
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191855Z-eb67eb/augmented-prd.md:276:Named-deferred (see Out of Scope): **runtime dead-CSS detection** and **spec-truthfulness**
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191855Z-eb67eb/augmented-prd.md:316:The schema **also defines the fields later referee controls need** (baseline + candidate paths,
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191855Z-eb67eb/augmented-prd.md:344:  regions (that is `spec-truthfulness`, named-deferred).
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191855Z-eb67eb/augmented-prd.md:419:      *matches* live UI is `spec-truthfulness`, named-deferred). The spec is **hand-authorable** —
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191855Z-eb67eb/augmented-prd.md:476:## Out of Scope (v1 — captured, named-deferred; not hidden)
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191855Z-eb67eb/augmented-prd.md:479:  deferral.** Pixel regression, if ever needed, uses an **existing** tool (Playwright
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191855Z-eb67eb/augmented-prd.md:489:  deferred.
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191855Z-eb67eb/augmented-prd.md:493:  (validated on the deskwork studio). Resolving link-liveness for those stacks is deferred.
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191855Z-eb67eb/augmented-prd.md:497:  only; a semantic-stability sampling check is named-deferred — NOT a Phase 5 obligation** (no
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191855Z-eb67eb/augmented-prd.md:498:  dangling "resolve later" commitment).
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191806Z-b0dbba/augmented-prd.md:96:  gate. This replaces "single Claude shim now, cross-model later."
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191806Z-b0dbba/augmented-prd.md:276:Named-deferred (see Out of Scope): **runtime dead-CSS detection** and **spec-truthfulness**
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191806Z-b0dbba/augmented-prd.md:316:The schema **also defines the fields later referee controls need** (baseline + candidate paths,
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191806Z-b0dbba/augmented-prd.md:344:  regions (that is `spec-truthfulness`, named-deferred).
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191806Z-b0dbba/augmented-prd.md:419:      *matches* live UI is `spec-truthfulness`, named-deferred). The spec is **hand-authorable** —
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191806Z-b0dbba/augmented-prd.md:476:## Out of Scope (v1 — captured, named-deferred; not hidden)
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191806Z-b0dbba/augmented-prd.md:479:  deferral.** Pixel regression, if ever needed, uses an **existing** tool (Playwright
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191806Z-b0dbba/augmented-prd.md:489:  deferred.
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191806Z-b0dbba/augmented-prd.md:493:  (validated on the deskwork studio). Resolving link-liveness for those stacks is deferred.
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191806Z-b0dbba/augmented-prd.md:497:  only; a semantic-stability sampling check is named-deferred — NOT a Phase 5 obligation** (no
plugins/design-control/specs/001-design-control/scope-inventory/widen-runs/20260610T191806Z-b0dbba/augmented-prd.md:498:  dangling "resolve later" commitment).

exec
/bin/zsh -lc "git diff --unified=30 0391a0c0..HEAD -- plugins/design-control/src/design-language/schema.ts plugins/design-control/src/design-language/link-liveness.ts plugins/design-control/src/design-language/check-spec-file.ts | sed -n '1,260p'" in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
diff --git a/plugins/design-control/src/design-language/check-spec-file.ts b/plugins/design-control/src/design-language/check-spec-file.ts
new file mode 100644
index 00000000..19e071a8
--- /dev/null
+++ b/plugins/design-control/src/design-language/check-spec-file.ts
@@ -0,0 +1,96 @@
+/**
+ * File-level entry to the design-language spec check — the enforcement seam
+ * the `/design-control:translate-design-language` skill (and the
+ * `bin/check-design-spec` shim) route EVERY spec draft through, hand-authored
+ * or engine-accelerated alike. Mirrors `@/authoring/lint-file` for wireframes:
+ * a thin composition of the existing axes, no parallel validation path.
+ */
+
+import { readFileSync } from 'node:fs';
+import { dirname, resolve } from 'node:path';
+import {
+  checkCssLinkLiveness,
+  checkLinkLiveness,
+  type SkippedLink,
+} from '@/design-language/link-liveness';
+import { parseDesignSpec } from '@/design-language/schema';
+import type { DesignSpecFinding, ParsedDesignSpec } from '@/design-language/types';
+
+export interface DesignSpecCheckResult {
+  /** True iff findings is empty (skipped links stay green but visible). */
+  readonly ok: boolean;
+  readonly spec: ParsedDesignSpec;
+  /** Schema findings followed by link-liveness findings. */
+  readonly findings: readonly DesignSpecFinding[];
+  readonly skipped: readonly SkippedLink[];
+}
+
+/**
+ * Check a design-language spec FILE: read it, validate the markdown schema,
+ * then check link-liveness with css paths resolved against the spec file's
+ * own directory. Liveness runs over the valid rules AND the parse result's
+ * auxiliary css links (links housed in structurally-invalid / duplicate
+ * sections), so a schema defect and a dead link on the same section surface
+ * in ONE run — never in fix-and-rerun waves. Fails loud on an unreadable
+ * file — a missing spec is an error, never a clean verdict.
+ */
+export function checkDesignSpecFile(filePath: string): DesignSpecCheckResult {
+  const absolute = resolve(filePath);
+  const markdown = readFileSync(absolute, 'utf8');
+  const parsed = parseDesignSpec(markdown);
+  const baseDir = dirname(absolute);
+  const liveness = checkLinkLiveness(parsed.spec, baseDir);
+  const auxiliary = checkCssLinkLiveness(parsed.auxiliaryCssLinks, baseDir);
+  const findings = [...parsed.findings, ...liveness.findings, ...auxiliary.findings];
+  return {
+    ok: findings.length === 0,
+    spec: parsed.spec,
+    findings,
+    skipped: [...liveness.skipped, ...auxiliary.skipped],
+  };
+}
+
+/** Line-oriented output sink, injected so the CLI core is testable. */
+export interface CliIo {
+  out(line: string): void;
+  err(line: string): void;
+}
+
+const USAGE = 'usage: check-design-spec <design-language-spec.md>';
+
+/**
+ * CLI core behind `bin/check-design-spec`. Exit-code contract (the skill's
+ * gate, same shape as check-wireframe):
+ *   0 — spec green (zero findings; skipped links are reported but green)
+ *   1 — findings present, or the file could not be read (descriptive error;
+ *       never a fabricated verdict)
+ *   2 — usage error
+ */
+export function runCheckDesignSpec(argv: readonly string[], io: CliIo): number {
+  if (argv.length !== 1) {
+    io.err(USAGE);
+    return 2;
+  }
+  const filePath = argv[0];
+  let result: DesignSpecCheckResult;
+  try {
+    result = checkDesignSpecFile(filePath);
+  } catch (error) {
+    io.err(error instanceof Error ? error.message : String(error));
+    return 1;
+  }
+  for (const skip of result.skipped) {
+    io.out(
+      `note: rule "${skip.ruleId}" link "${skip.link.path} ${skip.link.selector}" is a non-CSS target — reported unchecked, does not establish link-liveness (this check validates selectors in author-written .css sources only; CSS-in-JS / utility-framework / CSS-Modules targets are outside its validated scope).`,
+    );
+  }
+  if (result.ok) {
+    io.out(`${filePath}: spec green — 0 findings (${result.spec.rules.length} rule(s))`);
+    return 0;
+  }
+  for (const finding of result.findings) {
+    io.err(`${finding.rule}${finding.ruleId ? ` (rule: ${finding.ruleId})` : ''}: ${finding.message}`);
+  }
+  io.err(`${filePath}: ${result.findings.length} finding(s)`);
+  return 1;
+}
diff --git a/plugins/design-control/src/design-language/link-liveness.ts b/plugins/design-control/src/design-language/link-liveness.ts
new file mode 100644
index 00000000..3e6b0983
--- /dev/null
+++ b/plugins/design-control/src/design-language/link-liveness.ts
@@ -0,0 +1,295 @@
+/**
+ * Static link-liveness for design-language spec rules (Phase 2, axis B).
+ *
+ * Each rule's `css: <path> <selector>` link must point at an author-written
+ * CSS file in which the selector is DEFINED. The check is STATIC — pure file
+ * reads against source, no app boot, no engine (round-6 M1: "authoring
+ * artifacts only / no capture dependency").
+ *
+ * Scope: this check validates selectors in author-written `.css` sources
+ * only. Utility-framework, CSS-in-JS, and hashed CSS-Modules links do
+ * not establish link-liveness — they are recorded as `skipped` (visible in
+ * the result and in CLI output), never silently dropped and never fabricated
+ * into a dead-link verdict. Liveness ≠ truthfulness: a resolving selector
+ * does not establish that the live CSS still matches the rule's described
+ * intent — `spec-truthfulness` is a separate axis this check does not
+ * perform (scope decision recorded in specs/001-design-control/spec.md).
+ *
+ * "Defined in source" is implemented as: the selector appears, ident-boundary
+ * exact, inside some selector prelude of the file — preludes are the text
+ * runs that precede `{` after comments are stripped and at-rule preludes are
+ * excluded (their blocks are descended into, so a rule inside `@media` counts;
+ * `content: ".ghost"` and commented-out rules do not). Both sides are
+ * canonicalized before comparison (AUDIT-round2-codex-01 / -claude-04):
+ * attribute values rewrite to one double-quoted spelling with the value text
+ * PRESERVED, so `input[type=text]` matches `input[type="text"]` while
+ * `[data-state="open"]` never matches `[data-state="closed"]`; functional
+ * pseudo-class arguments (`:not(.ghost)`, `:is(...)`, `:where(...)`,
+ * `:has(...)`, `:nth-child(...)`) are compared when the QUERY names them and
+ * excluded from the scan when it does not — a class that exists only as an
+ * exclusion, matcher argument, or attribute value has no styling of its own,
+ * so it must not count as a live anchor for a query that never named it.
+ * Remaining accepted approximations are stated in `selector-canon.ts`
+ * (verbatim escapes; collapsed-not-erased argument whitespace).
+ */
+
+import { readFileSync } from 'node:fs';
+import { resolve } from 'node:path';
+import { isNonPortableCssPath } from '@/design-language/schema';
+import {
+  blankAttributeNames,
+  blankAttributeValues,
+  canonicalizeAttributeSelectors,
+  copyStringLiteral,
+  isIdentChar,
+  normalizeSelectorWhitespace,
+  stripComments,
+  stripFunctionalPseudoArgs,
+} from '@/design-language/selector-canon';
+import type {
+  CssLink,
+  DesignSpecFinding,
+  ParsedDesignSpec,
+  RuleScopedCssLink,
+} from '@/design-language/types';
+
+/** A link outside the validated .css scope, recorded visibly. */
+export interface SkippedLink {
+  readonly ruleId: string;
+  readonly link: CssLink;
+  readonly reason: 'non-css-target';
+}
+
+export interface LivenessResult {
+  /** True iff findings is empty (skipped links do not fail the check). */
+  readonly ok: boolean;
+  readonly findings: readonly DesignSpecFinding[];
+  readonly skipped: readonly SkippedLink[];
+}
+
+/**
+ * `@keyframes` at-rule names, vendor-prefix tolerant (`@-webkit-keyframes`,
+ * `@-moz-keyframes`, …). Their blocks are skipped wholesale by
+ * {@link collectSelectorPreludes}.
+ */
+const KEYFRAMES_AT_RULE = /^@(?:-[a-z]+-)?keyframes\b/i;
+
+/**
+ * Return the index just past the `}` matching the `{` at `css[open]`,
+ * balancing nested braces and skipping comments and string literals (a brace
+ * inside either is content, not structure). Unclosed input runs to the end.
+ */
+function skipBlock(css: string, open: number): number {
+  let depth = 1;
+  let i = open + 1;
+  while (i < css.length && depth > 0) {
+    const ch = css[i];
+    if (ch === '/' && css[i + 1] === '*') {
+      const end = css.indexOf('*/', i + 2);
+      i = end === -1 ? css.length : end + 2;
+      continue;
+    }
+    if (ch === '"' || ch === "'") {
+      i = copyStringLiteral(css, i).end;
+      continue;
+    }
+    if (ch === '{') {
+      depth += 1;
+    } else if (ch === '}') {
+      depth -= 1;
+    }
+    i += 1;
+  }
+  return i;
+}
+
+/**
+ * Collect selector preludes: text runs preceding `{`, at every nesting depth,
+ * excluding at-rule preludes (which are descended into, not matched against)
+ * — with one descent exception: `@keyframes` blocks (vendor-prefixed forms
+ * included) are skipped WHOLE, contributing no preludes, because their step
+ * "selectors" (`from` / `to` / percentages) are animation waypoints, not
+ * element selectors a design rule can anchor to (AUDIT-round4-claude-04).
+ * Preludes are matched FLAT, not ancestor-composed (AUDIT-round4-claude-02):
+ * a nested source `.btn { .icon {} }` contributes the preludes `.btn` and
+ * `.icon` — never the composed `.btn .icon` — and `&:hover` stays `&:hover`,
+ * never `.btn:hover`; on nested sources anchor rules to the leaf selector.
+ * Comments are stripped; string-literal CONTENTS are copied verbatim (their
+ * structural characters are content, never braces/semicolons) so attribute
+ * values survive into preludes for canonical comparison. Prelude buffers
+ * reset on `{`, `}`, and `;` so declaration text (`content: ".ghost"`) never
+ * leaks into selector position.
+ */
+function collectSelectorPreludes(css: string): string[] {
+  const preludes: string[] = [];
+  let buffer = '';
+  let i = 0;
+  while (i < css.length) {
+    const ch = css[i];
+    if (ch === '/' && css[i + 1] === '*') {
+      const end = css.indexOf('*/', i + 2);
+      i = end === -1 ? css.length : end + 2;
+      continue;
+    }
+    if (ch === '"' || ch === "'") {
+      const literal = copyStringLiteral(css, i);
+      buffer += literal.copied;
+      i = literal.end;
+      continue;
+    }
+    if (ch === '{') {
+      const prelude = buffer.trim();
+      buffer = '';
+      if (KEYFRAMES_AT_RULE.test(prelude)) {
+        i = skipBlock(css, i);
+        continue;
+      }
+      if (prelude !== '' && !prelude.startsWith('@')) {
+        preludes.push(prelude);
+      }
+    } else if (ch === '}' || ch === ';') {
+      buffer = '';
+    } else {

codex
I also scanned for banned deferral wording. The hits are either historical audit-log text or already-dispositioned workplan/spec scope language, so I’m not treating those as fresh findings.
exec
/bin/zsh -lc "nl -ba plugins/design-control/skills/translate-design-language/SKILL.md | sed -n '1,140p'" in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
     1	---
     2	name: translate-design-language
     3	description: Draft or maintain the project's design-language spec — the hand-authorable markdown artifact that anchors visual identity (palette/type/spacing tokens + signature components, each rule linked to live CSS + ≥1 example). Hand-authoring is the default and needs NO engine; the optional /frontend-design accelerator drafts from approved wireframe intent and is judged by the same check-design-spec gate.
     4	---
     5	
     6	# /design-control:translate-design-language
     7	
     8	Author or update the **design-language spec** for this project. The spec is the
     9	visual-*letter* artifact of the design-control discipline: the durable home for
    10	visual identity (the lo-fi wireframe carries UX *spirit* and is structurally
    11	incapable of carrying visual detail). Every rule binds to reality — a live CSS
    12	file + selector, ≥1 current example — so the spec cannot quietly drift into
    13	fiction the way a mockup's incidental polish does.
    14	
    15	> Per the plugin thesis (`DESIGN-DISCIPLINE-THESIS.md`): policy is enforced by
    16	> a process, not a rule. "Each rule links to live CSS" is not a convention the
    17	> author is trusted to follow — it is mechanically enforced by the
    18	> `check-design-spec` gate (schema + static link-liveness), which every draft
    19	> MUST pass before it may be presented.
    20	
    21	## Spec convention (hand-authorable markdown)
    22	
    23	One markdown file (conventionally `design-language.md` in the operator's chosen
    24	design docs directory). Rules are declared under ATX headings, fields are
    25	bullets with a closed key set:
    26	
    27	```markdown
    28	# Design language: <project>
    29	
    30	## Palette
    31	
    32	### rule: ink-primary
    33	- kind: palette
    34	- css: styles/studio.css .btn-primary
    35	- example: dashboard compose button uses .btn-primary
    36	- do: Use the ink palette for every primary action.
    37	- don't: Never introduce raw hex blues outside the palette tokens.
    38	```
    39	
    40	- `kind:` — one of `palette` / `type` / `spacing` / `component` (the closed
    41	  vocabulary; `component` is the signature-component class).
    42	- `css: <path> <selector>` — ≥1 per rule; the path is relative to the spec
    43	  file (absolute or `~`-rooted paths are rejected as malformed — they only
    44	  resolve on the author's machine; `../` traversal within the repository is
    45	  fine); the selector must be **defined in that author-written CSS source**
    46	  (checked statically — no app boot). Non-CSS targets (CSS-in-JS, utility
    47	  frameworks, CSS-Modules) are reported as unchecked notes and do not
    48	  establish link-liveness. For nested CSS rules link the leaf selector —
    49	  composed selectors like `.btn .icon` written via nesting do not match
    50	  (preludes are checked flat, not ancestor-composed).
    51	- `example:` — ≥1 per rule (a rule with zero examples is rejected). Presence
    52	  is structural; whether the example still matches live UI is
    53	  `spec-truthfulness`, a separate concern this gate does not check.
    54	- `do:` / `don't:` — ≥1 guidance line per rule.
    55	
    56	Headings (or line-initial paragraphs / setext headings) that look like rule
    57	declarations but miss the strict ATX `rule: <id>` form (`Rule: x`, `rule : x`,
    58	`rule x`) are flagged as `malformed-rule-heading`; prose headings starting
    59	with "Rule" (e.g. `## Rule of thumb`) are fine. Fenced and indented code
    60	blocks are inert — an authoring example like the one above never parses as a
    61	live rule.
    62	
    63	## Procedure
    64	
    65	1. **Locate or create the spec file.** One spec per design language; do not
    66	   fork per-surface copies. If the operator has no spec yet, scaffold the
    67	   heading + one rule per obvious anchor (masthead, primary action, body type)
    68	   directly from the live CSS — with the operator naming the files that count
    69	   as design-language source.
    70	
    71	2. **Author or update rules — manual path (default, requires NO engine).** The
    72	   operator (or the agent under operator direction) writes the rules by hand:
    73	   pick the selector in live CSS the rule is anchored to, cite ≥1 current
    74	   example, state the do/don't. Scaffold completion never depends on engine
    75	   presence — this path never calls the engine preflight.
    76	
    77	3. **Optional engine accelerator.** Only if the operator asks for it: gate on
    78	   `preflightEngine` (`@/engine-adapter`, method `translate-design-language`)
    79	   — absence fails loud naming the remedy — then request a draft from the
    80	   engine (input: the approved wireframe intent + the live CSS files the
    81	   operator names). **Engine output gets zero trust:** it lands in the same
    82	   file and is judged by the same gate as a hand-authored draft. Engine
    83	   conformance (`@/engine-adapter/conformance`) is exercised only when the
    84	   engine is present — never stub it to simulate presence.
    85	
    86	4. **Validation gate — the non-negotiable step.** Run:
    87	
    88	   ```bash
    89	   plugins/design-control/bin/check-design-spec <path/to/design-language.md>
    90	   ```
    91	
    92	   - Exit `0` (spec green, zero findings) → the draft may be presented.
    93	     Read any `does not establish link-liveness` notes aloud to the operator —
    94	     a skipped link is visible scope, not silent coverage.
    95	   - Exit `1` → fix every finding and re-run. A dead selector means either the
    96	     rule rots (fix the link) or the CSS moved (update the rule) — NEVER
    97	     delete the rule just to silence the finding; that decision is the
    98	     operator's.
    99	
   100	5. **Present and stop.** Show the operator the green spec (path + `0 findings`
   101	   output + rule count). The operator owns acceptance; implementation against
   102	   the spec and refereeing are separate steps of the loop, not this skill's
   103	   job.
   104	
   105	## What this skill does NOT do
   106	
   107	- It does not author wireframes (`/design-control:wireframe`), implement, or
   108	  referee.
   109	- It does not boot the app, capture screenshots, or verify that examples still
   110	  match live UI (that is `spec-truthfulness`, which no step of this skill
   111	  performs).
   112	- It does not skip the gate for engine-authored drafts — same gate, same
   113	  checker, zero findings.

exec
/bin/zsh -lc "nl -ba plugins/design-control/src/__tests__/design-language/schema.test.ts | sed -n '150,190p'" in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
   150	- example: a button
   151	- do: x
   152	`),
   153	    ).toContain('unknown-kind');
   154	  });
   155	
   156	  it('flags a rule with no css link (every rule links to live CSS)', () => {
   157	    expect(
   158	      findingsFor(`### rule: ink
   159	- kind: palette
   160	- example: a button
   161	- do: x
   162	`),
   163	    ).toContain('missing-css-link');
   164	  });
   165	
   166	  it('flags a css link without a selector as malformed', () => {
   167	    expect(
   168	      findingsFor(`### rule: ink
   169	- kind: palette
   170	- css: styles/studio.css
   171	- example: a button
   172	- do: x
   173	`),
   174	    ).toContain('malformed-css-link');
   175	  });
   176	
   177	  it('flags a rule with neither do nor don’t guidance', () => {
   178	    expect(
   179	      findingsFor(`### rule: ink
   180	- kind: palette
   181	- css: styles/studio.css .btn
   182	- example: a button
   183	`),
   184	    ).toContain('missing-guidance');
   185	  });
   186	
   187	  it('flags duplicate rule ids', () => {
   188	    expect(
   189	      findingsFor(`### rule: ink
   190	- kind: palette

exec
/bin/zsh -lc "nl -ba plugins/design-control/src/__tests__/design-language/check-spec-file.test.ts | sed -n '190,225p'" in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
   190	  });
   191	
   192	  it('exit 1 + descriptive error on an unreadable file', () => {
   193	    const { err, io } = captureIo();
   194	    expect(runCheckDesignSpec([join(makeFixtureDir(), 'absent.md')], io)).toBe(1);
   195	    expect(err.length).toBeGreaterThan(0);
   196	  });
   197	
   198	  it('exit 2 on usage error', () => {
   199	    const { err, io } = captureIo();
   200	    expect(runCheckDesignSpec([], io)).toBe(2);
   201	    expect(err.join('\n')).toContain('usage');
   202	  });
   203	
   204	  it('reports skipped non-css targets visibly while staying green', () => {
   205	    const dir = makeFixtureDir();
   206	    writeFileSync(join(dir, 'styles.ts'), 'export const x = 1;\n');
   207	    const specPath = join(dir, 'design-language.md');
   208	    writeFileSync(
   209	      specPath,
   210	      `### rule: css-in-js
   211	- kind: component
   212	- css: styles.ts .btn
   213	- example: somewhere
   214	- do: x
   215	`,
   216	    );
   217	    const { out, io } = captureIo();
   218	    expect(runCheckDesignSpec([specPath], io)).toBe(0);
   219	    expect(out.join('\n')).toContain('does not establish link-liveness');
   220	  });
   221	});

codex
The tests pin `css: styles/studio.css` as malformed, but not the same field with trailing spaces after the path. The parser split currently treats that as a link with `selector: ""`, which is the gap I’m planning to report.
exec
/bin/zsh -lc "nl -ba plugins/design-control/src/__tests__/design-language/link-liveness.test.ts | sed -n '1,520p'" in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
     1	/**
     2	 * Static link-liveness tests (Phase 2, axis B).
     3	 *
     4	 * Each rule's `css: <path> <selector>` link must resolve to an author-written
     5	 * CSS file in which the selector is DEFINED — checked statically against
     6	 * source with NO app boot (the check is pure file reads; the acceptance's
     7	 * "flags a dead selector with no app boot — engine absent" lands here).
     8	 *
     9	 * Scope is author-written CSS only: non-.css targets (CSS-in-JS, hashed
    10	 * CSS-Modules, utility frameworks) do not establish link-liveness — they are
    11	 * recorded as skipped, visibly, never silently dropped and never fabricated
    12	 * into a dead-link finding.
    13	 *
    14	 * Real-fs temp fixtures per .claude/rules/testing.md — never mock the fs.
    15	 */
    16	
    17	import { mkdtempSync, mkdirSync, rmSync, writeFileSync } from 'node:fs';
    18	import { tmpdir } from 'node:os';
    19	import { join } from 'node:path';
    20	import { afterEach, describe, expect, it } from 'vitest';
    21	import { checkCssLinkLiveness, checkLinkLiveness } from '@/design-language/link-liveness';
    22	import type { ParsedDesignSpec } from '@/design-language/types';
    23	
    24	const tempDirs: string[] = [];
    25	
    26	function makeFixtureDir(): string {
    27	  const dir = mkdtempSync(join(tmpdir(), 'design-language-liveness-'));
    28	  tempDirs.push(dir);
    29	  return dir;
    30	}
    31	
    32	afterEach(() => {
    33	  while (tempDirs.length > 0) {
    34	    rmSync(tempDirs.pop() as string, { recursive: true, force: true });
    35	  }
    36	});
    37	
    38	function specWithLink(path: string, selector: string): ParsedDesignSpec {
    39	  return {
    40	    rules: [
    41	      {
    42	        id: 'probe',
    43	        kind: 'palette',
    44	        cssLinks: [{ path, selector }],
    45	        examples: ['an example'],
    46	        dos: ['guidance'],
    47	        donts: [],
    48	      },
    49	    ],
    50	  };
    51	}
    52	
    53	describe('checkLinkLiveness — live selectors pass', () => {
    54	  it('passes a class selector defined in the referenced file', () => {
    55	    const dir = makeFixtureDir();
    56	    writeFileSync(join(dir, 'studio.css'), '.btn-primary { color: navy; }\n');
    57	    const result = checkLinkLiveness(specWithLink('studio.css', '.btn-primary'), dir);
    58	    expect(result.findings).toEqual([]);
    59	    expect(result.ok).toBe(true);
    60	  });
    61	
    62	  it('finds a selector defined inside an @media block', () => {
    63	    const dir = makeFixtureDir();
    64	    writeFileSync(
    65	      join(dir, 'studio.css'),
    66	      '@media (min-width: 80rem) {\n  .desktop-rail { width: 16rem; }\n}\n',
    67	    );
    68	    expect(checkLinkLiveness(specWithLink('studio.css', '.desktop-rail'), dir).ok).toBe(true);
    69	  });
    70	
    71	  it('finds a selector that appears with a pseudo-class in source', () => {
    72	    const dir = makeFixtureDir();
    73	    writeFileSync(join(dir, 'studio.css'), '.chip:hover { outline: 1px solid; }\n');
    74	    expect(checkLinkLiveness(specWithLink('studio.css', '.chip'), dir).ok).toBe(true);
    75	  });
    76	
    77	  it('finds a descendant selector sequence regardless of whitespace', () => {
    78	    const dir = makeFixtureDir();
    79	    writeFileSync(join(dir, 'studio.css'), '.masthead   nav a { text-decoration: none; }\n');
    80	    expect(checkLinkLiveness(specWithLink('studio.css', '.masthead nav a'), dir).ok).toBe(true);
    81	  });
    82	
    83	  it('finds a quoted attribute selector — input[type="text"] is live, not fabricated dead', () => {
    84	    const dir = makeFixtureDir();
    85	    writeFileSync(join(dir, 'studio.css'), 'input[type="text"] { border: 1px solid; }\n');
    86	    const result = checkLinkLiveness(specWithLink('studio.css', 'input[type="text"]'), dir);
    87	    expect(result.findings).toEqual([]);
    88	    expect(result.ok).toBe(true);
    89	  });
    90	
    91	  it('finds a class + quoted attribute selector — .chip[data-state="open"] is live', () => {
    92	    const dir = makeFixtureDir();
    93	    writeFileSync(join(dir, 'studio.css'), '.chip[data-state="open"] { outline: 2px solid; }\n');
    94	    expect(
    95	      checkLinkLiveness(specWithLink('studio.css', '.chip[data-state="open"]'), dir).ok,
    96	    ).toBe(true);
    97	  });
    98	
    99	  it('resolves the css path relative to the spec base dir, including subdirectories', () => {
   100	    const dir = makeFixtureDir();
   101	    mkdirSync(join(dir, 'styles'));
   102	    writeFileSync(join(dir, 'styles', 'chrome.css'), '.masthead-rule { border-top: 2px solid; }\n');
   103	    expect(checkLinkLiveness(specWithLink('styles/chrome.css', '.masthead-rule'), dir).ok).toBe(true);
   104	  });
   105	
   106	  it('resolves parent traversal — a subdir spec referencing ../ css is live', () => {
   107	    // Deliberate (AUDIT-round2-codex-02): `../` stays accepted — a spec
   108	    // legitimately references author CSS elsewhere in the repository tree;
   109	    // the portability boundary is the repository/collection, not the spec's
   110	    // own directory.
   111	    const dir = makeFixtureDir();
   112	    mkdirSync(join(dir, 'specs'));
   113	    writeFileSync(join(dir, 'shared.css'), '.btn-primary { color: navy; }\n');
   114	    const result = checkLinkLiveness(specWithLink('../shared.css', '.btn-primary'), join(dir, 'specs'));
   115	    expect(result.findings).toEqual([]);
   116	    expect(result.ok).toBe(true);
   117	  });
   118	});
   119	
   120	describe('checkCssLinkLiveness — public-seam portability defense (AUDIT-round2-codex-02)', () => {
   121	  it('throws on a machine-rooted link path instead of fabricating a machine-local verdict', () => {
   122	    // The path EXISTS on this machine — exactly the trap: resolve() would
   123	    // ignore baseDir and the check would go green here while the spec is
   124	    // nonportable. The schema never emits such a link; a hand-built one at
   125	    // this public seam is a caller contract violation → fail loud.
   126	    const dir = makeFixtureDir();
   127	    writeFileSync(join(dir, 'studio.css'), '.btn { color: navy; }\n');
   128	    expect(() =>
   129	      checkCssLinkLiveness(
   130	        [{ ruleId: 'probe', link: { path: join(dir, 'studio.css'), selector: '.btn' } }],
   131	        dir,
   132	      ),
   133	    ).toThrow(/relative to the spec file/);
   134	  });
   135	
   136	  it('throws on a ~-prefixed link path too (home expansion is machine-local)', () => {
   137	    const dir = makeFixtureDir();
   138	    expect(() =>
   139	      checkCssLinkLiveness(
   140	        [{ ruleId: 'probe', link: { path: '~/styles/studio.css', selector: '.btn' } }],
   141	        dir,
   142	      ),
   143	    ).toThrow(/relative to the spec file/);
   144	  });
   145	
   146	  it('throws on a drive-RELATIVE Windows link path (C:styles.css — machine-contextual on Windows)', () => {
   147	    const dir = makeFixtureDir();
   148	    expect(() =>
   149	      checkCssLinkLiveness(
   150	        [{ ruleId: 'probe', link: { path: 'C:styles.css', selector: '.btn' } }],
   151	        dir,
   152	      ),
   153	    ).toThrow(/relative to the spec file/);
   154	  });
   155	});
   156	
   157	describe('checkLinkLiveness — dead links flagged (no app boot)', () => {
   158	  it('flags a selector that is not defined anywhere in the file', () => {
   159	    const dir = makeFixtureDir();
   160	    writeFileSync(join(dir, 'studio.css'), '.btn-primary { color: navy; }\n');
   161	    const result = checkLinkLiveness(specWithLink('studio.css', '.btn-ghost'), dir);
   162	    expect(result.ok).toBe(false);
   163	    expect(result.findings.some((f) => f.rule === 'dead-link-selector' && f.ruleId === 'probe')).toBe(
   164	      true,
   165	    );
   166	  });
   167	
   168	  it('flags a missing css file', () => {
   169	    const dir = makeFixtureDir();
   170	    const result = checkLinkLiveness(specWithLink('nope.css', '.btn'), dir);
   171	    expect(result.findings.some((f) => f.rule === 'dead-link-file')).toBe(true);
   172	  });
   173	
   174	  it('does not let a longer ident satisfy a shorter selector (.btn vs .btn-primary)', () => {
   175	    const dir = makeFixtureDir();
   176	    writeFileSync(join(dir, 'studio.css'), '.btn-primary { color: navy; }\n');
   177	    expect(checkLinkLiveness(specWithLink('studio.css', '.btn'), dir).ok).toBe(false);
   178	  });
   179	
   180	  it('a selector appearing only in a comment is dead', () => {
   181	    const dir = makeFixtureDir();
   182	    writeFileSync(join(dir, 'studio.css'), '/* .ghost was retired */\n.real { color: ink; }\n');
   183	    expect(checkLinkLiveness(specWithLink('studio.css', '.ghost'), dir).ok).toBe(false);
   184	  });
   185	
   186	  it('a selector appearing only inside a declaration string is dead', () => {
   187	    const dir = makeFixtureDir();
   188	    writeFileSync(join(dir, 'studio.css'), '.real::before { content: ".ghost"; }\n');
   189	    expect(checkLinkLiveness(specWithLink('studio.css', '.ghost'), dir).ok).toBe(false);
   190	  });
   191	
   192	  it('a selector appearing only as a property value token is dead', () => {
   193	    const dir = makeFixtureDir();
   194	    writeFileSync(join(dir, 'studio.css'), '.real { background: url(.ghost/x.png); }\n');
   195	    expect(checkLinkLiveness(specWithLink('studio.css', '.ghost'), dir).ok).toBe(false);
   196	  });
   197	
   198	  it('a selector appearing only inside :not(...) is dead — exclusion is not styling', () => {
   199	    const dir = makeFixtureDir();
   200	    writeFileSync(join(dir, 'studio.css'), '.real:not(.ghost) { color: ink; }\n');
   201	    expect(checkLinkLiveness(specWithLink('studio.css', '.ghost'), dir).ok).toBe(false);
   202	  });
   203	
   204	  it('a selector appearing only inside :is(...) arguments is dead', () => {
   205	    const dir = makeFixtureDir();
   206	    writeFileSync(join(dir, 'studio.css'), '.real:is(.ghost, .other) { color: ink; }\n');
   207	    expect(checkLinkLiveness(specWithLink('studio.css', '.ghost'), dir).ok).toBe(false);
   208	  });
   209	
   210	  it('a selector nested inside :not(:is(...)) is dead — parens balance across nesting', () => {
   211	    const dir = makeFixtureDir();
   212	    writeFileSync(join(dir, 'studio.css'), '.real:not(:is(.ghost)) { color: ink; }\n');
   213	    expect(checkLinkLiveness(specWithLink('studio.css', '.ghost'), dir).ok).toBe(false);
   214	  });
   215	
   216	  it('the subject outside the functional pseudo-class still matches (.real in .real:not(.ghost))', () => {
   217	    const dir = makeFixtureDir();
   218	    writeFileSync(join(dir, 'studio.css'), '.real:not(.ghost) { color: ink; }\n');
   219	    expect(checkLinkLiveness(specWithLink('studio.css', '.real'), dir).ok).toBe(true);
   220	  });
   221	
   222	  it('a full-selector query with parens matches its source rule — argument text compared', () => {
   223	    const dir = makeFixtureDir();
   224	    writeFileSync(join(dir, 'studio.css'), '.real:not(.ghost) { color: ink; }\n');
   225	    expect(checkLinkLiveness(specWithLink('studio.css', '.real:not(.ghost)'), dir).ok).toBe(true);
   226	  });
   227	});
   228	
   229	describe('checkLinkLiveness — attribute values compared canonically (AUDIT-round2-codex-01 / -claude-04)', () => {
   230	  it('a [data-state="open"] query is dead against a source defining only [data-state="closed"]', () => {
   231	    const dir = makeFixtureDir();
   232	    writeFileSync(join(dir, 'studio.css'), '.chip[data-state="closed"] { outline: none; }\n');
   233	    const result = checkLinkLiveness(specWithLink('studio.css', '.chip[data-state="open"]'), dir);
   234	    expect(result.ok).toBe(false);
   235	    expect(result.findings.some((f) => f.rule === 'dead-link-selector')).toBe(true);
   236	  });
   237	
   238	  it('an unquoted query matches a double-quoted source — input[type=text] vs input[type="text"]', () => {
   239	    const dir = makeFixtureDir();
   240	    writeFileSync(join(dir, 'studio.css'), 'input[type="text"] { border: 1px solid; }\n');
   241	    expect(checkLinkLiveness(specWithLink('studio.css', 'input[type=text]'), dir).ok).toBe(true);
   242	  });
   243	
   244	  it('a double-quoted query matches an unquoted source — input[type="text"] vs input[type=text]', () => {
   245	    const dir = makeFixtureDir();
   246	    writeFileSync(join(dir, 'studio.css'), 'input[type=text] { border: 1px solid; }\n');
   247	    expect(checkLinkLiveness(specWithLink('studio.css', 'input[type="text"]'), dir).ok).toBe(true);
   248	  });
   249	
   250	  it('a single-quoted query matches a double-quoted source', () => {
   251	    const dir = makeFixtureDir();
   252	    writeFileSync(join(dir, 'studio.css'), 'input[type="text"] { border: 1px solid; }\n');
   253	    expect(checkLinkLiveness(specWithLink('studio.css', "input[type='text']"), dir).ok).toBe(true);
   254	  });
   255	
   256	  it('a double-quoted query matches a single-quoted source', () => {
   257	    const dir = makeFixtureDir();
   258	    writeFileSync(join(dir, 'studio.css'), ".chip[data-state='open'] { outline: 2px solid; }\n");
   259	    expect(
   260	      checkLinkLiveness(specWithLink('studio.css', '.chip[data-state="open"]'), dir).ok,
   261	    ).toBe(true);
   262	  });
   263	
   264	  it('a selector appearing only as a quoted attribute VALUE is dead — values are not definitions', () => {
   265	    const dir = makeFixtureDir();
   266	    writeFileSync(join(dir, 'studio.css'), '.real[data-icon=".ghost"] { color: ink; }\n');
   267	    expect(checkLinkLiveness(specWithLink('studio.css', '.ghost'), dir).ok).toBe(false);
   268	  });
   269	
   270	  it('a selector appearing only as an UNQUOTED attribute value is dead too', () => {
   271	    const dir = makeFixtureDir();
   272	    writeFileSync(join(dir, 'studio.css'), '.real[class~=ghost] { color: ink; }\n');
   273	    expect(checkLinkLiveness(specWithLink('studio.css', 'ghost'), dir).ok).toBe(false);
   274	  });
   275	});
   276	
   277	describe('checkLinkLiveness — functional pseudo-class arguments compared (AUDIT-round2-codex-01)', () => {
   278	  it('a .real:not(.ghost) query is dead against a source defining only .real:not(.other)', () => {
   279	    const dir = makeFixtureDir();
   280	    writeFileSync(join(dir, 'studio.css'), '.real:not(.other) { color: ink; }\n');
   281	    const result = checkLinkLiveness(specWithLink('studio.css', '.real:not(.ghost)'), dir);
   282	    expect(result.ok).toBe(false);
   283	    expect(result.findings.some((f) => f.rule === 'dead-link-selector')).toBe(true);
   284	  });
   285	
   286	  it('argument whitespace is normalized — .real:not(.ghost) matches a :not( .ghost ) source', () => {
   287	    const dir = makeFixtureDir();
   288	    writeFileSync(join(dir, 'studio.css'), '.real:not( .ghost ) { color: ink; }\n');
   289	    expect(checkLinkLiveness(specWithLink('studio.css', '.real:not(.ghost)'), dir).ok).toBe(true);
   290	  });
   291	
   292	  it('argument-list comma spacing is normalized — :is(.a,.b) matches :is(.a, .b)', () => {
   293	    const dir = makeFixtureDir();
   294	    writeFileSync(join(dir, 'studio.css'), '.real:is(.a, .b) { color: ink; }\n');
   295	    expect(checkLinkLiveness(specWithLink('studio.css', '.real:is(.a,.b)'), dir).ok).toBe(true);
   296	  });
   297	});
   298	
   299	describe('checkLinkLiveness — combinator spacing normalized (AUDIT-20260611 combinator finding)', () => {
   300	  it('a spaced child query matches an unspaced source — .a > .b vs .a>.b', () => {
   301	    const dir = makeFixtureDir();
   302	    writeFileSync(join(dir, 'studio.css'), '.a>.b { color: ink; }\n');
   303	    const result = checkLinkLiveness(specWithLink('studio.css', '.a > .b'), dir);
   304	    expect(result.findings).toEqual([]);
   305	    expect(result.ok).toBe(true);
   306	  });
   307	
   308	  it('an unspaced adjacent-sibling query matches a spaced source — .c+.d vs .c + .d', () => {
   309	    // Prettier writes spaced combinators, so a CSS reformat must not flip a
   310	    // green link dead.
   311	    const dir = makeFixtureDir();
   312	    writeFileSync(join(dir, 'studio.css'), '.c + .d { color: ink; }\n');
   313	    expect(checkLinkLiveness(specWithLink('studio.css', '.c+.d'), dir).ok).toBe(true);
   314	  });
   315	
   316	  it('a spaced general-sibling query matches an unspaced source — .x ~ .y vs .x~.y', () => {
   317	    const dir = makeFixtureDir();
   318	    writeFileSync(join(dir, 'studio.css'), '.x~.y { color: ink; }\n');
   319	    expect(checkLinkLiveness(specWithLink('studio.css', '.x ~ .y'), dir).ok).toBe(true);
   320	  });
   321	
   322	  it('an unspaced column-combinator query matches a spaced source — .col||.cell vs .col || .cell', () => {
   323	    const dir = makeFixtureDir();
   324	    writeFileSync(join(dir, 'studio.css'), '.col || .cell { width: 4rem; }\n');
   325	    expect(checkLinkLiveness(specWithLink('studio.css', '.col||.cell'), dir).ok).toBe(true);
   326	  });
   327	
   328	  it('combinator unification never fabricates a green — .a>.b is dead against a descendant .a .b source', () => {
   329	    const dir = makeFixtureDir();
   330	    writeFileSync(join(dir, 'studio.css'), '.a .b { color: ink; }\n');
   331	    expect(checkLinkLiveness(specWithLink('studio.css', '.a>.b'), dir).ok).toBe(false);
   332	  });
   333	
   334	  it('descendant combinators keep their single-space normalization alongside the combinator pass', () => {
   335	    const dir = makeFixtureDir();
   336	    writeFileSync(join(dir, 'studio.css'), '.list   >   .item   .label { color: ink; }\n');
   337	    expect(checkLinkLiveness(specWithLink('studio.css', '.list > .item .label'), dir).ok).toBe(true);
   338	  });
   339	
   340	  it('the ~= attribute operator is untouched — [a~="x"] matches a spaced [a ~= "x"] source', () => {
   341	    // Attribute canonicalization owns operator spacing (the ~ there is part of
   342	    // ~=); the combinator pass must not corrupt it.
   343	    const dir = makeFixtureDir();
   344	    writeFileSync(join(dir, 'studio.css'), '.real[a ~= "x"] { color: ink; }\n');
   345	    expect(checkLinkLiveness(specWithLink('studio.css', '.real[a~="x"]'), dir).ok).toBe(true);
   346	  });
   347	
   348	  it('nth-child + spelling unifies as a consequence — :nth-child(2n+1) matches :nth-child(2n + 1)', () => {
   349	    const dir = makeFixtureDir();
   350	    writeFileSync(join(dir, 'studio.css'), '.row:nth-child(2n + 1) { background: paper; }\n');
   351	    expect(
   352	      checkLinkLiveness(specWithLink('studio.css', '.row:nth-child(2n+1)'), dir).ok,
   353	    ).toBe(true);
   354	  });
   355	});
   356	
   357	describe('checkLinkLiveness — leading discriminator required (AUDIT-round4-claude-01)', () => {
   358	  it('a bare-ident query does not match a class definition — btn-primary vs .btn-primary', () => {
   359	    // Dropping the dot from a real class name must go DEAD, not silently green:
   360	    // a class definition does not define the type selector of the same name.
   361	    const dir = makeFixtureDir();
   362	    writeFileSync(join(dir, 'studio.css'), '.btn-primary { color: navy; }\n');
   363	    const result = checkLinkLiveness(specWithLink('studio.css', 'btn-primary'), dir);
   364	    expect(result.ok).toBe(false);
   365	    expect(result.findings.some((f) => f.rule === 'dead-link-selector')).toBe(true);
   366	  });
   367	
   368	  it('a bare-ident query does not match a class definition — header vs .header', () => {
   369	    const dir = makeFixtureDir();
   370	    writeFileSync(join(dir, 'studio.css'), '.header { position: sticky; }\n');
   371	    expect(checkLinkLiveness(specWithLink('studio.css', 'header'), dir).ok).toBe(false);
   372	  });
   373	
   374	  it('a bare-ident query does not match an id definition — header vs #header', () => {
   375	    const dir = makeFixtureDir();
   376	    writeFileSync(join(dir, 'studio.css'), '#header { position: sticky; }\n');
   377	    expect(checkLinkLiveness(specWithLink('studio.css', 'header'), dir).ok).toBe(false);
   378	  });
   379	
   380	  it('a bare-ident query does not match an attribute NAME — ghost vs [ghost]', () => {
   381	    // Names mirror values (AUDIT-round2): an attribute name is not a selector
   382	    // definition for a query that never named the bracket.
   383	    const dir = makeFixtureDir();
   384	    writeFileSync(join(dir, 'studio.css'), '[ghost] { color: ink; }\n');
   385	    expect(checkLinkLiveness(specWithLink('studio.css', 'ghost'), dir).ok).toBe(false);
   386	  });
   387	
   388	  it('a genuine type selector still matches a bare element rule — header vs header', () => {
   389	    const dir = makeFixtureDir();
   390	    writeFileSync(join(dir, 'studio.css'), 'header { position: sticky; }\n');
   391	    const result = checkLinkLiveness(specWithLink('studio.css', 'header'), dir);
   392	    expect(result.findings).toEqual([]);
   393	    expect(result.ok).toBe(true);
   394	  });
   395	
   396	  it('a type selector after a combinator still matches — header vs main > header', () => {
   397	    const dir = makeFixtureDir();
   398	    writeFileSync(join(dir, 'studio.css'), 'main > header { position: sticky; }\n');
   399	    expect(checkLinkLiveness(specWithLink('studio.css', 'header'), dir).ok).toBe(true);
   400	  });
   401	
   402	  it('a class query still matches its class definition — .btn vs .btn (pin)', () => {
   403	    const dir = makeFixtureDir();
   404	    writeFileSync(join(dir, 'studio.css'), '.btn { color: navy; }\n');
   405	    expect(checkLinkLiveness(specWithLink('studio.css', '.btn'), dir).ok).toBe(true);
   406	  });
   407	});
   408	
   409	describe('checkLinkLiveness — @keyframes steps are not preludes (AUDIT-round4-claude-04)', () => {
   410	  it('a "from" query is dead against a @keyframes block — step selectors are not definitions', () => {
   411	    const dir = makeFixtureDir();
   412	    writeFileSync(
   413	      join(dir, 'studio.css'),
   414	      '@keyframes spin { from { opacity: 0; } to { opacity: 1; } }\n',
   415	    );
   416	    const result = checkLinkLiveness(specWithLink('studio.css', 'from'), dir);
   417	    expect(result.ok).toBe(false);
   418	    expect(result.findings.some((f) => f.rule === 'dead-link-selector')).toBe(true);
   419	  });
   420	
   421	  it('prefix-tolerant — a "from" query is dead against @-webkit-keyframes too', () => {
   422	    const dir = makeFixtureDir();
   423	    writeFileSync(
   424	      join(dir, 'studio.css'),
   425	      '@-webkit-keyframes spin { from { opacity: 0; } to { opacity: 1; } }\n',
   426	    );
   427	    expect(checkLinkLiveness(specWithLink('studio.css', 'from'), dir).ok).toBe(false);
   428	  });
   429	
   430	  it('a rule following a skipped @keyframes block is still live — the skip is exact', () => {
   431	    const dir = makeFixtureDir();
   432	    writeFileSync(
   433	      join(dir, 'studio.css'),
   434	      '@keyframes spin { from { opacity: 0; } to { opacity: 1; } }\n.after { color: ink; }\n',
   435	    );
   436	    expect(checkLinkLiveness(specWithLink('studio.css', '.after'), dir).ok).toBe(true);
   437	  });
   438	
   439	  it('a rule inside @media remains live — conditional at-rule descent unchanged (pin)', () => {
   440	    const dir = makeFixtureDir();
   441	    writeFileSync(
   442	      join(dir, 'studio.css'),
   443	      '@media (min-width: 80rem) {\n  .rail { width: 16rem; }\n}\n',
   444	    );
   445	    expect(checkLinkLiveness(specWithLink('studio.css', '.rail'), dir).ok).toBe(true);
   446	  });
   447	});
   448	
   449	describe('checkLinkLiveness — CSS nesting matched flat, not ancestor-composed (AUDIT-round4-claude-02 pins)', () => {
   450	  // These two tests PIN the stated approximation (they pass against current
   451	  // behavior): preludes are collected flat at their own nesting depth, never
   452	  // composed with ancestors — on nested sources, rules anchor to the LEAF
   453	  // selector. The approximation is documented in selector-canon.ts and
   454	  // collectSelectorPreludes, not discovered by an author's dead link.
   455	  it('a composed query is dead against a nested source — .btn .icon vs .btn { .icon {} } (pin)', () => {
   456	    const dir = makeFixtureDir();
   457	    writeFileSync(join(dir, 'studio.css'), '.btn { color: navy; .icon { width: 1rem; } }\n');
   458	    const result = checkLinkLiveness(specWithLink('studio.css', '.btn .icon'), dir);
   459	    expect(result.ok).toBe(false);
   460	    expect(result.findings.some((f) => f.rule === 'dead-link-selector')).toBe(true);
   461	  });
   462	
   463	  it('the leaf query is live against the same nested source — anchor to the leaf selector (pin)', () => {
   464	    const dir = makeFixtureDir();
   465	    writeFileSync(join(dir, 'studio.css'), '.btn { color: navy; .icon { width: 1rem; } }\n');
   466	    const result = checkLinkLiveness(specWithLink('studio.css', '.icon'), dir);
   467	    expect(result.findings).toEqual([]);
   468	    expect(result.ok).toBe(true);
   469	  });
   470	});
   471	
   472	describe('checkLinkLiveness — validated-scope boundary (non-CSS targets, visible)', () => {
   473	  it('records a non-.css target as skipped — no finding, never silent', () => {
   474	    const dir = makeFixtureDir();
   475	    writeFileSync(join(dir, 'styles.ts'), 'export const btn = css`color: navy;`;\n');
   476	    const result = checkLinkLiveness(specWithLink('styles.ts', '.btn'), dir);
   477	    expect(result.findings).toEqual([]);
   478	    expect(result.ok).toBe(true);
   479	    expect(result.skipped).toHaveLength(1);
   480	    expect(result.skipped[0]).toMatchObject({
   481	      ruleId: 'probe',
   482	      reason: 'non-css-target',
   483	      link: { path: 'styles.ts', selector: '.btn' },
   484	    });
   485	  });
   486	});

exec
/bin/zsh -lc "nl -ba plugins/design-control/src/__tests__/design-language/check-spec-file.test.ts | sed -n '1,190p'" in /Users/orion/work/deskwork-work/design-control
 succeeded in 0ms:
     1	/**
     2	 * File-level + CLI-core tests for `check-design-spec` (Phase 2).
     3	 *
     4	 * `checkDesignSpecFile` composes the two axes — markdown schema validation +
     5	 * static link-liveness — over a spec FILE, resolving css paths relative to the
     6	 * spec's own directory. `runCheckDesignSpec` is the tested CLI core behind
     7	 * `bin/check-design-spec` (the shim only dispatches), mirroring the
     8	 * check-wireframe exit contract: 0 green / 1 findings-or-error / 2 usage.
     9	 *
    10	 * Real-fs temp fixtures per .claude/rules/testing.md.
    11	 */
    12	
    13	import { mkdtempSync, rmSync, writeFileSync } from 'node:fs';
    14	import { tmpdir } from 'node:os';
    15	import { join } from 'node:path';
    16	import { afterEach, describe, expect, it } from 'vitest';
    17	import { checkDesignSpecFile, runCheckDesignSpec } from '@/design-language/check-spec-file';
    18	
    19	const tempDirs: string[] = [];
    20	
    21	function makeFixtureDir(): string {
    22	  const dir = mkdtempSync(join(tmpdir(), 'design-language-cli-'));
    23	  tempDirs.push(dir);
    24	  return dir;
    25	}
    26	
    27	afterEach(() => {
    28	  while (tempDirs.length > 0) {
    29	    rmSync(tempDirs.pop() as string, { recursive: true, force: true });
    30	  }
    31	});
    32	
    33	interface CapturedIo {
    34	  readonly out: string[];
    35	  readonly err: string[];
    36	  readonly io: { out(line: string): void; err(line: string): void };
    37	}
    38	
    39	function captureIo(): CapturedIo {
    40	  const out: string[] = [];
    41	  const err: string[] = [];
    42	  return { out, err, io: { out: (line) => out.push(line), err: (line) => err.push(line) } };
    43	}
    44	
    45	const GREEN_SPEC = `# Design language: fixture
    46	
    47	### rule: ink-primary
    48	- kind: palette
    49	- css: studio.css .btn-primary
    50	- example: dashboard compose button
    51	- do: Use the ink palette for primary actions.
    52	`;
    53	
    54	function writeGreenFixture(dir: string): string {
    55	  writeFileSync(join(dir, 'studio.css'), '.btn-primary { color: navy; }\n');
    56	  const specPath = join(dir, 'design-language.md');
    57	  writeFileSync(specPath, GREEN_SPEC);
    58	  return specPath;
    59	}
    60	
    61	describe('checkDesignSpecFile', () => {
    62	  it('passes a hand-authored spec whose links are live', () => {
    63	    const specPath = writeGreenFixture(makeFixtureDir());
    64	    const result = checkDesignSpecFile(specPath);
    65	    expect(result.findings).toEqual([]);
    66	    expect(result.ok).toBe(true);
    67	    expect(result.spec.rules).toHaveLength(1);
    68	  });
    69	
    70	  it('combines schema findings and liveness findings in one result', () => {
    71	    const dir = makeFixtureDir();
    72	    writeFileSync(join(dir, 'studio.css'), '.real { color: ink; }\n');
    73	    const specPath = join(dir, 'design-language.md');
    74	    writeFileSync(
    75	      specPath,
    76	      `### rule: no-example
    77	- kind: palette
    78	- css: studio.css .real
    79	- do: x
    80	
    81	### rule: dead-link
    82	- kind: component
    83	- css: studio.css .ghost
    84	- example: somewhere
    85	- do: x
    86	`,
    87	    );
    88	    const result = checkDesignSpecFile(specPath);
    89	    const rules = result.findings.map((f) => f.rule);
    90	    expect(rules).toContain('missing-example');
    91	    expect(rules).toContain('dead-link-selector');
    92	    expect(result.ok).toBe(false);
    93	  });
    94	
    95	  it('throws loud on an unreadable spec file (never a clean verdict)', () => {
    96	    expect(() => checkDesignSpecFile(join(makeFixtureDir(), 'absent.md'))).toThrow();
    97	  });
    98	
    99	  it('surfaces a schema defect AND a dead link on the SAME rule in one run (no waves)', () => {
   100	    const dir = makeFixtureDir();
   101	    writeFileSync(join(dir, 'studio.css'), '.real { color: ink; }\n');
   102	    const specPath = join(dir, 'design-language.md');
   103	    writeFileSync(
   104	      specPath,
   105	      `### rule: both-defects
   106	- kind: palette
   107	- css: studio.css .ghost
   108	- do: x
   109	`,
   110	    );
   111	    const result = checkDesignSpecFile(specPath);
   112	    const rules = result.findings.map((f) => f.rule);
   113	    expect(rules).toContain('missing-example');
   114	    expect(rules).toContain('dead-link-selector');
   115	    expect(result.spec.rules).toEqual([]);
   116	  });
   117	
   118	  it('surfaces a duplicate section’s dead css link in the same run as duplicate-rule-id', () => {
   119	    const dir = makeFixtureDir();
   120	    writeFileSync(join(dir, 'studio.css'), '.real { color: ink; }\n');
   121	    const specPath = join(dir, 'design-language.md');
   122	    writeFileSync(
   123	      specPath,
   124	      `### rule: ink
   125	- kind: palette
   126	- css: studio.css .real
   127	- example: a button
   128	- do: x
   129	
   130	### rule: ink
   131	- kind: palette
   132	- css: studio.css .ghost
   133	- example: a button
   134	- do: x
   135	`,
   136	    );
   137	    const result = checkDesignSpecFile(specPath);
   138	    const rules = result.findings.map((f) => f.rule);
   139	    expect(rules).toContain('duplicate-rule-id');
   140	    expect(rules).toContain('dead-link-selector');
   141	    expect(result.spec.rules.map((r) => r.id)).toEqual(['ink']);
   142	  });
   143	
   144	  it('records a non-css auxiliary link as skipped, never silently dropped', () => {
   145	    const dir = makeFixtureDir();
   146	    writeFileSync(join(dir, 'styles.ts'), 'export const x = 1;\n');
   147	    const specPath = join(dir, 'design-language.md');
   148	    writeFileSync(
   149	      specPath,
   150	      `### rule: invalid-housing
   151	- kind: palette
   152	- css: styles.ts .btn
   153	- do: x
   154	`,
   155	    );
   156	    const result = checkDesignSpecFile(specPath);
   157	    expect(result.findings.map((f) => f.rule)).toEqual(['missing-example']);
   158	    expect(result.skipped).toHaveLength(1);
   159	    expect(result.skipped[0]).toMatchObject({
   160	      ruleId: 'invalid-housing',
   161	      reason: 'non-css-target',
   162	    });
   163	  });
   164	});
   165	
   166	describe('runCheckDesignSpec — exit contract', () => {
   167	  it('exit 0 + green line on a passing spec', () => {
   168	    const specPath = writeGreenFixture(makeFixtureDir());
   169	    const { out, io } = captureIo();
   170	    expect(runCheckDesignSpec([specPath], io)).toBe(0);
   171	    expect(out.join('\n')).toContain('0 findings');
   172	  });
   173	
   174	  it('exit 1 + findings on stderr for a dead selector', () => {
   175	    const dir = makeFixtureDir();
   176	    writeFileSync(join(dir, 'studio.css'), '.real { color: ink; }\n');
   177	    const specPath = join(dir, 'design-language.md');
   178	    writeFileSync(
   179	      specPath,
   180	      `### rule: dead
   181	- kind: palette
   182	- css: studio.css .ghost
   183	- example: somewhere
   184	- do: x
   185	`,
   186	    );
   187	    const { err, io } = captureIo();
   188	    expect(runCheckDesignSpec([specPath], io)).toBe(1);
   189	    expect(err.join('\n')).toContain('dead-link-selector');
   190	  });

codex
### Trailing whitespace after a css path can create an empty selector link

Finding-ID: AUDIT-BARRAGE-codex-01
Status:     open
Severity:   medium
Surface:    plugins/design-control/src/design-language/schema.ts:192-219; plugins/design-control/src/design-language/link-liveness.ts:259-261

`recordField` only treats a `css:` value as missing its selector when `value.search(/\s/)` returns `-1`. If the author writes `- css: styles.ts   ` or `- css: studio.css   `, the first whitespace exists, so the parser records `{ path: "styles.ts", selector: "" }` at lines 216-219 instead of emitting `malformed-css-link`. That also makes `validateSection` consider the rule to have a css link because `section.cssLinks.length > 0`.

The blast radius is medium because this can become a silent green in the non-CSS path: `checkCssLinkLiveness` skips non-`.css` targets before validating selector content at lines 259-261, and skipped links do not fail the file check. A rule with `kind`, `example`, `do`, and only `css: styles.ts   ` can therefore pass schema and liveness with an empty selector, despite the documented `css: <path> <selector>` contract. The reasonable fix is to compute `const selector = value.slice(spaceAt).trim()` and reject it as `malformed-css-link` when empty, with tests for both `.css` and skipped non-CSS targets carrying trailing whitespace but no selector.
tokens used
83,074
