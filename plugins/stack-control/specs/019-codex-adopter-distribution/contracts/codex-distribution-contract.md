# Contract: Codex adopter distribution

## Rules

1. Codex adopters can install `stack-control` without cloning the deskwork repo.
2. The install path is documented as a product distribution flow, not a local
   development workaround.
3. Distribution metadata resolves to released plugin versions.
