# Dashboard BFF API Contract (Browser-Facing)

This document will be completed in Phase 4 (T017) and Phase 5 (T020) as the browser UI contract is finalized.

## Status

- **Phase 4 (US2 — Live Fleet Table)**: `/api/instances`, `/api/stream` contract
- **Phase 5 (US3 — Drill-in Drawer)**: deep-linkable state, live updates under open drawer

## Implementation Notes

UI implementation is deferred to `/frontend-design` (FR-030) — no framework is chosen yet. This contract boundary document describes what the BFF serves; the browser UI's implementation is a downstream effort after the design pass.
