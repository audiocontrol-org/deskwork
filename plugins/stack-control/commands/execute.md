---
description: "Run a Spec Kit spec via native /speckit-implement in-session, with deskwork governance firing automatically afterward"
---

Invoke the `execute` skill from the `stack-control` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
