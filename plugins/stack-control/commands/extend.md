---
description: "Refine an EXISTING Spec Kit spec in place, in-session — edit/iterate/review loop reusing the current spec dir, bringing it to runnable"
---

Invoke the `extend` skill from the `stack-control` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
