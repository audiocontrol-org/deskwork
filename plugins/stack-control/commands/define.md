---
description: "Author a NEW Spec Kit spec for a new feature, in-session, via native /speckit-specify + the downstream chain (spec-authoring only)"
---

Invoke the `define` skill from the `stack-control` plugin via the Skill tool. The skill's SKILL.md is the canonical procedure — follow it end to end.
