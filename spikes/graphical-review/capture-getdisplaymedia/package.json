{
  "name": "capture-getdisplaymedia-spike",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phase 1 Task 1.4.1 spike — navigator.mediaDevices.getDisplayMedia() screen capture, single-frame snapshot, PNG download. Vanilla JS, not part of the workspace.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify": "node scripts/verify.mjs"
  },
  "dependencies": {},
  "devDependencies": {
    "playwright": "^1.60.0",
    "vite": "^5.4.0"
  }
}
