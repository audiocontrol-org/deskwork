{
  "name": "deskwork",
  "version": "0.15.0",
  "private": true,
  "description": "Open-source Claude Code plugins monorepo — deskwork editorial calendar plugin, feature-image, analytics",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/audiocontrol-org/deskwork.git"
  },
  "license": "GPL-3.0-or-later",
  "workspaces": [
    "packages/core",
    "packages/cli",
    "packages/bridge",
    "packages/studio",
    "plugins/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prepare": "command -v husky >/dev/null 2>&1 && husky || true",
    "build": "npm run build --workspaces --if-present && bash scripts/link-workspace-bins.sh",
    "dev": "npm run build && (npm run dev --workspace @deskwork/core & npm run dev --workspace @deskwork/cli & npm run dev --workspace @deskwork/bridge & npm run dev --workspace @deskwork/studio & wait)",
    "version:bump": "tsx scripts/bump-version.ts"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "jsdom": "^29.1.1",
    "tsx": "^4.21.0"
  }
}
